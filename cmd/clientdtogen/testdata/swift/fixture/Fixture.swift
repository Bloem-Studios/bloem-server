// Code generated by cmd/clientdtogen from bloem-server cmd/clientdtogen/internal/graph/testdata/fixture. DO NOT EDIT.
// Server revision: 0000000000000000000000000000000000000000 (generator v1, registry cmd/clientdtogen/internal/graph/graphtest/graphtest.go)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `cmd/clientdtogen/internal/graph/testdata/fixture`.
public enum Fixture {
    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.BloemOnly`. Direction: both. Dialect: bloem. Gate: cap.bloem. Registered root.
    public struct BloemOnly: Codable, Hashable, Sendable {
        public let card: PromoCard

        public enum CodingKeys: String, CodingKey {
            case card = "card"
        }

        public init(
            card: PromoCard = PromoCard()
        ) {
            self.card = card
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.card = try container.decodeIfPresent(PromoCard.self, forKey: .card) ?? PromoCard()
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.card, forKey: .card)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Child`. Direction: both. Dialect: upstream-compat.
    public struct Child: Codable, Hashable, Sendable {
        public let name: String
        /// Self-referential on the wire; stored through a box.
        public var next: Child? { _next.value }
        private let _next: Indirect<Child?>

        public enum CodingKeys: String, CodingKey {
            case name = "name"
            case next = "next"
        }

        public init(
            name: String = "",
            next: Child? = nil
        ) {
            self.name = name
            self._next = Indirect(next)
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self._next = Indirect(try container.decodeIfPresent(Child.self, forKey: .next))
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.name, forKey: .name)
            try container.encodeIfPresent(self.next, forKey: .next)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Collections`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct Collections: Codable, Hashable, Sendable {
        public let strings: [String]
        public let structs: [Child]
        public let optionalList: [Child]?
        public let headers: [String: String]
        public let structMap: [String: Child]
        public let enumMap: [String: Int]
        public let fixed: [Int]
        public let nested: [[String]]
        public let ptrList: [Child?]
        public let fixedOmit: [Int]
        public let structOmit: Child
        public let mapOmit: [String: Int]

        public enum CodingKeys: String, CodingKey {
            case strings = "strings"
            case structs = "structs"
            case optionalList = "optional_list"
            case headers = "headers"
            case structMap = "struct_map"
            case enumMap = "enum_map"
            case fixed = "fixed"
            case nested = "nested"
            case ptrList = "ptr_list"
            case fixedOmit = "fixed_omit"
            case structOmit = "struct_omit"
            case mapOmit = "map_omit"
        }

        public init(
            strings: [String] = [],
            structs: [Child] = [],
            optionalList: [Child]? = nil,
            headers: [String: String] = [:],
            structMap: [String: Child] = [:],
            enumMap: [String: Int] = [:],
            fixed: [Int] = [],
            nested: [[String]] = [],
            ptrList: [Child?] = [],
            fixedOmit: [Int] = [],
            structOmit: Child = Child(),
            mapOmit: [String: Int] = [:]
        ) {
            self.strings = strings
            self.structs = structs
            self.optionalList = optionalList
            self.headers = headers
            self.structMap = structMap
            self.enumMap = enumMap
            self.fixed = fixed
            self.nested = nested
            self.ptrList = ptrList
            self.fixedOmit = fixedOmit
            self.structOmit = structOmit
            self.mapOmit = mapOmit
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.strings = try container.decodeIfPresent([String].self, forKey: .strings) ?? []
            self.structs = try container.decodeIfPresent([Child].self, forKey: .structs) ?? []
            self.optionalList = try container.decodeIfPresent([Child].self, forKey: .optionalList)
            self.headers = try container.decodeIfPresent([String: String].self, forKey: .headers) ?? [:]
            self.structMap = try container.decodeIfPresent([String: Child].self, forKey: .structMap) ?? [:]
            self.enumMap = try container.decodeIfPresent([String: Int].self, forKey: .enumMap) ?? [:]
            self.fixed = try container.decodeIfPresent([Int].self, forKey: .fixed) ?? []
            self.nested = try container.decodeIfPresent([[String]].self, forKey: .nested) ?? []
            self.ptrList = try container.decodeIfPresent([Child?].self, forKey: .ptrList) ?? []
            self.fixedOmit = try container.decodeIfPresent([Int].self, forKey: .fixedOmit) ?? []
            self.structOmit = try container.decodeIfPresent(Child.self, forKey: .structOmit) ?? Child()
            self.mapOmit = try container.decodeIfPresent([String: Int].self, forKey: .mapOmit) ?? [:]
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.strings, forKey: .strings)
            try container.encode(self.structs, forKey: .structs)
            try container.encodeIfPresent(self.optionalList, forKey: .optionalList)
            try container.encode(self.headers, forKey: .headers)
            try container.encode(self.structMap, forKey: .structMap)
            try container.encode(self.enumMap, forKey: .enumMap)
            try container.encode(self.fixed, forKey: .fixed)
            try container.encode(self.nested, forKey: .nested)
            try container.encode(self.ptrList, forKey: .ptrList)
            try container.encode(self.fixedOmit, forKey: .fixedOmit)
            try container.encode(self.structOmit, forKey: .structOmit)
            try container.encode(self.mapOmit, forKey: .mapOmit)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Compat`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct Compat: Codable, Hashable, Sendable {
        public let plain: String
        /// Dialect: bloem.
        public let promo: PromoCard?

        public enum CodingKeys: String, CodingKey {
            case plain = "plain"
            case promo = "promo"
        }

        public init(
            plain: String = "",
            promo: PromoCard? = nil
        ) {
            self.plain = plain
            self.promo = promo
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.plain = try container.decodeIfPresent(String.self, forKey: .plain) ?? ""
            self.promo = try container.decodeIfPresent(PromoCard.self, forKey: .promo)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.plain, forKey: .plain)
            try container.encodeIfPresent(self.promo, forKey: .promo)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Embedded`. Direction: request. Dialect: upstream-compat. Registered root.
    public struct Embedded: Codable, Hashable, Sendable {
        public let id: Int64
        public let deep: Inner
        public let kind: String
        public let extra: String
        public let dash: String
        public let mixed: String
        public let low: String
        public let zero: Child?
        public let direct: Child
        public let ptr: Child?

        public enum CodingKeys: String, CodingKey {
            case id = "id"
            case deep = "deep"
            case kind = "kind"
            case extra = "extra"
            case dash = "-"
            case mixed = "mixed"
            case low = "low"
            case zero = "zero"
            case direct = "direct"
            case ptr = "ptr"
        }

        public init(
            id: Int64,
            deep: Inner,
            kind: String,
            extra: String,
            dash: String,
            mixed: String,
            low: String,
            zero: Child? = nil,
            direct: Child,
            ptr: Child? = nil
        ) {
            self.id = id
            self.deep = deep
            self.kind = kind
            self.extra = extra
            self.dash = dash
            self.mixed = mixed
            self.low = low
            self.zero = zero
            self.direct = direct
            self.ptr = ptr
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.id = try container.decode(Int64.self, forKey: .id)
            self.deep = try container.decode(Inner.self, forKey: .deep)
            self.kind = try container.decode(String.self, forKey: .kind)
            self.extra = try container.decode(String.self, forKey: .extra)
            self.dash = try container.decode(String.self, forKey: .dash)
            self.mixed = try container.decode(String.self, forKey: .mixed)
            self.low = try container.decode(String.self, forKey: .low)
            self.zero = try container.decodeIfPresent(Child.self, forKey: .zero)
            self.direct = try container.decode(Child.self, forKey: .direct)
            self.ptr = try container.decodeIfPresent(Child.self, forKey: .ptr)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.id, forKey: .id)
            try container.encode(self.deep, forKey: .deep)
            try container.encode(self.kind, forKey: .kind)
            try container.encode(self.extra, forKey: .extra)
            try container.encode(self.dash, forKey: .dash)
            try container.encode(self.mixed, forKey: .mixed)
            try container.encode(self.low, forKey: .low)
            try container.encodeIfPresent(self.zero, forKey: .zero)
            try container.encode(self.direct, forKey: .direct)
            try container.encodeIfPresent(self.ptr, forKey: .ptr)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Gated`. Direction: response. Dialect: upstream-compat. Gate: cap.gated. Registered root.
    public struct Gated: Codable, Hashable, Sendable {
        public let child: GatedChild
        public let only: GatedOnly
        public let promo: PromoCard?

        public enum CodingKeys: String, CodingKey {
            case child = "child"
            case only = "only"
            case promo = "promo"
        }

        public init(
            child: GatedChild = GatedChild(),
            only: GatedOnly = GatedOnly(),
            promo: PromoCard? = nil
        ) {
            self.child = child
            self.only = only
            self.promo = promo
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.child = try container.decodeIfPresent(GatedChild.self, forKey: .child) ?? GatedChild()
            self.only = try container.decodeIfPresent(GatedOnly.self, forKey: .only) ?? GatedOnly()
            self.promo = try container.decodeIfPresent(PromoCard.self, forKey: .promo)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.child, forKey: .child)
            try container.encode(self.only, forKey: .only)
            try container.encodeIfPresent(self.promo, forKey: .promo)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Gated2`. Direction: response. Dialect: upstream-compat. Gate: cap.other. Registered root.
    public struct Gated2: Codable, Hashable, Sendable {
        public let only: GatedOnly

        public enum CodingKeys: String, CodingKey {
            case only = "only"
        }

        public init(
            only: GatedOnly = GatedOnly()
        ) {
            self.only = only
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.only = try container.decodeIfPresent(GatedOnly.self, forKey: .only) ?? GatedOnly()
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.only, forKey: .only)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.GatedChild`. Direction: response. Dialect: upstream-compat.
    public struct GatedChild: Codable, Hashable, Sendable {
        public let v: String

        public enum CodingKeys: String, CodingKey {
            case v = "v"
        }

        public init(
            v: String = ""
        ) {
            self.v = v
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.v = try container.decodeIfPresent(String.self, forKey: .v) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.v, forKey: .v)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.GatedOnly`. Direction: response. Dialect: upstream-compat. Gate: cap.gated|cap.other.
    public struct GatedOnly: Codable, Hashable, Sendable {
        public let v: String

        public enum CodingKeys: String, CodingKey {
            case v = "v"
        }

        public init(
            v: String = ""
        ) {
            self.v = v
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.v = try container.decodeIfPresent(String.self, forKey: .v) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.v, forKey: .v)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Inner`. Direction: request. Dialect: upstream-compat.
    public struct Inner: Codable, Hashable, Sendable {
        public let depth: Int

        public enum CodingKeys: String, CodingKey {
            case depth = "depth"
        }

        public init(
            depth: Int
        ) {
            self.depth = depth
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.depth = try container.decode(Int.self, forKey: .depth)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.depth, forKey: .depth)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.PromoCard`. Direction: both. Dialect: upstream-compat.
    public struct PromoCard: Codable, Hashable, Sendable {
        public let title: String

        public enum CodingKeys: String, CodingKey {
            case title = "title"
        }

        public init(
            title: String = ""
        ) {
            self.title = title
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.title, forKey: .title)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Protocol`. Direction: both. Dialect: upstream-compat. Registered root.
    public struct `Protocol`: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let HLS: `Protocol` = `Protocol`(rawValue: "hls")
        public static let PROGRESSIVE: `Protocol` = `Protocol`(rawValue: "progressive")
        public static let LATE: `Protocol` = `Protocol`(rawValue: "late")
        public static let KNOWN: [`Protocol`] = [HLS, PROGRESSIVE, LATE]
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Request`. Direction: request. Dialect: upstream-compat. Registered root.
    public struct Request: Codable, Hashable, Sendable {
        public let shared: Shared
        public let proto: `Protocol`

        public enum CodingKeys: String, CodingKey {
            case shared = "shared"
            case proto = "proto"
        }

        public init(
            shared: Shared,
            proto: `Protocol`
        ) {
            self.shared = shared
            self.proto = proto
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.shared = try container.decode(Shared.self, forKey: .shared)
            self.proto = try container.decode(`Protocol`.self, forKey: .proto)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.shared, forKey: .shared)
            try container.encode(self.proto, forKey: .proto)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Response`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct Response: Codable, Hashable, Sendable {
        public let target: Other.Target
        public let rate: FrameRateWire?
        public let ratePtr: FrameRateWire?
        public let shared: Shared
        public let gated: GatedChild

        public enum CodingKeys: String, CodingKey {
            case target = "target"
            case rate = "frame_rate"
            case ratePtr = "frame_rate_ptr"
            case shared = "shared"
            case gated = "gated_child"
        }

        public init(
            target: Other.Target = Other.Target(),
            rate: FrameRateWire? = nil,
            ratePtr: FrameRateWire? = nil,
            shared: Shared = Shared(),
            gated: GatedChild = GatedChild()
        ) {
            self.target = target
            self.rate = rate
            self.ratePtr = ratePtr
            self.shared = shared
            self.gated = gated
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.target = try container.decodeIfPresent(Other.Target.self, forKey: .target) ?? Other.Target()
            self.rate = try container.decodeIfPresent(FrameRateWire.self, forKey: .rate)
            self.ratePtr = try container.decodeIfPresent(FrameRateWire.self, forKey: .ratePtr)
            self.shared = try container.decodeIfPresent(Shared.self, forKey: .shared) ?? Shared()
            self.gated = try container.decodeIfPresent(GatedChild.self, forKey: .gated) ?? GatedChild()
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.target, forKey: .target)
            try container.encodeIfPresent(self.rate, forKey: .rate)
            try container.encodeIfPresent(self.ratePtr, forKey: .ratePtr)
            try container.encode(self.shared, forKey: .shared)
            try container.encode(self.gated, forKey: .gated)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Scalars`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct Scalars: Codable, Hashable, Sendable {
        public let s: String
        public let b: Bool
        public let i: Int
        public let i8: Int
        public let i16: Int
        public let i32: Int
        public let u8: Int
        public let u16: Int
        public let u32: Int
        public let i64: Int64
        public let u64: Int64
        public let f32: Double
        public let f64: Double
        public let t: String
        public let tp: String?
        public let u: String
        public let up: String?
        public let raw: BloemJSONValue?
        public let any: BloemJSONValue?
        public let anyMap: [String: BloemJSONValue]
        public let bytes: String
        public let label: String
        public let count: Int
        public let names: [String]
        public let proto: `Protocol`
        public let protoPtr: `Protocol`?
        public let intPtr: Int?
        public let omit: Int
        public let tOmit: String
        public let uOmit: String
        public let rOmit: BloemJSONValue?
        public let bOmit: String
        public let pOmit: `Protocol`

        public enum CodingKeys: String, CodingKey {
            case s = "s"
            case b = "b"
            case i = "i"
            case i8 = "i8"
            case i16 = "i16"
            case i32 = "i32"
            case u8 = "u8"
            case u16 = "u16"
            case u32 = "u32"
            case i64 = "i64"
            case u64 = "u64"
            case f32 = "f32"
            case f64 = "f64"
            case t = "t"
            case tp = "tp"
            case u = "u"
            case up = "up"
            case raw = "raw"
            case any = "any"
            case anyMap = "any_map"
            case bytes = "bytes"
            case label = "label"
            case count = "count"
            case names = "names"
            case proto = "proto"
            case protoPtr = "proto_ptr"
            case intPtr = "int_ptr"
            case omit = "omit"
            case tOmit = "t_omit"
            case uOmit = "u_omit"
            case rOmit = "raw_omit"
            case bOmit = "bytes_omit"
            case pOmit = "proto_omit"
        }

        public init(
            s: String = "",
            b: Bool = false,
            i: Int = 0,
            i8: Int = 0,
            i16: Int = 0,
            i32: Int = 0,
            u8: Int = 0,
            u16: Int = 0,
            u32: Int = 0,
            i64: Int64 = 0,
            u64: Int64 = 0,
            f32: Double = 0.0,
            f64: Double = 0.0,
            t: String = "",
            tp: String? = nil,
            u: String = "",
            up: String? = nil,
            raw: BloemJSONValue? = nil,
            any: BloemJSONValue? = nil,
            anyMap: [String: BloemJSONValue] = [:],
            bytes: String = "",
            label: String = "",
            count: Int = 0,
            names: [String] = [],
            proto: `Protocol` = `Protocol`(rawValue: ""),
            protoPtr: `Protocol`? = nil,
            intPtr: Int? = nil,
            omit: Int = 0,
            tOmit: String = "",
            uOmit: String = "",
            rOmit: BloemJSONValue? = nil,
            bOmit: String = "",
            pOmit: `Protocol` = `Protocol`(rawValue: "")
        ) {
            self.s = s
            self.b = b
            self.i = i
            self.i8 = i8
            self.i16 = i16
            self.i32 = i32
            self.u8 = u8
            self.u16 = u16
            self.u32 = u32
            self.i64 = i64
            self.u64 = u64
            self.f32 = f32
            self.f64 = f64
            self.t = t
            self.tp = tp
            self.u = u
            self.up = up
            self.raw = raw
            self.any = any
            self.anyMap = anyMap
            self.bytes = bytes
            self.label = label
            self.count = count
            self.names = names
            self.proto = proto
            self.protoPtr = protoPtr
            self.intPtr = intPtr
            self.omit = omit
            self.tOmit = tOmit
            self.uOmit = uOmit
            self.rOmit = rOmit
            self.bOmit = bOmit
            self.pOmit = pOmit
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.s = try container.decodeIfPresent(String.self, forKey: .s) ?? ""
            self.b = try container.decodeIfPresent(Bool.self, forKey: .b) ?? false
            self.i = try container.decodeIfPresent(Int.self, forKey: .i) ?? 0
            self.i8 = try container.decodeIfPresent(Int.self, forKey: .i8) ?? 0
            self.i16 = try container.decodeIfPresent(Int.self, forKey: .i16) ?? 0
            self.i32 = try container.decodeIfPresent(Int.self, forKey: .i32) ?? 0
            self.u8 = try container.decodeIfPresent(Int.self, forKey: .u8) ?? 0
            self.u16 = try container.decodeIfPresent(Int.self, forKey: .u16) ?? 0
            self.u32 = try container.decodeIfPresent(Int.self, forKey: .u32) ?? 0
            self.i64 = try container.decodeIfPresent(Int64.self, forKey: .i64) ?? 0
            self.u64 = try container.decodeIfPresent(Int64.self, forKey: .u64) ?? 0
            self.f32 = try container.decodeIfPresent(Double.self, forKey: .f32) ?? 0.0
            self.f64 = try container.decodeIfPresent(Double.self, forKey: .f64) ?? 0.0
            self.t = try container.decodeIfPresent(String.self, forKey: .t) ?? ""
            self.tp = try container.decodeIfPresent(String.self, forKey: .tp)
            self.u = try container.decodeIfPresent(String.self, forKey: .u) ?? ""
            self.up = try container.decodeIfPresent(String.self, forKey: .up)
            self.raw = try container.decodeIfPresent(BloemJSONValue.self, forKey: .raw)
            self.any = try container.decodeIfPresent(BloemJSONValue.self, forKey: .any)
            self.anyMap = try container.decodeIfPresent([String: BloemJSONValue].self, forKey: .anyMap) ?? [:]
            self.bytes = try container.decodeIfPresent(String.self, forKey: .bytes) ?? ""
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
            self.count = try container.decodeIfPresent(Int.self, forKey: .count) ?? 0
            self.names = try container.decodeIfPresent([String].self, forKey: .names) ?? []
            self.proto = try container.decodeIfPresent(`Protocol`.self, forKey: .proto) ?? `Protocol`(rawValue: "")
            self.protoPtr = try container.decodeIfPresent(`Protocol`.self, forKey: .protoPtr)
            self.intPtr = try container.decodeIfPresent(Int.self, forKey: .intPtr)
            self.omit = try container.decodeIfPresent(Int.self, forKey: .omit) ?? 0
            self.tOmit = try container.decodeIfPresent(String.self, forKey: .tOmit) ?? ""
            self.uOmit = try container.decodeIfPresent(String.self, forKey: .uOmit) ?? ""
            self.rOmit = try container.decodeIfPresent(BloemJSONValue.self, forKey: .rOmit)
            self.bOmit = try container.decodeIfPresent(String.self, forKey: .bOmit) ?? ""
            self.pOmit = try container.decodeIfPresent(`Protocol`.self, forKey: .pOmit) ?? `Protocol`(rawValue: "")
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.s, forKey: .s)
            try container.encode(self.b, forKey: .b)
            try container.encode(self.i, forKey: .i)
            try container.encode(self.i8, forKey: .i8)
            try container.encode(self.i16, forKey: .i16)
            try container.encode(self.i32, forKey: .i32)
            try container.encode(self.u8, forKey: .u8)
            try container.encode(self.u16, forKey: .u16)
            try container.encode(self.u32, forKey: .u32)
            try container.encode(self.i64, forKey: .i64)
            try container.encode(self.u64, forKey: .u64)
            try container.encode(self.f32, forKey: .f32)
            try container.encode(self.f64, forKey: .f64)
            try container.encode(self.t, forKey: .t)
            try container.encodeIfPresent(self.tp, forKey: .tp)
            try container.encode(self.u, forKey: .u)
            try container.encodeIfPresent(self.up, forKey: .up)
            try container.encodeIfPresent(self.raw, forKey: .raw)
            try container.encodeIfPresent(self.any, forKey: .any)
            try container.encode(self.anyMap, forKey: .anyMap)
            try container.encode(self.bytes, forKey: .bytes)
            try container.encode(self.label, forKey: .label)
            try container.encode(self.count, forKey: .count)
            try container.encode(self.names, forKey: .names)
            try container.encode(self.proto, forKey: .proto)
            try container.encodeIfPresent(self.protoPtr, forKey: .protoPtr)
            try container.encodeIfPresent(self.intPtr, forKey: .intPtr)
            try container.encode(self.omit, forKey: .omit)
            try container.encode(self.tOmit, forKey: .tOmit)
            try container.encode(self.uOmit, forKey: .uOmit)
            try container.encodeIfPresent(self.rOmit, forKey: .rOmit)
            try container.encode(self.bOmit, forKey: .bOmit)
            try container.encode(self.pOmit, forKey: .pOmit)
        }
    }

    /// Wire type `cmd/clientdtogen/internal/graph/testdata/fixture.Shared`. Direction: both. Dialect: upstream-compat.
    public struct Shared: Codable, Hashable, Sendable {
        public let v: String

        public enum CodingKeys: String, CodingKey {
            case v = "v"
        }

        public init(
            v: String = ""
        ) {
            self.v = v
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.v = try container.decodeIfPresent(String.self, forKey: .v) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.v, forKey: .v)
        }
    }
}
