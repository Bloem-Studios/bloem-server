// Code generated by cmd/clientdtogen from bloem-server internal/ambience. DO NOT EDIT.
// Server revision: 8c612bd9 (generator v1, registry contracts/client/v1/registry.json)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `internal/ambience`.
public enum Ambience {
    /// Wire type `internal/ambience.Assets`. Direction: response. Dialect: bloem.
    public struct Assets: Codable, Hashable, Sendable {
        public let bannerUrl: String
        public let sprites: [String]

        public enum CodingKeys: String, CodingKey {
            case bannerUrl = "banner_url"
            case sprites = "sprites"
        }

        public init(
            bannerUrl: String = "",
            sprites: [String] = []
        ) {
            self.bannerUrl = bannerUrl
            self.sprites = sprites
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.bannerUrl = try container.decodeIfPresent(String.self, forKey: .bannerUrl) ?? ""
            self.sprites = try container.decodeIfPresent([String].self, forKey: .sprites) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.bannerUrl, forKey: .bannerUrl)
            try container.encode(self.sprites, forKey: .sprites)
        }
    }

    /// Wire type `internal/ambience.Window`. Direction: response. Dialect: bloem.
    public struct Window: Codable, Hashable, Sendable {
        public let startsAt: String
        public let repeatYearly: Bool
        public let timezone: String
        public let endsAt: String

        public enum CodingKeys: String, CodingKey {
            case startsAt = "starts_at"
            case repeatYearly = "repeat_yearly"
            case timezone = "timezone"
            case endsAt = "ends_at"
        }

        public init(
            startsAt: String = "",
            repeatYearly: Bool = false,
            timezone: String = "",
            endsAt: String = ""
        ) {
            self.startsAt = startsAt
            self.repeatYearly = repeatYearly
            self.timezone = timezone
            self.endsAt = endsAt
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.startsAt = try container.decodeIfPresent(String.self, forKey: .startsAt) ?? ""
            self.repeatYearly = try container.decodeIfPresent(Bool.self, forKey: .repeatYearly) ?? false
            self.timezone = try container.decodeIfPresent(String.self, forKey: .timezone) ?? ""
            self.endsAt = try container.decodeIfPresent(String.self, forKey: .endsAt) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.startsAt, forKey: .startsAt)
            try container.encode(self.repeatYearly, forKey: .repeatYearly)
            try container.encode(self.timezone, forKey: .timezone)
            try container.encode(self.endsAt, forKey: .endsAt)
        }
    }

    /// Wire type `internal/ambience.Wire`. Direction: response. Dialect: bloem. Registered root.
    public struct Wire: Codable, Hashable, Sendable {
        public let id: String
        public let effectId: String
        public let window: Window
        public let intensity: Double
        public let surfaces: [String]
        public let assets: Assets

        public enum CodingKeys: String, CodingKey {
            case id = "id"
            case effectId = "effect_id"
            case window = "window"
            case intensity = "intensity"
            case surfaces = "surfaces"
            case assets = "assets"
        }

        public init(
            id: String = "",
            effectId: String = "",
            window: Window = Window(),
            intensity: Double = 0.0,
            surfaces: [String] = [],
            assets: Assets = Assets()
        ) {
            self.id = id
            self.effectId = effectId
            self.window = window
            self.intensity = intensity
            self.surfaces = surfaces
            self.assets = assets
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.id = try container.decodeIfPresent(String.self, forKey: .id) ?? ""
            self.effectId = try container.decodeIfPresent(String.self, forKey: .effectId) ?? ""
            self.window = try container.decodeIfPresent(Window.self, forKey: .window) ?? Window()
            self.intensity = try container.decodeIfPresent(Double.self, forKey: .intensity) ?? 0.0
            self.surfaces = try container.decodeIfPresent([String].self, forKey: .surfaces) ?? []
            self.assets = try container.decodeIfPresent(Assets.self, forKey: .assets) ?? Assets()
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.id, forKey: .id)
            try container.encode(self.effectId, forKey: .effectId)
            try container.encode(self.window, forKey: .window)
            try container.encode(self.intensity, forKey: .intensity)
            try container.encode(self.surfaces, forKey: .surfaces)
            try container.encode(self.assets, forKey: .assets)
        }
    }
}
