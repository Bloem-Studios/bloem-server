// Code generated by cmd/clientdtogen from bloem-server internal/catalog. DO NOT EDIT.
// Server revision: cc4b2ed9850295115f5f95209531885050cecc78 (generator v1, registry contracts/client/v1/registry.json)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `internal/catalog`.
public enum Catalog {
    /// Wire type `internal/catalog.AudiobookDetailExtension`. Direction: response. Dialect: bloem.
    public struct AudiobookDetailExtension: Codable, Hashable, Sendable {
        public let authors: [AudiobookPerson]
        public let narrators: [AudiobookPerson]
        public let publisher: String
        public let totalDurationSeconds: Int
        public let series: AudiobookSeriesGroup?
        public let otherNarrations: [AudiobookNarration]
        public let related: AudiobookRelatedContent

        public enum CodingKeys: String, CodingKey {
            case authors = "authors"
            case narrators = "narrators"
            case publisher = "publisher"
            case totalDurationSeconds = "total_duration_seconds"
            case series = "series"
            case otherNarrations = "other_narrations"
            case related = "related"
        }

        public init(
            authors: [AudiobookPerson] = [],
            narrators: [AudiobookPerson] = [],
            publisher: String = "",
            totalDurationSeconds: Int = 0,
            series: AudiobookSeriesGroup? = nil,
            otherNarrations: [AudiobookNarration] = [],
            related: AudiobookRelatedContent = AudiobookRelatedContent()
        ) {
            self.authors = authors
            self.narrators = narrators
            self.publisher = publisher
            self.totalDurationSeconds = totalDurationSeconds
            self.series = series
            self.otherNarrations = otherNarrations
            self.related = related
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.authors = try container.decodeIfPresent([AudiobookPerson].self, forKey: .authors) ?? []
            self.narrators = try container.decodeIfPresent([AudiobookPerson].self, forKey: .narrators) ?? []
            self.publisher = try container.decodeIfPresent(String.self, forKey: .publisher) ?? ""
            self.totalDurationSeconds = try container.decodeIfPresent(Int.self, forKey: .totalDurationSeconds) ?? 0
            self.series = try container.decodeIfPresent(AudiobookSeriesGroup.self, forKey: .series)
            self.otherNarrations = try container.decodeIfPresent([AudiobookNarration].self, forKey: .otherNarrations) ?? []
            self.related = try container.decodeIfPresent(AudiobookRelatedContent.self, forKey: .related) ?? AudiobookRelatedContent()
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.authors, forKey: .authors)
            try container.encode(self.narrators, forKey: .narrators)
            try container.encode(self.publisher, forKey: .publisher)
            try container.encode(self.totalDurationSeconds, forKey: .totalDurationSeconds)
            try container.encodeIfPresent(self.series, forKey: .series)
            try container.encode(self.otherNarrations, forKey: .otherNarrations)
            try container.encode(self.related, forKey: .related)
        }
    }

    /// Wire type `internal/catalog.AudiobookNarration`. Direction: response. Dialect: bloem.
    public struct AudiobookNarration: Codable, Hashable, Sendable {
        public let contentId: String
        public let title: String
        public let year: Int
        public let narrators: [String]

        public enum CodingKeys: String, CodingKey {
            case contentId = "content_id"
            case title = "title"
            case year = "year"
            case narrators = "narrators"
        }

        public init(
            contentId: String = "",
            title: String = "",
            year: Int = 0,
            narrators: [String] = []
        ) {
            self.contentId = contentId
            self.title = title
            self.year = year
            self.narrators = narrators
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.contentId = try container.decodeIfPresent(String.self, forKey: .contentId) ?? ""
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.year = try container.decodeIfPresent(Int.self, forKey: .year) ?? 0
            self.narrators = try container.decodeIfPresent([String].self, forKey: .narrators) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.contentId, forKey: .contentId)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.year, forKey: .year)
            try container.encode(self.narrators, forKey: .narrators)
        }
    }

    /// Wire type `internal/catalog.AudiobookPerson`. Direction: response. Dialect: bloem.
    public struct AudiobookPerson: Codable, Hashable, Sendable {
        public let personId: String
        public let name: String
        public let photoUrl: String
        public let photoThumbhash: String

        public enum CodingKeys: String, CodingKey {
            case personId = "person_id"
            case name = "name"
            case photoUrl = "photo_url"
            case photoThumbhash = "photo_thumbhash"
        }

        public init(
            personId: String = "",
            name: String = "",
            photoUrl: String = "",
            photoThumbhash: String = ""
        ) {
            self.personId = personId
            self.name = name
            self.photoUrl = photoUrl
            self.photoThumbhash = photoThumbhash
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.personId = try container.decodeIfPresent(String.self, forKey: .personId) ?? ""
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self.photoUrl = try container.decodeIfPresent(String.self, forKey: .photoUrl) ?? ""
            self.photoThumbhash = try container.decodeIfPresent(String.self, forKey: .photoThumbhash) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.personId, forKey: .personId)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.photoUrl, forKey: .photoUrl)
            try container.encode(self.photoThumbhash, forKey: .photoThumbhash)
        }
    }

    /// Wire type `internal/catalog.AudiobookRelatedContent`. Direction: response. Dialect: bloem.
    public struct AudiobookRelatedContent: Codable, Hashable, Sendable {
        public let alsoByAuthor: [AudiobookRelatedItem]
        public let similar: [AudiobookRelatedItem]

        public enum CodingKeys: String, CodingKey {
            case alsoByAuthor = "also_by_author"
            case similar = "similar"
        }

        public init(
            alsoByAuthor: [AudiobookRelatedItem] = [],
            similar: [AudiobookRelatedItem] = []
        ) {
            self.alsoByAuthor = alsoByAuthor
            self.similar = similar
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.alsoByAuthor = try container.decodeIfPresent([AudiobookRelatedItem].self, forKey: .alsoByAuthor) ?? []
            self.similar = try container.decodeIfPresent([AudiobookRelatedItem].self, forKey: .similar) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.alsoByAuthor, forKey: .alsoByAuthor)
            try container.encode(self.similar, forKey: .similar)
        }
    }

    /// Wire type `internal/catalog.AudiobookRelatedItem`. Direction: response. Dialect: bloem.
    public struct AudiobookRelatedItem: Codable, Hashable, Sendable {
        public let contentId: String
        public let title: String
        public let year: Int
        public let posterUrl: String
        public let seriesIndex: Int?

        public enum CodingKeys: String, CodingKey {
            case contentId = "content_id"
            case title = "title"
            case year = "year"
            case posterUrl = "poster_url"
            case seriesIndex = "series_index"
        }

        public init(
            contentId: String = "",
            title: String = "",
            year: Int = 0,
            posterUrl: String = "",
            seriesIndex: Int? = nil
        ) {
            self.contentId = contentId
            self.title = title
            self.year = year
            self.posterUrl = posterUrl
            self.seriesIndex = seriesIndex
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.contentId = try container.decodeIfPresent(String.self, forKey: .contentId) ?? ""
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.year = try container.decodeIfPresent(Int.self, forKey: .year) ?? 0
            self.posterUrl = try container.decodeIfPresent(String.self, forKey: .posterUrl) ?? ""
            self.seriesIndex = try container.decodeIfPresent(Int.self, forKey: .seriesIndex)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.contentId, forKey: .contentId)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.year, forKey: .year)
            try container.encode(self.posterUrl, forKey: .posterUrl)
            try container.encodeIfPresent(self.seriesIndex, forKey: .seriesIndex)
        }
    }

    /// Wire type `internal/catalog.AudiobookSeriesGroup`. Direction: response. Dialect: bloem.
    public struct AudiobookSeriesGroup: Codable, Hashable, Sendable {
        public let name: String
        public let entries: [AudiobookRelatedItem]

        public enum CodingKeys: String, CodingKey {
            case name = "name"
            case entries = "entries"
        }

        public init(
            name: String = "",
            entries: [AudiobookRelatedItem] = []
        ) {
            self.name = name
            self.entries = entries
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self.entries = try container.decodeIfPresent([AudiobookRelatedItem].self, forKey: .entries) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.entries, forKey: .entries)
        }
    }

    /// Wire type `internal/catalog.CastCredit`. Direction: response. Dialect: bloem.
    public struct CastCredit: Codable, Hashable, Sendable {
        public let name: String
        public let character: String
        public let order: Int
        public let personId: String
        public let tmdbId: String
        public let tvdbId: String
        public let imdbId: String
        public let plexGuid: String
        public let photoUrl: String
        public let photoThumbhash: String

        public enum CodingKeys: String, CodingKey {
            case name = "name"
            case character = "character"
            case order = "order"
            case personId = "person_id"
            case tmdbId = "tmdb_id"
            case tvdbId = "tvdb_id"
            case imdbId = "imdb_id"
            case plexGuid = "plex_guid"
            case photoUrl = "photo_url"
            case photoThumbhash = "photo_thumbhash"
        }

        public init(
            name: String = "",
            character: String = "",
            order: Int = 0,
            personId: String = "",
            tmdbId: String = "",
            tvdbId: String = "",
            imdbId: String = "",
            plexGuid: String = "",
            photoUrl: String = "",
            photoThumbhash: String = ""
        ) {
            self.name = name
            self.character = character
            self.order = order
            self.personId = personId
            self.tmdbId = tmdbId
            self.tvdbId = tvdbId
            self.imdbId = imdbId
            self.plexGuid = plexGuid
            self.photoUrl = photoUrl
            self.photoThumbhash = photoThumbhash
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self.character = try container.decodeIfPresent(String.self, forKey: .character) ?? ""
            self.order = try container.decodeIfPresent(Int.self, forKey: .order) ?? 0
            self.personId = try container.decodeIfPresent(String.self, forKey: .personId) ?? ""
            self.tmdbId = try container.decodeIfPresent(String.self, forKey: .tmdbId) ?? ""
            self.tvdbId = try container.decodeIfPresent(String.self, forKey: .tvdbId) ?? ""
            self.imdbId = try container.decodeIfPresent(String.self, forKey: .imdbId) ?? ""
            self.plexGuid = try container.decodeIfPresent(String.self, forKey: .plexGuid) ?? ""
            self.photoUrl = try container.decodeIfPresent(String.self, forKey: .photoUrl) ?? ""
            self.photoThumbhash = try container.decodeIfPresent(String.self, forKey: .photoThumbhash) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.character, forKey: .character)
            try container.encode(self.order, forKey: .order)
            try container.encode(self.personId, forKey: .personId)
            try container.encode(self.tmdbId, forKey: .tmdbId)
            try container.encode(self.tvdbId, forKey: .tvdbId)
            try container.encode(self.imdbId, forKey: .imdbId)
            try container.encode(self.plexGuid, forKey: .plexGuid)
            try container.encode(self.photoUrl, forKey: .photoUrl)
            try container.encode(self.photoThumbhash, forKey: .photoThumbhash)
        }
    }

    /// Wire type `internal/catalog.CrewCredit`. Direction: response. Dialect: bloem.
    public struct CrewCredit: Codable, Hashable, Sendable {
        public let name: String
        public let job: String
        public let personId: String
        public let tmdbId: String
        public let tvdbId: String
        public let imdbId: String
        public let plexGuid: String
        public let photoUrl: String
        public let photoThumbhash: String

        public enum CodingKeys: String, CodingKey {
            case name = "name"
            case job = "job"
            case personId = "person_id"
            case tmdbId = "tmdb_id"
            case tvdbId = "tvdb_id"
            case imdbId = "imdb_id"
            case plexGuid = "plex_guid"
            case photoUrl = "photo_url"
            case photoThumbhash = "photo_thumbhash"
        }

        public init(
            name: String = "",
            job: String = "",
            personId: String = "",
            tmdbId: String = "",
            tvdbId: String = "",
            imdbId: String = "",
            plexGuid: String = "",
            photoUrl: String = "",
            photoThumbhash: String = ""
        ) {
            self.name = name
            self.job = job
            self.personId = personId
            self.tmdbId = tmdbId
            self.tvdbId = tvdbId
            self.imdbId = imdbId
            self.plexGuid = plexGuid
            self.photoUrl = photoUrl
            self.photoThumbhash = photoThumbhash
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self.job = try container.decodeIfPresent(String.self, forKey: .job) ?? ""
            self.personId = try container.decodeIfPresent(String.self, forKey: .personId) ?? ""
            self.tmdbId = try container.decodeIfPresent(String.self, forKey: .tmdbId) ?? ""
            self.tvdbId = try container.decodeIfPresent(String.self, forKey: .tvdbId) ?? ""
            self.imdbId = try container.decodeIfPresent(String.self, forKey: .imdbId) ?? ""
            self.plexGuid = try container.decodeIfPresent(String.self, forKey: .plexGuid) ?? ""
            self.photoUrl = try container.decodeIfPresent(String.self, forKey: .photoUrl) ?? ""
            self.photoThumbhash = try container.decodeIfPresent(String.self, forKey: .photoThumbhash) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.job, forKey: .job)
            try container.encode(self.personId, forKey: .personId)
            try container.encode(self.tmdbId, forKey: .tmdbId)
            try container.encode(self.tvdbId, forKey: .tvdbId)
            try container.encode(self.imdbId, forKey: .imdbId)
            try container.encode(self.plexGuid, forKey: .plexGuid)
            try container.encode(self.photoUrl, forKey: .photoUrl)
            try container.encode(self.photoThumbhash, forKey: .photoThumbhash)
        }
    }

    /// Wire type `internal/catalog.EbookDetailExtension`. Direction: response. Dialect: bloem.
    public struct EbookDetailExtension: Codable, Hashable, Sendable {
        public let authors: [AudiobookPerson]
        public let publisher: String
        public let series: AudiobookSeriesGroup?
        public let related: AudiobookRelatedContent

        public enum CodingKeys: String, CodingKey {
            case authors = "authors"
            case publisher = "publisher"
            case series = "series"
            case related = "related"
        }

        public init(
            authors: [AudiobookPerson] = [],
            publisher: String = "",
            series: AudiobookSeriesGroup? = nil,
            related: AudiobookRelatedContent = AudiobookRelatedContent()
        ) {
            self.authors = authors
            self.publisher = publisher
            self.series = series
            self.related = related
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.authors = try container.decodeIfPresent([AudiobookPerson].self, forKey: .authors) ?? []
            self.publisher = try container.decodeIfPresent(String.self, forKey: .publisher) ?? ""
            self.series = try container.decodeIfPresent(AudiobookSeriesGroup.self, forKey: .series)
            self.related = try container.decodeIfPresent(AudiobookRelatedContent.self, forKey: .related) ?? AudiobookRelatedContent()
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.authors, forKey: .authors)
            try container.encode(self.publisher, forKey: .publisher)
            try container.encodeIfPresent(self.series, forKey: .series)
            try container.encode(self.related, forKey: .related)
        }
    }

    /// Wire type `internal/catalog.ItemExtraInfo`. Direction: response. Dialect: bloem.
    public struct ItemExtraInfo: Codable, Hashable, Sendable {
        public let contentId: String
        public let kind: String
        public let title: String
        public let durationSeconds: Int
        public let fileId: Int

        public enum CodingKeys: String, CodingKey {
            case contentId = "content_id"
            case kind = "kind"
            case title = "title"
            case durationSeconds = "duration_seconds"
            case fileId = "file_id"
        }

        public init(
            contentId: String = "",
            kind: String = "",
            title: String = "",
            durationSeconds: Int = 0,
            fileId: Int = 0
        ) {
            self.contentId = contentId
            self.kind = kind
            self.title = title
            self.durationSeconds = durationSeconds
            self.fileId = fileId
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.contentId = try container.decodeIfPresent(String.self, forKey: .contentId) ?? ""
            self.kind = try container.decodeIfPresent(String.self, forKey: .kind) ?? ""
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.durationSeconds = try container.decodeIfPresent(Int.self, forKey: .durationSeconds) ?? 0
            self.fileId = try container.decodeIfPresent(Int.self, forKey: .fileId) ?? 0
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.contentId, forKey: .contentId)
            try container.encode(self.kind, forKey: .kind)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.durationSeconds, forKey: .durationSeconds)
            try container.encode(self.fileId, forKey: .fileId)
        }
    }

    /// Wire type `internal/catalog.ItemVideoInfo`. Direction: response. Dialect: bloem.
    public struct ItemVideoInfo: Codable, Hashable, Sendable {
        public let kind: String
        public let site: String
        public let siteKey: String
        public let name: String
        public let language: String
        public let isOfficial: Bool

        public enum CodingKeys: String, CodingKey {
            case kind = "kind"
            case site = "site"
            case siteKey = "site_key"
            case name = "name"
            case language = "language"
            case isOfficial = "is_official"
        }

        public init(
            kind: String = "",
            site: String = "",
            siteKey: String = "",
            name: String = "",
            language: String = "",
            isOfficial: Bool = false
        ) {
            self.kind = kind
            self.site = site
            self.siteKey = siteKey
            self.name = name
            self.language = language
            self.isOfficial = isOfficial
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.kind = try container.decodeIfPresent(String.self, forKey: .kind) ?? ""
            self.site = try container.decodeIfPresent(String.self, forKey: .site) ?? ""
            self.siteKey = try container.decodeIfPresent(String.self, forKey: .siteKey) ?? ""
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self.language = try container.decodeIfPresent(String.self, forKey: .language) ?? ""
            self.isOfficial = try container.decodeIfPresent(Bool.self, forKey: .isOfficial) ?? false
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.kind, forKey: .kind)
            try container.encode(self.site, forKey: .site)
            try container.encode(self.siteKey, forKey: .siteKey)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.language, forKey: .language)
            try container.encode(self.isOfficial, forKey: .isOfficial)
        }
    }

    /// Wire type `internal/catalog.MangaChapter`. Direction: response. Dialect: bloem.
    public struct MangaChapter: Codable, Hashable, Sendable {
        public let contentId: String
        public let title: String
        public let chapterIndex: Double?
        public let volume: String
        public let read: Bool
        public let progress: Double?
        public let posterUrl: String

        public enum CodingKeys: String, CodingKey {
            case contentId = "content_id"
            case title = "title"
            case chapterIndex = "chapter_index"
            case volume = "volume"
            case read = "read"
            case progress = "progress"
            case posterUrl = "poster_url"
        }

        public init(
            contentId: String = "",
            title: String = "",
            chapterIndex: Double? = nil,
            volume: String = "",
            read: Bool = false,
            progress: Double? = nil,
            posterUrl: String = ""
        ) {
            self.contentId = contentId
            self.title = title
            self.chapterIndex = chapterIndex
            self.volume = volume
            self.read = read
            self.progress = progress
            self.posterUrl = posterUrl
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.contentId = try container.decodeIfPresent(String.self, forKey: .contentId) ?? ""
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.chapterIndex = try container.decodeIfPresent(Double.self, forKey: .chapterIndex)
            self.volume = try container.decodeIfPresent(String.self, forKey: .volume) ?? ""
            self.read = try container.decodeIfPresent(Bool.self, forKey: .read) ?? false
            self.progress = try container.decodeIfPresent(Double.self, forKey: .progress)
            self.posterUrl = try container.decodeIfPresent(String.self, forKey: .posterUrl) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.contentId, forKey: .contentId)
            try container.encode(self.title, forKey: .title)
            try container.encodeIfPresent(self.chapterIndex, forKey: .chapterIndex)
            try container.encode(self.volume, forKey: .volume)
            try container.encode(self.read, forKey: .read)
            try container.encodeIfPresent(self.progress, forKey: .progress)
            try container.encode(self.posterUrl, forKey: .posterUrl)
        }
    }

    /// Wire type `internal/catalog.MangaDetailExtension`. Direction: response. Dialect: bloem.
    public struct MangaDetailExtension: Codable, Hashable, Sendable {
        public let chapters: [MangaChapter]

        public enum CodingKeys: String, CodingKey {
            case chapters = "chapters"
        }

        public init(
            chapters: [MangaChapter] = []
        ) {
            self.chapters = chapters
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.chapters = try container.decodeIfPresent([MangaChapter].self, forKey: .chapters) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.chapters, forKey: .chapters)
        }
    }

    /// Wire type `internal/catalog.Marker`. Direction: response. Dialect: bloem.
    public struct Marker: Codable, Hashable, Sendable {
        public let start: Double
        public let end: Double

        public enum CodingKeys: String, CodingKey {
            case start = "start"
            case end = "end"
        }

        public init(
            start: Double = 0.0,
            end: Double = 0.0
        ) {
            self.start = start
            self.end = end
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.start = try container.decodeIfPresent(Double.self, forKey: .start) ?? 0.0
            self.end = try container.decodeIfPresent(Double.self, forKey: .end) ?? 0.0
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.start, forKey: .start)
            try container.encode(self.end, forKey: .end)
        }
    }

    /// Wire type `internal/catalog.SeasonUserData`. Direction: response. Dialect: upstream-compat.
    public struct SeasonUserData: Codable, Hashable, Sendable {
        public let positionSeconds: Double
        public let durationSeconds: Double
        public let isInProgress: Bool
        public let watchedCount: Int
        public let unplayedCount: Int
        public let inProgressCount: Int
        public let played: Bool
        public let lastFileId: Int?
        public let lastResolution: String?
        public let lastHdr: Bool?
        public let lastCodecVideo: String?
        public let lastEditionKey: String?

        public enum CodingKeys: String, CodingKey {
            case positionSeconds = "position_seconds"
            case durationSeconds = "duration_seconds"
            case isInProgress = "is_in_progress"
            case watchedCount = "watched_count"
            case unplayedCount = "unplayed_count"
            case inProgressCount = "in_progress_count"
            case played = "played"
            case lastFileId = "last_file_id"
            case lastResolution = "last_resolution"
            case lastHdr = "last_hdr"
            case lastCodecVideo = "last_codec_video"
            case lastEditionKey = "last_edition_key"
        }

        public init(
            positionSeconds: Double = 0.0,
            durationSeconds: Double = 0.0,
            isInProgress: Bool = false,
            watchedCount: Int = 0,
            unplayedCount: Int = 0,
            inProgressCount: Int = 0,
            played: Bool = false,
            lastFileId: Int? = nil,
            lastResolution: String? = nil,
            lastHdr: Bool? = nil,
            lastCodecVideo: String? = nil,
            lastEditionKey: String? = nil
        ) {
            self.positionSeconds = positionSeconds
            self.durationSeconds = durationSeconds
            self.isInProgress = isInProgress
            self.watchedCount = watchedCount
            self.unplayedCount = unplayedCount
            self.inProgressCount = inProgressCount
            self.played = played
            self.lastFileId = lastFileId
            self.lastResolution = lastResolution
            self.lastHdr = lastHdr
            self.lastCodecVideo = lastCodecVideo
            self.lastEditionKey = lastEditionKey
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.positionSeconds = try container.decodeIfPresent(Double.self, forKey: .positionSeconds) ?? 0.0
            self.durationSeconds = try container.decodeIfPresent(Double.self, forKey: .durationSeconds) ?? 0.0
            self.isInProgress = try container.decodeIfPresent(Bool.self, forKey: .isInProgress) ?? false
            self.watchedCount = try container.decodeIfPresent(Int.self, forKey: .watchedCount) ?? 0
            self.unplayedCount = try container.decodeIfPresent(Int.self, forKey: .unplayedCount) ?? 0
            self.inProgressCount = try container.decodeIfPresent(Int.self, forKey: .inProgressCount) ?? 0
            self.played = try container.decodeIfPresent(Bool.self, forKey: .played) ?? false
            self.lastFileId = try container.decodeIfPresent(Int.self, forKey: .lastFileId)
            self.lastResolution = try container.decodeIfPresent(String.self, forKey: .lastResolution)
            self.lastHdr = try container.decodeIfPresent(Bool.self, forKey: .lastHdr)
            self.lastCodecVideo = try container.decodeIfPresent(String.self, forKey: .lastCodecVideo)
            self.lastEditionKey = try container.decodeIfPresent(String.self, forKey: .lastEditionKey)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.positionSeconds, forKey: .positionSeconds)
            try container.encode(self.durationSeconds, forKey: .durationSeconds)
            try container.encode(self.isInProgress, forKey: .isInProgress)
            try container.encode(self.watchedCount, forKey: .watchedCount)
            try container.encode(self.unplayedCount, forKey: .unplayedCount)
            try container.encode(self.inProgressCount, forKey: .inProgressCount)
            try container.encode(self.played, forKey: .played)
            try container.encodeIfPresent(self.lastFileId, forKey: .lastFileId)
            try container.encodeIfPresent(self.lastResolution, forKey: .lastResolution)
            try container.encodeIfPresent(self.lastHdr, forKey: .lastHdr)
            try container.encodeIfPresent(self.lastCodecVideo, forKey: .lastCodecVideo)
            try container.encodeIfPresent(self.lastEditionKey, forKey: .lastEditionKey)
        }
    }

    /// Wire type `internal/catalog.SubtitleInfo`. Direction: response. Dialect: bloem.
    public struct SubtitleInfo: Codable, Hashable, Sendable {
        public let source: String
        public let language: String
        public let codec: String
        public let forced: Bool
        public let hearingImpaired: Bool
        public let title: String

        public enum CodingKeys: String, CodingKey {
            case source = "source"
            case language = "language"
            case codec = "codec"
            case forced = "forced"
            case hearingImpaired = "hearing_impaired"
            case title = "title"
        }

        public init(
            source: String = "",
            language: String = "",
            codec: String = "",
            forced: Bool = false,
            hearingImpaired: Bool = false,
            title: String = ""
        ) {
            self.source = source
            self.language = language
            self.codec = codec
            self.forced = forced
            self.hearingImpaired = hearingImpaired
            self.title = title
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.source = try container.decodeIfPresent(String.self, forKey: .source) ?? ""
            self.language = try container.decodeIfPresent(String.self, forKey: .language) ?? ""
            self.codec = try container.decodeIfPresent(String.self, forKey: .codec) ?? ""
            self.forced = try container.decodeIfPresent(Bool.self, forKey: .forced) ?? false
            self.hearingImpaired = try container.decodeIfPresent(Bool.self, forKey: .hearingImpaired) ?? false
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.source, forKey: .source)
            try container.encode(self.language, forKey: .language)
            try container.encode(self.codec, forKey: .codec)
            try container.encode(self.forced, forKey: .forced)
            try container.encode(self.hearingImpaired, forKey: .hearingImpaired)
            try container.encode(self.title, forKey: .title)
        }
    }

    /// Wire type `internal/catalog.VersionChapter`. Direction: response. Dialect: bloem.
    public struct VersionChapter: Codable, Hashable, Sendable {
        public let index: Int
        public let title: String
        public let startSeconds: Double
        public let endSeconds: Double
        public let source: String
        public let thumbnailUrl: String
        public let thumbnailThumbhash: String

        public enum CodingKeys: String, CodingKey {
            case index = "index"
            case title = "title"
            case startSeconds = "start_seconds"
            case endSeconds = "end_seconds"
            case source = "source"
            case thumbnailUrl = "thumbnail_url"
            case thumbnailThumbhash = "thumbnail_thumbhash"
        }

        public init(
            index: Int = 0,
            title: String = "",
            startSeconds: Double = 0.0,
            endSeconds: Double = 0.0,
            source: String = "",
            thumbnailUrl: String = "",
            thumbnailThumbhash: String = ""
        ) {
            self.index = index
            self.title = title
            self.startSeconds = startSeconds
            self.endSeconds = endSeconds
            self.source = source
            self.thumbnailUrl = thumbnailUrl
            self.thumbnailThumbhash = thumbnailThumbhash
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.index = try container.decodeIfPresent(Int.self, forKey: .index) ?? 0
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.startSeconds = try container.decodeIfPresent(Double.self, forKey: .startSeconds) ?? 0.0
            self.endSeconds = try container.decodeIfPresent(Double.self, forKey: .endSeconds) ?? 0.0
            self.source = try container.decodeIfPresent(String.self, forKey: .source) ?? ""
            self.thumbnailUrl = try container.decodeIfPresent(String.self, forKey: .thumbnailUrl) ?? ""
            self.thumbnailThumbhash = try container.decodeIfPresent(String.self, forKey: .thumbnailThumbhash) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.index, forKey: .index)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.startSeconds, forKey: .startSeconds)
            try container.encode(self.endSeconds, forKey: .endSeconds)
            try container.encode(self.source, forKey: .source)
            try container.encode(self.thumbnailUrl, forKey: .thumbnailUrl)
            try container.encode(self.thumbnailThumbhash, forKey: .thumbnailThumbhash)
        }
    }

    /// Wire type `internal/catalog.VersionSubtitleTrack`. Direction: response. Dialect: bloem.
    public struct VersionSubtitleTrack: Codable, Hashable, Sendable {
        public let index: Int
        public let language: String
        public let codec: String
        public let title: String
        public let embeddedTitle: String
        public let resolution: String
        public let forced: Bool
        public let defaultValue: Bool
        public let hearingImpaired: Bool
        public let external: Bool
        public let fileName: String

        public enum CodingKeys: String, CodingKey {
            case index = "index"
            case language = "language"
            case codec = "codec"
            case title = "title"
            case embeddedTitle = "embedded_title"
            case resolution = "resolution"
            case forced = "forced"
            case defaultValue = "default"
            case hearingImpaired = "hearing_impaired"
            case external = "external"
            case fileName = "file_name"
        }

        public init(
            index: Int = 0,
            language: String = "",
            codec: String = "",
            title: String = "",
            embeddedTitle: String = "",
            resolution: String = "",
            forced: Bool = false,
            defaultValue: Bool = false,
            hearingImpaired: Bool = false,
            external: Bool = false,
            fileName: String = ""
        ) {
            self.index = index
            self.language = language
            self.codec = codec
            self.title = title
            self.embeddedTitle = embeddedTitle
            self.resolution = resolution
            self.forced = forced
            self.defaultValue = defaultValue
            self.hearingImpaired = hearingImpaired
            self.external = external
            self.fileName = fileName
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.index = try container.decodeIfPresent(Int.self, forKey: .index) ?? 0
            self.language = try container.decodeIfPresent(String.self, forKey: .language) ?? ""
            self.codec = try container.decodeIfPresent(String.self, forKey: .codec) ?? ""
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.embeddedTitle = try container.decodeIfPresent(String.self, forKey: .embeddedTitle) ?? ""
            self.resolution = try container.decodeIfPresent(String.self, forKey: .resolution) ?? ""
            self.forced = try container.decodeIfPresent(Bool.self, forKey: .forced) ?? false
            self.defaultValue = try container.decodeIfPresent(Bool.self, forKey: .defaultValue) ?? false
            self.hearingImpaired = try container.decodeIfPresent(Bool.self, forKey: .hearingImpaired) ?? false
            self.external = try container.decodeIfPresent(Bool.self, forKey: .external) ?? false
            self.fileName = try container.decodeIfPresent(String.self, forKey: .fileName) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.index, forKey: .index)
            try container.encode(self.language, forKey: .language)
            try container.encode(self.codec, forKey: .codec)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.embeddedTitle, forKey: .embeddedTitle)
            try container.encode(self.resolution, forKey: .resolution)
            try container.encode(self.forced, forKey: .forced)
            try container.encode(self.defaultValue, forKey: .defaultValue)
            try container.encode(self.hearingImpaired, forKey: .hearingImpaired)
            try container.encode(self.external, forKey: .external)
            try container.encode(self.fileName, forKey: .fileName)
        }
    }

    /// Wire type `internal/catalog.WorkFormatSummary`. Direction: response. Dialect: upstream-compat.
    public struct WorkFormatSummary: Codable, Hashable, Sendable {
        public let type: String
        public let contentId: String
        public let libraryId: Int

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case contentId = "content_id"
            case libraryId = "library_id"
        }

        public init(
            type: String = "",
            contentId: String = "",
            libraryId: Int = 0
        ) {
            self.type = type
            self.contentId = contentId
            self.libraryId = libraryId
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decodeIfPresent(String.self, forKey: .type) ?? ""
            self.contentId = try container.decodeIfPresent(String.self, forKey: .contentId) ?? ""
            self.libraryId = try container.decodeIfPresent(Int.self, forKey: .libraryId) ?? 0
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.contentId, forKey: .contentId)
            try container.encode(self.libraryId, forKey: .libraryId)
        }
    }
}
