// Code generated by cmd/clientdtogen from bloem-server internal/events. DO NOT EDIT.
// Server revision: 55d3ae52f51751e4da769252179e9988a005782e (generator v1, registry contracts/client/v1/registry.json)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `internal/events`.
public enum Events {
    /// Wire type `internal/events.EventChannel`. Direction: both. Dialect: bloem.
    public struct EventChannel: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let CATALOG: EventChannel = EventChannel(rawValue: "catalog")
        public static let JOBS: EventChannel = EventChannel(rawValue: "jobs")
        public static let SESSIONS: EventChannel = EventChannel(rawValue: "sessions")
        public static let TASKS: EventChannel = EventChannel(rawValue: "tasks")
        public static let SCANS: EventChannel = EventChannel(rawValue: "scans")
        public static let HISTORY_IMPORT: EventChannel = EventChannel(rawValue: "history_import")
        public static let USER_STATE: EventChannel = EventChannel(rawValue: "user_state")
        public static let USER_SETTINGS: EventChannel = EventChannel(rawValue: "user_settings")
        public static let SETTINGS: EventChannel = EventChannel(rawValue: "settings")
        public static let PLUGINS: EventChannel = EventChannel(rawValue: "plugins")
        public static let NOTIFICATIONS: EventChannel = EventChannel(rawValue: "notifications")
        public static let KNOWN: [EventChannel] = [CATALOG, JOBS, SESSIONS, TASKS, SCANS, HISTORY_IMPORT, USER_STATE, USER_SETTINGS, SETTINGS, PLUGINS, NOTIFICATIONS]
    }

    /// Wire type `internal/events.EventsErrorMessage`. Direction: response. Dialect: bloem. Registered root.
    public struct EventsErrorMessage: Codable, Hashable, Sendable {
        public let type: String
        public let code: String
        public let message: String

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case code = "code"
            case message = "message"
        }

        public init(
            type: String = "",
            code: String = "",
            message: String = ""
        ) {
            self.type = type
            self.code = code
            self.message = message
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decodeIfPresent(String.self, forKey: .type) ?? ""
            self.code = try container.decodeIfPresent(String.self, forKey: .code) ?? ""
            self.message = try container.decodeIfPresent(String.self, forKey: .message) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.code, forKey: .code)
            try container.encode(self.message, forKey: .message)
        }
    }

    /// Wire type `internal/events.EventsEventMessage`. Direction: response. Dialect: bloem. Registered root.
    public struct EventsEventMessage: Codable, Hashable, Sendable {
        public let type: String
        public let channel: EventChannel
        public let event: String
        public let eventId: String
        public let timestamp: String
        public let data: BloemJSONValue?

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case channel = "channel"
            case event = "event"
            case eventId = "event_id"
            case timestamp = "timestamp"
            case data = "data"
        }

        public init(
            type: String = "",
            channel: EventChannel = EventChannel(rawValue: ""),
            event: String = "",
            eventId: String = "",
            timestamp: String = "",
            data: BloemJSONValue? = nil
        ) {
            self.type = type
            self.channel = channel
            self.event = event
            self.eventId = eventId
            self.timestamp = timestamp
            self.data = data
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decodeIfPresent(String.self, forKey: .type) ?? ""
            self.channel = try container.decodeIfPresent(EventChannel.self, forKey: .channel) ?? EventChannel(rawValue: "")
            self.event = try container.decodeIfPresent(String.self, forKey: .event) ?? ""
            self.eventId = try container.decodeIfPresent(String.self, forKey: .eventId) ?? ""
            self.timestamp = try container.decodeIfPresent(String.self, forKey: .timestamp) ?? ""
            self.data = try container.decodeIfPresent(BloemJSONValue.self, forKey: .data)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.channel, forKey: .channel)
            try container.encode(self.event, forKey: .event)
            try container.encode(self.eventId, forKey: .eventId)
            try container.encode(self.timestamp, forKey: .timestamp)
            try container.encodeIfPresent(self.data, forKey: .data)
        }
    }

    /// Wire type `internal/events.EventsHelloMessage`. Direction: response. Dialect: bloem. Registered root.
    public struct EventsHelloMessage: Codable, Hashable, Sendable {
        public let type: String
        public let schemaVersion: Int
        public let connectionId: String
        public let availableChannels: [EventChannel]
        public let requiredAction: String

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case schemaVersion = "schema_version"
            case connectionId = "connection_id"
            case availableChannels = "available_channels"
            case requiredAction = "required_action"
        }

        public init(
            type: String = "",
            schemaVersion: Int = 0,
            connectionId: String = "",
            availableChannels: [EventChannel] = [],
            requiredAction: String = ""
        ) {
            self.type = type
            self.schemaVersion = schemaVersion
            self.connectionId = connectionId
            self.availableChannels = availableChannels
            self.requiredAction = requiredAction
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decodeIfPresent(String.self, forKey: .type) ?? ""
            self.schemaVersion = try container.decodeIfPresent(Int.self, forKey: .schemaVersion) ?? 0
            self.connectionId = try container.decodeIfPresent(String.self, forKey: .connectionId) ?? ""
            self.availableChannels = try container.decodeIfPresent([EventChannel].self, forKey: .availableChannels) ?? []
            self.requiredAction = try container.decodeIfPresent(String.self, forKey: .requiredAction) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.schemaVersion, forKey: .schemaVersion)
            try container.encode(self.connectionId, forKey: .connectionId)
            try container.encode(self.availableChannels, forKey: .availableChannels)
            try container.encode(self.requiredAction, forKey: .requiredAction)
        }
    }

    /// Wire type `internal/events.EventsRejectedChannel`. Direction: response. Dialect: bloem.
    public struct EventsRejectedChannel: Codable, Hashable, Sendable {
        public let channel: EventChannel
        public let code: String
        public let message: String

        public enum CodingKeys: String, CodingKey {
            case channel = "channel"
            case code = "code"
            case message = "message"
        }

        public init(
            channel: EventChannel = EventChannel(rawValue: ""),
            code: String = "",
            message: String = ""
        ) {
            self.channel = channel
            self.code = code
            self.message = message
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.channel = try container.decodeIfPresent(EventChannel.self, forKey: .channel) ?? EventChannel(rawValue: "")
            self.code = try container.decodeIfPresent(String.self, forKey: .code) ?? ""
            self.message = try container.decodeIfPresent(String.self, forKey: .message) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.channel, forKey: .channel)
            try container.encode(self.code, forKey: .code)
            try container.encode(self.message, forKey: .message)
        }
    }

    /// Wire type `internal/events.EventsSnapshotMessage`. Direction: response. Dialect: bloem. Registered root.
    public struct EventsSnapshotMessage: Codable, Hashable, Sendable {
        public let type: String
        public let channel: EventChannel
        public let timestamp: String
        public let data: BloemJSONValue?

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case channel = "channel"
            case timestamp = "timestamp"
            case data = "data"
        }

        public init(
            type: String = "",
            channel: EventChannel = EventChannel(rawValue: ""),
            timestamp: String = "",
            data: BloemJSONValue? = nil
        ) {
            self.type = type
            self.channel = channel
            self.timestamp = timestamp
            self.data = data
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decodeIfPresent(String.self, forKey: .type) ?? ""
            self.channel = try container.decodeIfPresent(EventChannel.self, forKey: .channel) ?? EventChannel(rawValue: "")
            self.timestamp = try container.decodeIfPresent(String.self, forKey: .timestamp) ?? ""
            self.data = try container.decodeIfPresent(BloemJSONValue.self, forKey: .data)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.channel, forKey: .channel)
            try container.encode(self.timestamp, forKey: .timestamp)
            try container.encodeIfPresent(self.data, forKey: .data)
        }
    }

    /// Wire type `internal/events.EventsSubscribeMessage`. Direction: request. Dialect: bloem. Registered root.
    public struct EventsSubscribeMessage: Codable, Hashable, Sendable {
        public let type: String
        public let requestId: String
        public let channels: [EventChannel]

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case requestId = "request_id"
            case channels = "channels"
        }

        public init(
            type: String,
            requestId: String = "",
            channels: [EventChannel]
        ) {
            self.type = type
            self.requestId = requestId
            self.channels = channels
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decode(String.self, forKey: .type)
            self.requestId = try container.decodeIfPresent(String.self, forKey: .requestId) ?? ""
            self.channels = try container.decode([EventChannel].self, forKey: .channels)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.requestId, forKey: .requestId)
            try container.encode(self.channels, forKey: .channels)
        }
    }

    /// Wire type `internal/events.EventsSubscribedMessage`. Direction: response. Dialect: bloem. Registered root.
    public struct EventsSubscribedMessage: Codable, Hashable, Sendable {
        public let type: String
        public let requestId: String
        public let channels: [EventChannel]
        public let rejected: [EventsRejectedChannel]

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case requestId = "request_id"
            case channels = "channels"
            case rejected = "rejected"
        }

        public init(
            type: String = "",
            requestId: String = "",
            channels: [EventChannel] = [],
            rejected: [EventsRejectedChannel] = []
        ) {
            self.type = type
            self.requestId = requestId
            self.channels = channels
            self.rejected = rejected
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decodeIfPresent(String.self, forKey: .type) ?? ""
            self.requestId = try container.decodeIfPresent(String.self, forKey: .requestId) ?? ""
            self.channels = try container.decodeIfPresent([EventChannel].self, forKey: .channels) ?? []
            self.rejected = try container.decodeIfPresent([EventsRejectedChannel].self, forKey: .rejected) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.requestId, forKey: .requestId)
            try container.encode(self.channels, forKey: .channels)
            try container.encode(self.rejected, forKey: .rejected)
        }
    }
}
