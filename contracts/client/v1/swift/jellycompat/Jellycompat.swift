// Code generated by cmd/clientdtogen from bloem-server internal/jellycompat. DO NOT EDIT.
// Server revision: 9ce66fd6fa9dfeeebe9a80491f58a7c77d205895 (generator v1, registry contracts/client/v1/registry.json)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `internal/jellycompat`.
public enum Jellycompat {
    /// Wire type `internal/jellycompat.ConnectInfo`. Direction: response. Dialect: upstream-compat.
    public struct ConnectInfo: Codable, Hashable, Sendable {
        public let enabled: Bool
        public let pendingRestart: Bool
        public let publicUrl: String
        public let serverName: String

        public enum CodingKeys: String, CodingKey {
            case enabled = "enabled"
            case pendingRestart = "pending_restart"
            case publicUrl = "public_url"
            case serverName = "server_name"
        }

        public init(
            enabled: Bool = false,
            pendingRestart: Bool = false,
            publicUrl: String = "",
            serverName: String = ""
        ) {
            self.enabled = enabled
            self.pendingRestart = pendingRestart
            self.publicUrl = publicUrl
            self.serverName = serverName
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.enabled = try container.decodeIfPresent(Bool.self, forKey: .enabled) ?? false
            self.pendingRestart = try container.decodeIfPresent(Bool.self, forKey: .pendingRestart) ?? false
            self.publicUrl = try container.decodeIfPresent(String.self, forKey: .publicUrl) ?? ""
            self.serverName = try container.decodeIfPresent(String.self, forKey: .serverName) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.enabled, forKey: .enabled)
            try container.encode(self.pendingRestart, forKey: .pendingRestart)
            try container.encode(self.publicUrl, forKey: .publicUrl)
            try container.encode(self.serverName, forKey: .serverName)
        }
    }
}
