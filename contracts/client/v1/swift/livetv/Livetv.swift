// Code generated by cmd/clientdtogen from bloem-server internal/livetv. DO NOT EDIT.
// Server revision: 8c612bd9 (generator v1, registry contracts/client/v1/registry.json)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `internal/livetv`.
public enum Livetv {
    /// Wire type `internal/livetv.CapabilityResponse`. Direction: response. Dialect: bloem. Registered root.
    public struct CapabilityResponse: Codable, Hashable, Sendable {
        public let supported: Bool
        public let allowed: Bool
        public let available: Bool
        public let heartbeatIntervalSeconds: Int

        public enum CodingKeys: String, CodingKey {
            case supported = "supported"
            case allowed = "allowed"
            case available = "available"
            case heartbeatIntervalSeconds = "heartbeat_interval_seconds"
        }

        public init(
            supported: Bool = false,
            allowed: Bool = false,
            available: Bool = false,
            heartbeatIntervalSeconds: Int = 0
        ) {
            self.supported = supported
            self.allowed = allowed
            self.available = available
            self.heartbeatIntervalSeconds = heartbeatIntervalSeconds
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.supported = try container.decodeIfPresent(Bool.self, forKey: .supported) ?? false
            self.allowed = try container.decodeIfPresent(Bool.self, forKey: .allowed) ?? false
            self.available = try container.decodeIfPresent(Bool.self, forKey: .available) ?? false
            self.heartbeatIntervalSeconds = try container.decodeIfPresent(Int.self, forKey: .heartbeatIntervalSeconds) ?? 0
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.supported, forKey: .supported)
            try container.encode(self.allowed, forKey: .allowed)
            try container.encode(self.available, forKey: .available)
            try container.encode(self.heartbeatIntervalSeconds, forKey: .heartbeatIntervalSeconds)
        }
    }

    /// Wire type `internal/livetv.ChannelResponse`. Direction: response. Dialect: upstream-compat.
    public struct ChannelResponse: Codable, Hashable, Sendable {
        public let streamUrl: String
        public let id: String
        public let tunerId: String
        public let number: String
        public let numberOverride: String?
        public let callsign: String
        public let name: String
        public let logoUrl: String
        public let hd: Bool
        public let enabled: Bool
        public let guideStationId: String

        public enum CodingKeys: String, CodingKey {
            case streamUrl = "stream_url"
            case id = "id"
            case tunerId = "tuner_id"
            case number = "number"
            case numberOverride = "number_override"
            case callsign = "callsign"
            case name = "name"
            case logoUrl = "logo_url"
            case hd = "hd"
            case enabled = "enabled"
            case guideStationId = "guide_station_id"
        }

        public init(
            streamUrl: String = "",
            id: String = "",
            tunerId: String = "",
            number: String = "",
            numberOverride: String? = nil,
            callsign: String = "",
            name: String = "",
            logoUrl: String = "",
            hd: Bool = false,
            enabled: Bool = false,
            guideStationId: String = ""
        ) {
            self.streamUrl = streamUrl
            self.id = id
            self.tunerId = tunerId
            self.number = number
            self.numberOverride = numberOverride
            self.callsign = callsign
            self.name = name
            self.logoUrl = logoUrl
            self.hd = hd
            self.enabled = enabled
            self.guideStationId = guideStationId
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.streamUrl = try container.decodeIfPresent(String.self, forKey: .streamUrl) ?? ""
            self.id = try container.decodeIfPresent(String.self, forKey: .id) ?? ""
            self.tunerId = try container.decodeIfPresent(String.self, forKey: .tunerId) ?? ""
            self.number = try container.decodeIfPresent(String.self, forKey: .number) ?? ""
            self.numberOverride = try container.decodeIfPresent(String.self, forKey: .numberOverride)
            self.callsign = try container.decodeIfPresent(String.self, forKey: .callsign) ?? ""
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self.logoUrl = try container.decodeIfPresent(String.self, forKey: .logoUrl) ?? ""
            self.hd = try container.decodeIfPresent(Bool.self, forKey: .hd) ?? false
            self.enabled = try container.decodeIfPresent(Bool.self, forKey: .enabled) ?? false
            self.guideStationId = try container.decodeIfPresent(String.self, forKey: .guideStationId) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.streamUrl, forKey: .streamUrl)
            try container.encode(self.id, forKey: .id)
            try container.encode(self.tunerId, forKey: .tunerId)
            try container.encode(self.number, forKey: .number)
            try container.encodeIfPresent(self.numberOverride, forKey: .numberOverride)
            try container.encode(self.callsign, forKey: .callsign)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.logoUrl, forKey: .logoUrl)
            try container.encode(self.hd, forKey: .hd)
            try container.encode(self.enabled, forKey: .enabled)
            try container.encode(self.guideStationId, forKey: .guideStationId)
        }
    }

    /// Wire type `internal/livetv.ChannelsResponse`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct ChannelsResponse: Codable, Hashable, Sendable {
        public let channels: [ChannelResponse]

        public enum CodingKeys: String, CodingKey {
            case channels = "channels"
        }

        public init(
            channels: [ChannelResponse] = []
        ) {
            self.channels = channels
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.channels = try container.decodeIfPresent([ChannelResponse].self, forKey: .channels) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.channels, forKey: .channels)
        }
    }

    /// Wire type `internal/livetv.ClientCapabilities`. Direction: request. Dialect: upstream-compat. Registered root.
    public struct ClientCapabilities: Codable, Hashable, Sendable {
        public let codecsVideo: [String]
        public let codecsAudio: [String]
        public let audioPassthroughCodecs: [String]
        public let maxAudioChannels: Int
        public let maxResolution: String

        public enum CodingKeys: String, CodingKey {
            case codecsVideo = "codecs_video"
            case codecsAudio = "codecs_audio"
            case audioPassthroughCodecs = "audio_passthrough_codecs"
            case maxAudioChannels = "max_audio_channels"
            case maxResolution = "max_resolution"
        }

        public init(
            codecsVideo: [String],
            codecsAudio: [String],
            audioPassthroughCodecs: [String],
            maxAudioChannels: Int,
            maxResolution: String
        ) {
            self.codecsVideo = codecsVideo
            self.codecsAudio = codecsAudio
            self.audioPassthroughCodecs = audioPassthroughCodecs
            self.maxAudioChannels = maxAudioChannels
            self.maxResolution = maxResolution
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.codecsVideo = try container.decode([String].self, forKey: .codecsVideo)
            self.codecsAudio = try container.decode([String].self, forKey: .codecsAudio)
            self.audioPassthroughCodecs = try container.decode([String].self, forKey: .audioPassthroughCodecs)
            self.maxAudioChannels = try container.decode(Int.self, forKey: .maxAudioChannels)
            self.maxResolution = try container.decode(String.self, forKey: .maxResolution)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.codecsVideo, forKey: .codecsVideo)
            try container.encode(self.codecsAudio, forKey: .codecsAudio)
            try container.encode(self.audioPassthroughCodecs, forKey: .audioPassthroughCodecs)
            try container.encode(self.maxAudioChannels, forKey: .maxAudioChannels)
            try container.encode(self.maxResolution, forKey: .maxResolution)
        }
    }

    /// Wire type `internal/livetv.CreateSeriesRuleRequest`. Direction: request. Dialect: upstream-compat. Registered root.
    public struct CreateSeriesRuleRequest: Codable, Hashable, Sendable {
        public let seriesId: String
        public let channelId: String?
        public let titleMatch: String
        public let newOnly: Bool
        public let keepLast: Int
        public let enabled: Bool?

        public enum CodingKeys: String, CodingKey {
            case seriesId = "series_id"
            case channelId = "channel_id"
            case titleMatch = "title_match"
            case newOnly = "new_only"
            case keepLast = "keep_last"
            case enabled = "enabled"
        }

        public init(
            seriesId: String,
            channelId: String? = nil,
            titleMatch: String,
            newOnly: Bool,
            keepLast: Int,
            enabled: Bool? = nil
        ) {
            self.seriesId = seriesId
            self.channelId = channelId
            self.titleMatch = titleMatch
            self.newOnly = newOnly
            self.keepLast = keepLast
            self.enabled = enabled
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.seriesId = try container.decode(String.self, forKey: .seriesId)
            self.channelId = try container.decodeIfPresent(String.self, forKey: .channelId)
            self.titleMatch = try container.decode(String.self, forKey: .titleMatch)
            self.newOnly = try container.decode(Bool.self, forKey: .newOnly)
            self.keepLast = try container.decode(Int.self, forKey: .keepLast)
            self.enabled = try container.decodeIfPresent(Bool.self, forKey: .enabled)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.seriesId, forKey: .seriesId)
            try container.encodeIfPresent(self.channelId, forKey: .channelId)
            try container.encode(self.titleMatch, forKey: .titleMatch)
            try container.encode(self.newOnly, forKey: .newOnly)
            try container.encode(self.keepLast, forKey: .keepLast)
            try container.encodeIfPresent(self.enabled, forKey: .enabled)
        }
    }

    /// Wire type `internal/livetv.GuideResponse`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct GuideResponse: Codable, Hashable, Sendable {
        public let programs: [Program]
        public let start: String
        public let end: String

        public enum CodingKeys: String, CodingKey {
            case programs = "programs"
            case start = "start"
            case end = "end"
        }

        public init(
            programs: [Program] = [],
            start: String = "",
            end: String = ""
        ) {
            self.programs = programs
            self.start = start
            self.end = end
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.programs = try container.decodeIfPresent([Program].self, forKey: .programs) ?? []
            self.start = try container.decodeIfPresent(String.self, forKey: .start) ?? ""
            self.end = try container.decodeIfPresent(String.self, forKey: .end) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.programs, forKey: .programs)
            try container.encode(self.start, forKey: .start)
            try container.encode(self.end, forKey: .end)
        }
    }

    /// Wire type `internal/livetv.Program`. Direction: response. Dialect: upstream-compat.
    public struct Program: Codable, Hashable, Sendable {
        public let id: String
        public let channelId: String
        public let sourceId: String
        public let seriesId: String
        public let externalId: String
        public let start: String
        public let stop: String
        public let title: String
        public let subtitle: String
        public let description: String
        public let season: Int?
        public let episode: Int?
        public let genres: [String]
        public let imageUrl: String
        public let isNew: Bool
        public let isLive: Bool

        public enum CodingKeys: String, CodingKey {
            case id = "id"
            case channelId = "channel_id"
            case sourceId = "source_id"
            case seriesId = "series_id"
            case externalId = "external_id"
            case start = "start"
            case stop = "stop"
            case title = "title"
            case subtitle = "subtitle"
            case description = "description"
            case season = "season"
            case episode = "episode"
            case genres = "genres"
            case imageUrl = "image_url"
            case isNew = "is_new"
            case isLive = "is_live"
        }

        public init(
            id: String = "",
            channelId: String = "",
            sourceId: String = "",
            seriesId: String = "",
            externalId: String = "",
            start: String = "",
            stop: String = "",
            title: String = "",
            subtitle: String = "",
            description: String = "",
            season: Int? = nil,
            episode: Int? = nil,
            genres: [String] = [],
            imageUrl: String = "",
            isNew: Bool = false,
            isLive: Bool = false
        ) {
            self.id = id
            self.channelId = channelId
            self.sourceId = sourceId
            self.seriesId = seriesId
            self.externalId = externalId
            self.start = start
            self.stop = stop
            self.title = title
            self.subtitle = subtitle
            self.description = description
            self.season = season
            self.episode = episode
            self.genres = genres
            self.imageUrl = imageUrl
            self.isNew = isNew
            self.isLive = isLive
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.id = try container.decodeIfPresent(String.self, forKey: .id) ?? ""
            self.channelId = try container.decodeIfPresent(String.self, forKey: .channelId) ?? ""
            self.sourceId = try container.decodeIfPresent(String.self, forKey: .sourceId) ?? ""
            self.seriesId = try container.decodeIfPresent(String.self, forKey: .seriesId) ?? ""
            self.externalId = try container.decodeIfPresent(String.self, forKey: .externalId) ?? ""
            self.start = try container.decodeIfPresent(String.self, forKey: .start) ?? ""
            self.stop = try container.decodeIfPresent(String.self, forKey: .stop) ?? ""
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.subtitle = try container.decodeIfPresent(String.self, forKey: .subtitle) ?? ""
            self.description = try container.decodeIfPresent(String.self, forKey: .description) ?? ""
            self.season = try container.decodeIfPresent(Int.self, forKey: .season)
            self.episode = try container.decodeIfPresent(Int.self, forKey: .episode)
            self.genres = try container.decodeIfPresent([String].self, forKey: .genres) ?? []
            self.imageUrl = try container.decodeIfPresent(String.self, forKey: .imageUrl) ?? ""
            self.isNew = try container.decodeIfPresent(Bool.self, forKey: .isNew) ?? false
            self.isLive = try container.decodeIfPresent(Bool.self, forKey: .isLive) ?? false
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.id, forKey: .id)
            try container.encode(self.channelId, forKey: .channelId)
            try container.encode(self.sourceId, forKey: .sourceId)
            try container.encode(self.seriesId, forKey: .seriesId)
            try container.encode(self.externalId, forKey: .externalId)
            try container.encode(self.start, forKey: .start)
            try container.encode(self.stop, forKey: .stop)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.subtitle, forKey: .subtitle)
            try container.encode(self.description, forKey: .description)
            try container.encodeIfPresent(self.season, forKey: .season)
            try container.encodeIfPresent(self.episode, forKey: .episode)
            try container.encode(self.genres, forKey: .genres)
            try container.encode(self.imageUrl, forKey: .imageUrl)
            try container.encode(self.isNew, forKey: .isNew)
            try container.encode(self.isLive, forKey: .isLive)
        }
    }

    /// Wire type `internal/livetv.Recording`. Direction: response. Dialect: upstream-compat.
    public struct Recording: Codable, Hashable, Sendable {
        public let id: String
        public let programId: String
        public let channelId: String
        public let seriesRuleId: String
        public let userId: Int
        public let profileId: String
        public let status: String
        public let path: String
        public let libraryItemId: String
        public let start: String
        public let stop: String
        public let title: String
        public let lastError: String

        public enum CodingKeys: String, CodingKey {
            case id = "id"
            case programId = "program_id"
            case channelId = "channel_id"
            case seriesRuleId = "series_rule_id"
            case userId = "user_id"
            case profileId = "profile_id"
            case status = "status"
            case path = "path"
            case libraryItemId = "library_item_id"
            case start = "start"
            case stop = "stop"
            case title = "title"
            case lastError = "last_error"
        }

        public init(
            id: String = "",
            programId: String = "",
            channelId: String = "",
            seriesRuleId: String = "",
            userId: Int = 0,
            profileId: String = "",
            status: String = "",
            path: String = "",
            libraryItemId: String = "",
            start: String = "",
            stop: String = "",
            title: String = "",
            lastError: String = ""
        ) {
            self.id = id
            self.programId = programId
            self.channelId = channelId
            self.seriesRuleId = seriesRuleId
            self.userId = userId
            self.profileId = profileId
            self.status = status
            self.path = path
            self.libraryItemId = libraryItemId
            self.start = start
            self.stop = stop
            self.title = title
            self.lastError = lastError
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.id = try container.decodeIfPresent(String.self, forKey: .id) ?? ""
            self.programId = try container.decodeIfPresent(String.self, forKey: .programId) ?? ""
            self.channelId = try container.decodeIfPresent(String.self, forKey: .channelId) ?? ""
            self.seriesRuleId = try container.decodeIfPresent(String.self, forKey: .seriesRuleId) ?? ""
            self.userId = try container.decodeIfPresent(Int.self, forKey: .userId) ?? 0
            self.profileId = try container.decodeIfPresent(String.self, forKey: .profileId) ?? ""
            self.status = try container.decodeIfPresent(String.self, forKey: .status) ?? ""
            self.path = try container.decodeIfPresent(String.self, forKey: .path) ?? ""
            self.libraryItemId = try container.decodeIfPresent(String.self, forKey: .libraryItemId) ?? ""
            self.start = try container.decodeIfPresent(String.self, forKey: .start) ?? ""
            self.stop = try container.decodeIfPresent(String.self, forKey: .stop) ?? ""
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.lastError = try container.decodeIfPresent(String.self, forKey: .lastError) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.id, forKey: .id)
            try container.encode(self.programId, forKey: .programId)
            try container.encode(self.channelId, forKey: .channelId)
            try container.encode(self.seriesRuleId, forKey: .seriesRuleId)
            try container.encode(self.userId, forKey: .userId)
            try container.encode(self.profileId, forKey: .profileId)
            try container.encode(self.status, forKey: .status)
            try container.encode(self.path, forKey: .path)
            try container.encode(self.libraryItemId, forKey: .libraryItemId)
            try container.encode(self.start, forKey: .start)
            try container.encode(self.stop, forKey: .stop)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.lastError, forKey: .lastError)
        }
    }

    /// Wire type `internal/livetv.RecordingsResponse`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct RecordingsResponse: Codable, Hashable, Sendable {
        public let recordings: [Recording]

        public enum CodingKeys: String, CodingKey {
            case recordings = "recordings"
        }

        public init(
            recordings: [Recording] = []
        ) {
            self.recordings = recordings
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.recordings = try container.decodeIfPresent([Recording].self, forKey: .recordings) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.recordings, forKey: .recordings)
        }
    }

    /// Wire type `internal/livetv.ScheduleRecordingRequest`. Direction: request. Dialect: upstream-compat. Registered root.
    public struct ScheduleRecordingRequest: Codable, Hashable, Sendable {
        public let programId: String
        public let channelId: String
        public let start: String
        public let stop: String
        public let title: String

        public enum CodingKeys: String, CodingKey {
            case programId = "program_id"
            case channelId = "channel_id"
            case start = "start"
            case stop = "stop"
            case title = "title"
        }

        public init(
            programId: String,
            channelId: String,
            start: String,
            stop: String,
            title: String
        ) {
            self.programId = programId
            self.channelId = channelId
            self.start = start
            self.stop = stop
            self.title = title
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.programId = try container.decode(String.self, forKey: .programId)
            self.channelId = try container.decode(String.self, forKey: .channelId)
            self.start = try container.decode(String.self, forKey: .start)
            self.stop = try container.decode(String.self, forKey: .stop)
            self.title = try container.decode(String.self, forKey: .title)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.programId, forKey: .programId)
            try container.encode(self.channelId, forKey: .channelId)
            try container.encode(self.start, forKey: .start)
            try container.encode(self.stop, forKey: .stop)
            try container.encode(self.title, forKey: .title)
        }
    }

    /// Wire type `internal/livetv.SeriesRule`. Direction: response. Dialect: upstream-compat.
    public struct SeriesRule: Codable, Hashable, Sendable {
        public let id: String
        public let seriesId: String
        public let channelId: String?
        public let userId: Int
        public let profileId: String
        public let titleMatch: String
        public let newOnly: Bool
        public let keepLast: Int
        public let enabled: Bool

        public enum CodingKeys: String, CodingKey {
            case id = "id"
            case seriesId = "series_id"
            case channelId = "channel_id"
            case userId = "user_id"
            case profileId = "profile_id"
            case titleMatch = "title_match"
            case newOnly = "new_only"
            case keepLast = "keep_last"
            case enabled = "enabled"
        }

        public init(
            id: String = "",
            seriesId: String = "",
            channelId: String? = nil,
            userId: Int = 0,
            profileId: String = "",
            titleMatch: String = "",
            newOnly: Bool = false,
            keepLast: Int = 0,
            enabled: Bool = false
        ) {
            self.id = id
            self.seriesId = seriesId
            self.channelId = channelId
            self.userId = userId
            self.profileId = profileId
            self.titleMatch = titleMatch
            self.newOnly = newOnly
            self.keepLast = keepLast
            self.enabled = enabled
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.id = try container.decodeIfPresent(String.self, forKey: .id) ?? ""
            self.seriesId = try container.decodeIfPresent(String.self, forKey: .seriesId) ?? ""
            self.channelId = try container.decodeIfPresent(String.self, forKey: .channelId)
            self.userId = try container.decodeIfPresent(Int.self, forKey: .userId) ?? 0
            self.profileId = try container.decodeIfPresent(String.self, forKey: .profileId) ?? ""
            self.titleMatch = try container.decodeIfPresent(String.self, forKey: .titleMatch) ?? ""
            self.newOnly = try container.decodeIfPresent(Bool.self, forKey: .newOnly) ?? false
            self.keepLast = try container.decodeIfPresent(Int.self, forKey: .keepLast) ?? 0
            self.enabled = try container.decodeIfPresent(Bool.self, forKey: .enabled) ?? false
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.id, forKey: .id)
            try container.encode(self.seriesId, forKey: .seriesId)
            try container.encodeIfPresent(self.channelId, forKey: .channelId)
            try container.encode(self.userId, forKey: .userId)
            try container.encode(self.profileId, forKey: .profileId)
            try container.encode(self.titleMatch, forKey: .titleMatch)
            try container.encode(self.newOnly, forKey: .newOnly)
            try container.encode(self.keepLast, forKey: .keepLast)
            try container.encode(self.enabled, forKey: .enabled)
        }
    }

    /// Wire type `internal/livetv.SeriesRulesResponse`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct SeriesRulesResponse: Codable, Hashable, Sendable {
        public let seriesRules: [SeriesRule]

        public enum CodingKeys: String, CodingKey {
            case seriesRules = "series_rules"
        }

        public init(
            seriesRules: [SeriesRule] = []
        ) {
            self.seriesRules = seriesRules
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.seriesRules = try container.decodeIfPresent([SeriesRule].self, forKey: .seriesRules) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.seriesRules, forKey: .seriesRules)
        }
    }

    /// Wire type `internal/livetv.SessionStartResponse`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct SessionStartResponse: Codable, Hashable, Sendable {
        public let sessionId: String
        public let playbackTicket: String
        public let hlsurl: String
        public let streamUrl: String
        public let transport: String
        public let note: String

        public enum CodingKeys: String, CodingKey {
            case sessionId = "session_id"
            case playbackTicket = "playback_ticket"
            case hlsurl = "hls_url"
            case streamUrl = "stream_url"
            case transport = "transport"
            case note = "note"
        }

        public init(
            sessionId: String = "",
            playbackTicket: String = "",
            hlsurl: String = "",
            streamUrl: String = "",
            transport: String = "",
            note: String = ""
        ) {
            self.sessionId = sessionId
            self.playbackTicket = playbackTicket
            self.hlsurl = hlsurl
            self.streamUrl = streamUrl
            self.transport = transport
            self.note = note
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.playbackTicket = try container.decodeIfPresent(String.self, forKey: .playbackTicket) ?? ""
            self.hlsurl = try container.decodeIfPresent(String.self, forKey: .hlsurl) ?? ""
            self.streamUrl = try container.decodeIfPresent(String.self, forKey: .streamUrl) ?? ""
            self.transport = try container.decodeIfPresent(String.self, forKey: .transport) ?? ""
            self.note = try container.decodeIfPresent(String.self, forKey: .note) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.playbackTicket, forKey: .playbackTicket)
            try container.encode(self.hlsurl, forKey: .hlsurl)
            try container.encode(self.streamUrl, forKey: .streamUrl)
            try container.encode(self.transport, forKey: .transport)
            try container.encode(self.note, forKey: .note)
        }
    }
}
