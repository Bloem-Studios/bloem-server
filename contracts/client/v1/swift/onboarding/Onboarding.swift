// Code generated by cmd/clientdtogen from bloem-server internal/onboarding. DO NOT EDIT.
// Server revision: 55d3ae52f51751e4da769252179e9988a005782e (generator v1, registry contracts/client/v1/registry.json)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `internal/onboarding`.
public enum Onboarding {
    /// Wire type `internal/onboarding.Flow`. Direction: response. Dialect: bloem. Registered root.
    public struct Flow: Codable, Hashable, Sendable {
        public let version: Int
        public let tourId: String
        public let steps: [Step]

        public enum CodingKeys: String, CodingKey {
            case version = "version"
            case tourId = "tour_id"
            case steps = "steps"
        }

        public init(
            version: Int = 0,
            tourId: String = "",
            steps: [Step] = []
        ) {
            self.version = version
            self.tourId = tourId
            self.steps = steps
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.version = try container.decodeIfPresent(Int.self, forKey: .version) ?? 0
            self.tourId = try container.decodeIfPresent(String.self, forKey: .tourId) ?? ""
            self.steps = try container.decodeIfPresent([Step].self, forKey: .steps) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.version, forKey: .version)
            try container.encode(self.tourId, forKey: .tourId)
            try container.encode(self.steps, forKey: .steps)
        }
    }

    /// Wire type `internal/onboarding.SettingOption`. Direction: response. Dialect: bloem.
    public struct SettingOption: Codable, Hashable, Sendable {
        public let value: String
        public let label: String

        public enum CodingKeys: String, CodingKey {
            case value = "value"
            case label = "label"
        }

        public init(
            value: String = "",
            label: String = ""
        ) {
            self.value = value
            self.label = label
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.value = try container.decodeIfPresent(String.self, forKey: .value) ?? ""
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.value, forKey: .value)
            try container.encode(self.label, forKey: .label)
        }
    }

    /// Wire type `internal/onboarding.SettingSpec`. Direction: response. Dialect: bloem.
    public struct SettingSpec: Codable, Hashable, Sendable {
        public let target: String
        public let key: String
        public let control: String
        public let options: [SettingOption]
        public let defaultValue: String
        public let label: String

        public enum CodingKeys: String, CodingKey {
            case target = "target"
            case key = "key"
            case control = "control"
            case options = "options"
            case defaultValue = "default"
            case label = "label"
        }

        public init(
            target: String = "",
            key: String = "",
            control: String = "",
            options: [SettingOption] = [],
            defaultValue: String = "",
            label: String = ""
        ) {
            self.target = target
            self.key = key
            self.control = control
            self.options = options
            self.defaultValue = defaultValue
            self.label = label
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.target = try container.decodeIfPresent(String.self, forKey: .target) ?? ""
            self.key = try container.decodeIfPresent(String.self, forKey: .key) ?? ""
            self.control = try container.decodeIfPresent(String.self, forKey: .control) ?? ""
            self.options = try container.decodeIfPresent([SettingOption].self, forKey: .options) ?? []
            self.defaultValue = try container.decodeIfPresent(String.self, forKey: .defaultValue) ?? ""
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.target, forKey: .target)
            try container.encode(self.key, forKey: .key)
            try container.encode(self.control, forKey: .control)
            try container.encode(self.options, forKey: .options)
            try container.encode(self.defaultValue, forKey: .defaultValue)
            try container.encode(self.label, forKey: .label)
        }
    }

    /// Wire type `internal/onboarding.Step`. Direction: response. Dialect: bloem.
    public struct Step: Codable, Hashable, Sendable {
        public let id: String
        public let kind: String
        public let title: String
        public let body: String
        public let illustration: String
        public let setting: SettingSpec?
        public let route: String
        public let actionLabel: String
        public let links: [StepLink]

        public enum CodingKeys: String, CodingKey {
            case id = "id"
            case kind = "kind"
            case title = "title"
            case body = "body"
            case illustration = "illustration"
            case setting = "setting"
            case route = "route"
            case actionLabel = "action_label"
            case links = "links"
        }

        public init(
            id: String = "",
            kind: String = "",
            title: String = "",
            body: String = "",
            illustration: String = "",
            setting: SettingSpec? = nil,
            route: String = "",
            actionLabel: String = "",
            links: [StepLink] = []
        ) {
            self.id = id
            self.kind = kind
            self.title = title
            self.body = body
            self.illustration = illustration
            self.setting = setting
            self.route = route
            self.actionLabel = actionLabel
            self.links = links
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.id = try container.decodeIfPresent(String.self, forKey: .id) ?? ""
            self.kind = try container.decodeIfPresent(String.self, forKey: .kind) ?? ""
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.body = try container.decodeIfPresent(String.self, forKey: .body) ?? ""
            self.illustration = try container.decodeIfPresent(String.self, forKey: .illustration) ?? ""
            self.setting = try container.decodeIfPresent(SettingSpec.self, forKey: .setting)
            self.route = try container.decodeIfPresent(String.self, forKey: .route) ?? ""
            self.actionLabel = try container.decodeIfPresent(String.self, forKey: .actionLabel) ?? ""
            self.links = try container.decodeIfPresent([StepLink].self, forKey: .links) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.id, forKey: .id)
            try container.encode(self.kind, forKey: .kind)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.body, forKey: .body)
            try container.encode(self.illustration, forKey: .illustration)
            try container.encodeIfPresent(self.setting, forKey: .setting)
            try container.encode(self.route, forKey: .route)
            try container.encode(self.actionLabel, forKey: .actionLabel)
            try container.encode(self.links, forKey: .links)
        }
    }

    /// Wire type `internal/onboarding.StepLink`. Direction: response. Dialect: bloem.
    public struct StepLink: Codable, Hashable, Sendable {
        public let label: String
        public let url: String

        public enum CodingKeys: String, CodingKey {
            case label = "label"
            case url = "url"
        }

        public init(
            label: String = "",
            url: String = ""
        ) {
            self.label = label
            self.url = url
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
            self.url = try container.decodeIfPresent(String.self, forKey: .url) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.label, forKey: .label)
            try container.encode(self.url, forKey: .url)
        }
    }
}
