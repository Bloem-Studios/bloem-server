// Code generated by cmd/clientdtogen from bloem-server internal/playback. DO NOT EDIT.
// Server revision: 55d3ae52f51751e4da769252179e9988a005782e (generator v1, registry contracts/client/v1/registry.json)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `internal/playback`.
public enum Playback {
    /// Wire type `internal/playback.AckEnvelope`. Direction: request. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct AckEnvelope: Codable, Hashable, Sendable {
        public let type: RealtimeMessageType
        public let commandId: String
        public let sessionId: String
        public let status: RealtimeAckStatus

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case commandId = "command_id"
            case sessionId = "session_id"
            case status = "status"
        }

        public init(
            type: RealtimeMessageType,
            commandId: String,
            sessionId: String,
            status: RealtimeAckStatus
        ) {
            self.type = type
            self.commandId = commandId
            self.sessionId = sessionId
            self.status = status
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decode(RealtimeMessageType.self, forKey: .type)
            self.commandId = try container.decode(String.self, forKey: .commandId)
            self.sessionId = try container.decode(String.self, forKey: .sessionId)
            self.status = try container.decode(RealtimeAckStatus.self, forKey: .status)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.commandId, forKey: .commandId)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.status, forKey: .status)
        }
    }

    /// Wire type `internal/playback.AppliedQuirkV3`. Direction: response. Dialect: upstream-compat.
    public struct AppliedQuirkV3: Codable, Hashable, Sendable {
        public let id: String
        public let registryRevision: String
        public let action: String
        public let reason: String

        public enum CodingKeys: String, CodingKey {
            case id = "id"
            case registryRevision = "registry_revision"
            case action = "action"
            case reason = "reason"
        }

        public init(
            id: String = "",
            registryRevision: String = "",
            action: String = "",
            reason: String = ""
        ) {
            self.id = id
            self.registryRevision = registryRevision
            self.action = action
            self.reason = reason
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.id = try container.decodeIfPresent(String.self, forKey: .id) ?? ""
            self.registryRevision = try container.decodeIfPresent(String.self, forKey: .registryRevision) ?? ""
            self.action = try container.decodeIfPresent(String.self, forKey: .action) ?? ""
            self.reason = try container.decodeIfPresent(String.self, forKey: .reason) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.id, forKey: .id)
            try container.encode(self.registryRevision, forKey: .registryRevision)
            try container.encode(self.action, forKey: .action)
            try container.encode(self.reason, forKey: .reason)
        }
    }

    /// Wire type `internal/playback.AudioClaimsV3`. Direction: response. Dialect: upstream-compat.
    public struct AudioClaimsV3: Codable, Hashable, Sendable {
        public let codec: String
        public let passthrough: Bool
        public let atmosPreserved: Bool
        public let dtsVariant: String
        public let reason: String

        public enum CodingKeys: String, CodingKey {
            case codec = "codec"
            case passthrough = "passthrough"
            case atmosPreserved = "atmos_preserved"
            case dtsVariant = "dts_variant"
            case reason = "reason"
        }

        public init(
            codec: String = "",
            passthrough: Bool = false,
            atmosPreserved: Bool = false,
            dtsVariant: String = "",
            reason: String = ""
        ) {
            self.codec = codec
            self.passthrough = passthrough
            self.atmosPreserved = atmosPreserved
            self.dtsVariant = dtsVariant
            self.reason = reason
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.codec = try container.decodeIfPresent(String.self, forKey: .codec) ?? ""
            self.passthrough = try container.decodeIfPresent(Bool.self, forKey: .passthrough) ?? false
            self.atmosPreserved = try container.decodeIfPresent(Bool.self, forKey: .atmosPreserved) ?? false
            self.dtsVariant = try container.decodeIfPresent(String.self, forKey: .dtsVariant) ?? ""
            self.reason = try container.decodeIfPresent(String.self, forKey: .reason) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.codec, forKey: .codec)
            try container.encode(self.passthrough, forKey: .passthrough)
            try container.encode(self.atmosPreserved, forKey: .atmosPreserved)
            try container.encode(self.dtsVariant, forKey: .dtsVariant)
            try container.encode(self.reason, forKey: .reason)
        }
    }

    /// Wire type `internal/playback.AudioPassthroughEntryV3`. Direction: request. Dialect: upstream-compat.
    public struct AudioPassthroughEntryV3: Codable, Hashable, Sendable {
        public let codec: String
        public let channelCounts: [Int]
        public let layouts: [String]

        public enum CodingKeys: String, CodingKey {
            case codec = "codec"
            case channelCounts = "channel_counts"
            case layouts = "layouts"
        }

        public init(
            codec: String,
            channelCounts: [Int] = [],
            layouts: [String] = []
        ) {
            self.codec = codec
            self.channelCounts = channelCounts
            self.layouts = layouts
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.codec = try container.decode(String.self, forKey: .codec)
            self.channelCounts = try container.decodeIfPresent([Int].self, forKey: .channelCounts) ?? []
            self.layouts = try container.decodeIfPresent([String].self, forKey: .layouts) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.codec, forKey: .codec)
            try container.encode(self.channelCounts, forKey: .channelCounts)
            try container.encode(self.layouts, forKey: .layouts)
        }
    }

    /// Wire type `internal/playback.AudioPassthroughV3`. Direction: request. Dialect: upstream-compat.
    public struct AudioPassthroughV3: Codable, Hashable, Sendable {
        public let passthroughCodecs: [String]
        public let spatializerEnabled: Bool
        public let maxChannels: Int
        public let entries: [AudioPassthroughEntryV3]

        public enum CodingKeys: String, CodingKey {
            case passthroughCodecs = "passthrough_codecs"
            case spatializerEnabled = "spatializer_enabled"
            case maxChannels = "max_channels"
            case entries = "entries"
        }

        public init(
            passthroughCodecs: [String],
            spatializerEnabled: Bool,
            maxChannels: Int,
            entries: [AudioPassthroughEntryV3] = []
        ) {
            self.passthroughCodecs = passthroughCodecs
            self.spatializerEnabled = spatializerEnabled
            self.maxChannels = maxChannels
            self.entries = entries
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.passthroughCodecs = try container.decode([String].self, forKey: .passthroughCodecs)
            self.spatializerEnabled = try container.decode(Bool.self, forKey: .spatializerEnabled)
            self.maxChannels = try container.decode(Int.self, forKey: .maxChannels)
            self.entries = try container.decodeIfPresent([AudioPassthroughEntryV3].self, forKey: .entries) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.passthroughCodecs, forKey: .passthroughCodecs)
            try container.encode(self.spatializerEnabled, forKey: .spatializerEnabled)
            try container.encode(self.maxChannels, forKey: .maxChannels)
            try container.encode(self.entries, forKey: .entries)
        }
    }

    /// Wire type `internal/playback.AvailableQualityV3`. Direction: response. Dialect: upstream-compat.
    public struct AvailableQualityV3: Codable, Hashable, Sendable {
        public let label: String
        public let displayName: String
        public let height: Int
        public let bitrateKbps: Int
        public let preservesSource: Bool

        public enum CodingKeys: String, CodingKey {
            case label = "label"
            case displayName = "display_name"
            case height = "height"
            case bitrateKbps = "bitrate_kbps"
            case preservesSource = "preserves_source"
        }

        public init(
            label: String = "",
            displayName: String = "",
            height: Int = 0,
            bitrateKbps: Int = 0,
            preservesSource: Bool = false
        ) {
            self.label = label
            self.displayName = displayName
            self.height = height
            self.bitrateKbps = bitrateKbps
            self.preservesSource = preservesSource
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
            self.displayName = try container.decodeIfPresent(String.self, forKey: .displayName) ?? ""
            self.height = try container.decodeIfPresent(Int.self, forKey: .height) ?? 0
            self.bitrateKbps = try container.decodeIfPresent(Int.self, forKey: .bitrateKbps) ?? 0
            self.preservesSource = try container.decodeIfPresent(Bool.self, forKey: .preservesSource) ?? false
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.label, forKey: .label)
            try container.encode(self.displayName, forKey: .displayName)
            try container.encode(self.height, forKey: .height)
            try container.encode(self.bitrateKbps, forKey: .bitrateKbps)
            try container.encode(self.preservesSource, forKey: .preservesSource)
        }
    }

    /// Wire type `internal/playback.CapabilityEvidenceV3`. Direction: request. Dialect: upstream-compat.
    public struct CapabilityEvidenceV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let EXACT: CapabilityEvidenceV3 = CapabilityEvidenceV3(rawValue: "exact")
        public static let PLATFORM_ATTESTED: CapabilityEvidenceV3 = CapabilityEvidenceV3(rawValue: "platform_attested")
        public static let DECLARED: CapabilityEvidenceV3 = CapabilityEvidenceV3(rawValue: "declared")
        public static let KNOWN: [CapabilityEvidenceV3] = [EXACT, PLATFORM_ATTESTED, DECLARED]
    }

    /// Wire type `internal/playback.CapabilityResponseV3`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct CapabilityResponseV3: Codable, Hashable, Sendable {
        public let enabled: Bool
        public let protocolVersions: [Int]
        public let features: [String]
        public let deliveries: [DeliveryV3]
        public let transformations: [TransformationV3]
        public let reason: String

        public enum CodingKeys: String, CodingKey {
            case enabled = "enabled"
            case protocolVersions = "protocol_versions"
            case features = "features"
            case deliveries = "deliveries"
            case transformations = "transformations"
            case reason = "reason"
        }

        public init(
            enabled: Bool = false,
            protocolVersions: [Int] = [],
            features: [String] = [],
            deliveries: [DeliveryV3] = [],
            transformations: [TransformationV3] = [],
            reason: String = ""
        ) {
            self.enabled = enabled
            self.protocolVersions = protocolVersions
            self.features = features
            self.deliveries = deliveries
            self.transformations = transformations
            self.reason = reason
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.enabled = try container.decodeIfPresent(Bool.self, forKey: .enabled) ?? false
            self.protocolVersions = try container.decodeIfPresent([Int].self, forKey: .protocolVersions) ?? []
            self.features = try container.decodeIfPresent([String].self, forKey: .features) ?? []
            self.deliveries = try container.decodeIfPresent([DeliveryV3].self, forKey: .deliveries) ?? []
            self.transformations = try container.decodeIfPresent([TransformationV3].self, forKey: .transformations) ?? []
            self.reason = try container.decodeIfPresent(String.self, forKey: .reason) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.enabled, forKey: .enabled)
            try container.encode(self.protocolVersions, forKey: .protocolVersions)
            try container.encode(self.features, forKey: .features)
            try container.encode(self.deliveries, forKey: .deliveries)
            try container.encode(self.transformations, forKey: .transformations)
            try container.encode(self.reason, forKey: .reason)
        }
    }

    /// Wire type `internal/playback.ChapterThumbnailReadyPayload`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct ChapterThumbnailReadyPayload: Codable, Hashable, Sendable {
        public let sessionId: String
        public let fileId: Int
        public let chapterIndex: Int
        public let thumbnailUrl: String
        public let thumbnailThumbhash: String

        public enum CodingKeys: String, CodingKey {
            case sessionId = "session_id"
            case fileId = "file_id"
            case chapterIndex = "chapter_index"
            case thumbnailUrl = "thumbnail_url"
            case thumbnailThumbhash = "thumbnail_thumbhash"
        }

        public init(
            sessionId: String = "",
            fileId: Int = 0,
            chapterIndex: Int = 0,
            thumbnailUrl: String = "",
            thumbnailThumbhash: String = ""
        ) {
            self.sessionId = sessionId
            self.fileId = fileId
            self.chapterIndex = chapterIndex
            self.thumbnailUrl = thumbnailUrl
            self.thumbnailThumbhash = thumbnailThumbhash
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.fileId = try container.decodeIfPresent(Int.self, forKey: .fileId) ?? 0
            self.chapterIndex = try container.decodeIfPresent(Int.self, forKey: .chapterIndex) ?? 0
            self.thumbnailUrl = try container.decodeIfPresent(String.self, forKey: .thumbnailUrl) ?? ""
            self.thumbnailThumbhash = try container.decodeIfPresent(String.self, forKey: .thumbnailThumbhash) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encode(self.chapterIndex, forKey: .chapterIndex)
            try container.encode(self.thumbnailUrl, forKey: .thumbnailUrl)
            try container.encode(self.thumbnailThumbhash, forKey: .thumbnailThumbhash)
        }
    }

    /// Wire type `internal/playback.ClientCapabilities`. Direction: request. Dialect: upstream-compat.
    public struct ClientCapabilities: Codable, Hashable, Sendable {
        public let clientFeatures: [String]
        public let videoEvidence: CapabilityEvidenceV3
        public let codecsVideo: [String]
        public let codecsAudio: [String]
        public let audioPassthroughCodecs: [String]
        public let containers: [String]
        public let maxResolution: String
        public let hdr: Bool
        public let videoDecode: [VideoDecodeCapabilityV3]

        public enum CodingKeys: String, CodingKey {
            case clientFeatures = "client_features"
            case videoEvidence = "video_evidence"
            case codecsVideo = "codecs_video"
            case codecsAudio = "codecs_audio"
            case audioPassthroughCodecs = "audio_passthrough_codecs"
            case containers = "containers"
            case maxResolution = "max_resolution"
            case hdr = "hdr"
            case videoDecode = "video_decode"
        }

        public init(
            clientFeatures: [String] = [],
            videoEvidence: CapabilityEvidenceV3 = CapabilityEvidenceV3(rawValue: ""),
            codecsVideo: [String],
            codecsAudio: [String],
            audioPassthroughCodecs: [String] = [],
            containers: [String],
            maxResolution: String,
            hdr: Bool,
            videoDecode: [VideoDecodeCapabilityV3] = []
        ) {
            self.clientFeatures = clientFeatures
            self.videoEvidence = videoEvidence
            self.codecsVideo = codecsVideo
            self.codecsAudio = codecsAudio
            self.audioPassthroughCodecs = audioPassthroughCodecs
            self.containers = containers
            self.maxResolution = maxResolution
            self.hdr = hdr
            self.videoDecode = videoDecode
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.clientFeatures = try container.decodeIfPresent([String].self, forKey: .clientFeatures) ?? []
            self.videoEvidence = try container.decodeIfPresent(CapabilityEvidenceV3.self, forKey: .videoEvidence) ?? CapabilityEvidenceV3(rawValue: "")
            self.codecsVideo = try container.decode([String].self, forKey: .codecsVideo)
            self.codecsAudio = try container.decode([String].self, forKey: .codecsAudio)
            self.audioPassthroughCodecs = try container.decodeIfPresent([String].self, forKey: .audioPassthroughCodecs) ?? []
            self.containers = try container.decode([String].self, forKey: .containers)
            self.maxResolution = try container.decode(String.self, forKey: .maxResolution)
            self.hdr = try container.decode(Bool.self, forKey: .hdr)
            self.videoDecode = try container.decodeIfPresent([VideoDecodeCapabilityV3].self, forKey: .videoDecode) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.clientFeatures, forKey: .clientFeatures)
            try container.encode(self.videoEvidence, forKey: .videoEvidence)
            try container.encode(self.codecsVideo, forKey: .codecsVideo)
            try container.encode(self.codecsAudio, forKey: .codecsAudio)
            try container.encode(self.audioPassthroughCodecs, forKey: .audioPassthroughCodecs)
            try container.encode(self.containers, forKey: .containers)
            try container.encode(self.maxResolution, forKey: .maxResolution)
            try container.encode(self.hdr, forKey: .hdr)
            try container.encode(self.videoDecode, forKey: .videoDecode)
        }
    }

    /// Wire type `internal/playback.ClientCodecCapabilitiesV3`. Direction: request. Dialect: upstream-compat.
    public struct ClientCodecCapabilitiesV3: Codable, Hashable, Sendable {
        public let videoEvidence: CapabilityEvidenceV3
        public let audioEvidence: CapabilityEvidenceV3
        public let codecsVideo: [String]
        public let codecsVideoHardware: [String]
        public let codecsAudio: [String]
        public let containers: [String]
        public let maxResolution: String
        public let hdr: Bool
        public let hdrDetails: HDRCapabilitiesV3?
        public let audioPassthrough: AudioPassthroughV3?
        public let videoDecode: [VideoDecodeCapabilityV3]

        public enum CodingKeys: String, CodingKey {
            case videoEvidence = "video_evidence"
            case audioEvidence = "audio_evidence"
            case codecsVideo = "codecs_video"
            case codecsVideoHardware = "codecs_video_hardware"
            case codecsAudio = "codecs_audio"
            case containers = "containers"
            case maxResolution = "max_resolution"
            case hdr = "hdr"
            case hdrDetails = "hdr_details"
            case audioPassthrough = "audio_passthrough"
            case videoDecode = "video_decode"
        }

        public init(
            videoEvidence: CapabilityEvidenceV3,
            audioEvidence: CapabilityEvidenceV3,
            codecsVideo: [String],
            codecsVideoHardware: [String],
            codecsAudio: [String],
            containers: [String],
            maxResolution: String = "",
            hdr: Bool,
            hdrDetails: HDRCapabilitiesV3? = nil,
            audioPassthrough: AudioPassthroughV3? = nil,
            videoDecode: [VideoDecodeCapabilityV3] = []
        ) {
            self.videoEvidence = videoEvidence
            self.audioEvidence = audioEvidence
            self.codecsVideo = codecsVideo
            self.codecsVideoHardware = codecsVideoHardware
            self.codecsAudio = codecsAudio
            self.containers = containers
            self.maxResolution = maxResolution
            self.hdr = hdr
            self.hdrDetails = hdrDetails
            self.audioPassthrough = audioPassthrough
            self.videoDecode = videoDecode
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.videoEvidence = try container.decode(CapabilityEvidenceV3.self, forKey: .videoEvidence)
            self.audioEvidence = try container.decode(CapabilityEvidenceV3.self, forKey: .audioEvidence)
            self.codecsVideo = try container.decode([String].self, forKey: .codecsVideo)
            self.codecsVideoHardware = try container.decode([String].self, forKey: .codecsVideoHardware)
            self.codecsAudio = try container.decode([String].self, forKey: .codecsAudio)
            self.containers = try container.decode([String].self, forKey: .containers)
            self.maxResolution = try container.decodeIfPresent(String.self, forKey: .maxResolution) ?? ""
            self.hdr = try container.decode(Bool.self, forKey: .hdr)
            self.hdrDetails = try container.decodeIfPresent(HDRCapabilitiesV3.self, forKey: .hdrDetails)
            self.audioPassthrough = try container.decodeIfPresent(AudioPassthroughV3.self, forKey: .audioPassthrough)
            self.videoDecode = try container.decodeIfPresent([VideoDecodeCapabilityV3].self, forKey: .videoDecode) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.videoEvidence, forKey: .videoEvidence)
            try container.encode(self.audioEvidence, forKey: .audioEvidence)
            try container.encode(self.codecsVideo, forKey: .codecsVideo)
            try container.encode(self.codecsVideoHardware, forKey: .codecsVideoHardware)
            try container.encode(self.codecsAudio, forKey: .codecsAudio)
            try container.encode(self.containers, forKey: .containers)
            try container.encode(self.maxResolution, forKey: .maxResolution)
            try container.encode(self.hdr, forKey: .hdr)
            try container.encodeIfPresent(self.hdrDetails, forKey: .hdrDetails)
            try container.encodeIfPresent(self.audioPassthrough, forKey: .audioPassthrough)
            try container.encode(self.videoDecode, forKey: .videoDecode)
        }
    }

    /// Wire type `internal/playback.ClientPlaybackContextV3`. Direction: request. Dialect: upstream-compat.
    public struct ClientPlaybackContextV3: Codable, Hashable, Sendable {
        public let protocolVersion: Int
        public let formFactor: String
        public let appVersion: String
        public let appBuild: String
        public let appChannel: String
        public let device: DeviceContextV3
        public let output: OutputContextV3
        public let deliveries: [String: DeliveryCapabilityV3]

        public enum CodingKeys: String, CodingKey {
            case protocolVersion = "protocol_version"
            case formFactor = "form_factor"
            case appVersion = "app_version"
            case appBuild = "app_build"
            case appChannel = "app_channel"
            case device = "device"
            case output = "output"
            case deliveries = "deliveries"
        }

        public init(
            protocolVersion: Int,
            formFactor: String,
            appVersion: String,
            appBuild: String = "",
            appChannel: String = "",
            device: DeviceContextV3,
            output: OutputContextV3,
            deliveries: [String: DeliveryCapabilityV3]
        ) {
            self.protocolVersion = protocolVersion
            self.formFactor = formFactor
            self.appVersion = appVersion
            self.appBuild = appBuild
            self.appChannel = appChannel
            self.device = device
            self.output = output
            self.deliveries = deliveries
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.protocolVersion = try container.decode(Int.self, forKey: .protocolVersion)
            self.formFactor = try container.decode(String.self, forKey: .formFactor)
            self.appVersion = try container.decode(String.self, forKey: .appVersion)
            self.appBuild = try container.decodeIfPresent(String.self, forKey: .appBuild) ?? ""
            self.appChannel = try container.decodeIfPresent(String.self, forKey: .appChannel) ?? ""
            self.device = try container.decode(DeviceContextV3.self, forKey: .device)
            self.output = try container.decode(OutputContextV3.self, forKey: .output)
            self.deliveries = try container.decode([String: DeliveryCapabilityV3].self, forKey: .deliveries)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.protocolVersion, forKey: .protocolVersion)
            try container.encode(self.formFactor, forKey: .formFactor)
            try container.encode(self.appVersion, forKey: .appVersion)
            try container.encode(self.appBuild, forKey: .appBuild)
            try container.encode(self.appChannel, forKey: .appChannel)
            try container.encode(self.device, forKey: .device)
            try container.encode(self.output, forKey: .output)
            try container.encode(self.deliveries, forKey: .deliveries)
        }
    }

    /// Wire type `internal/playback.CommandEnvelope`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct CommandEnvelope: Codable, Hashable, Sendable {
        public let type: RealtimeMessageType
        public let commandId: String
        public let sessionId: String
        public let name: CommandName
        public let reason: String
        public let issuedBy: CommandIssuedBy?
        public let deadlineMs: Int
        public let payload: BloemJSONValue?

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case commandId = "command_id"
            case sessionId = "session_id"
            case name = "name"
            case reason = "reason"
            case issuedBy = "issued_by"
            case deadlineMs = "deadline_ms"
            case payload = "payload"
        }

        public init(
            type: RealtimeMessageType = RealtimeMessageType(rawValue: ""),
            commandId: String = "",
            sessionId: String = "",
            name: CommandName = CommandName(rawValue: ""),
            reason: String = "",
            issuedBy: CommandIssuedBy? = nil,
            deadlineMs: Int = 0,
            payload: BloemJSONValue? = nil
        ) {
            self.type = type
            self.commandId = commandId
            self.sessionId = sessionId
            self.name = name
            self.reason = reason
            self.issuedBy = issuedBy
            self.deadlineMs = deadlineMs
            self.payload = payload
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decodeIfPresent(RealtimeMessageType.self, forKey: .type) ?? RealtimeMessageType(rawValue: "")
            self.commandId = try container.decodeIfPresent(String.self, forKey: .commandId) ?? ""
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.name = try container.decodeIfPresent(CommandName.self, forKey: .name) ?? CommandName(rawValue: "")
            self.reason = try container.decodeIfPresent(String.self, forKey: .reason) ?? ""
            self.issuedBy = try container.decodeIfPresent(CommandIssuedBy.self, forKey: .issuedBy)
            self.deadlineMs = try container.decodeIfPresent(Int.self, forKey: .deadlineMs) ?? 0
            self.payload = try container.decodeIfPresent(BloemJSONValue.self, forKey: .payload)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.commandId, forKey: .commandId)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.reason, forKey: .reason)
            try container.encodeIfPresent(self.issuedBy, forKey: .issuedBy)
            try container.encode(self.deadlineMs, forKey: .deadlineMs)
            try container.encodeIfPresent(self.payload, forKey: .payload)
        }
    }

    /// Wire type `internal/playback.CommandIssuedBy`. Direction: response. Dialect: upstream-compat. Gate: realtime.
    public struct CommandIssuedBy: Codable, Hashable, Sendable {
        public let kind: String

        public enum CodingKeys: String, CodingKey {
            case kind = "kind"
        }

        public init(
            kind: String = ""
        ) {
            self.kind = kind
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.kind = try container.decodeIfPresent(String.self, forKey: .kind) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.kind, forKey: .kind)
        }
    }

    /// Wire type `internal/playback.CommandName`. Direction: both. Dialect: upstream-compat. Gate: notifications.remote_control|realtime.
    public struct CommandName: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let PAUSE: CommandName = CommandName(rawValue: "pause")
        public static let UNPAUSE: CommandName = CommandName(rawValue: "unpause")
        public static let PLAY_PAUSE: CommandName = CommandName(rawValue: "play_pause")
        public static let SEEK: CommandName = CommandName(rawValue: "seek")
        public static let SET_VOLUME: CommandName = CommandName(rawValue: "set_volume")
        public static let STOP: CommandName = CommandName(rawValue: "stop")
        public static let TERMINATE: CommandName = CommandName(rawValue: "terminate")
        public static let DISPLAY_MESSAGE: CommandName = CommandName(rawValue: "display_message")
        public static let SERVER_RESTARTING: CommandName = CommandName(rawValue: "server_restarting")
        public static let SERVER_SHUTTING_DOWN: CommandName = CommandName(rawValue: "server_shutting_down")
        public static let PLAY_MEDIA: CommandName = CommandName(rawValue: "play_media")
        public static let SET_AUDIO_TRACK: CommandName = CommandName(rawValue: "set_audio_track")
        public static let SET_SUBTITLE_TRACK: CommandName = CommandName(rawValue: "set_subtitle_track")
        public static let PLAN_INVALIDATED: CommandName = CommandName(rawValue: "plan_invalidated")
        public static let KNOWN: [CommandName] = [PAUSE, UNPAUSE, PLAY_PAUSE, SEEK, SET_VOLUME, STOP, TERMINATE, DISPLAY_MESSAGE, SERVER_RESTARTING, SERVER_SHUTTING_DOWN, PLAY_MEDIA, SET_AUDIO_TRACK, SET_SUBTITLE_TRACK, PLAN_INVALIDATED]
    }

    /// Wire type `internal/playback.DecisionOutcomeV3`. Direction: response. Dialect: upstream-compat.
    public struct DecisionOutcomeV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let PLAYABLE: DecisionOutcomeV3 = DecisionOutcomeV3(rawValue: "playable")
        public static let ADAPTATION_UNAVAILABLE: DecisionOutcomeV3 = DecisionOutcomeV3(rawValue: "adaptation_unavailable")
        public static let KNOWN: [DecisionOutcomeV3] = [PLAYABLE, ADAPTATION_UNAVAILABLE]
    }

    /// Wire type `internal/playback.DecisionResponseV3`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct DecisionResponseV3: Codable, Hashable, Sendable {
        public let protocolVersion: Int
        public let serverFeatures: [String]
        public let negotiatedClientFeatures: [String]
        public let outcome: DecisionOutcomeV3
        public let sessionId: String
        public let playbackPlan: PlanV3?
        public let terminal: TerminalV3?

        public enum CodingKeys: String, CodingKey {
            case protocolVersion = "protocol_version"
            case serverFeatures = "server_features"
            case negotiatedClientFeatures = "negotiated_client_features"
            case outcome = "outcome"
            case sessionId = "session_id"
            case playbackPlan = "playback_plan"
            case terminal = "terminal"
        }

        public init(
            protocolVersion: Int = 0,
            serverFeatures: [String] = [],
            negotiatedClientFeatures: [String] = [],
            outcome: DecisionOutcomeV3 = DecisionOutcomeV3(rawValue: ""),
            sessionId: String = "",
            playbackPlan: PlanV3? = nil,
            terminal: TerminalV3? = nil
        ) {
            self.protocolVersion = protocolVersion
            self.serverFeatures = serverFeatures
            self.negotiatedClientFeatures = negotiatedClientFeatures
            self.outcome = outcome
            self.sessionId = sessionId
            self.playbackPlan = playbackPlan
            self.terminal = terminal
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.protocolVersion = try container.decodeIfPresent(Int.self, forKey: .protocolVersion) ?? 0
            self.serverFeatures = try container.decodeIfPresent([String].self, forKey: .serverFeatures) ?? []
            self.negotiatedClientFeatures = try container.decodeIfPresent([String].self, forKey: .negotiatedClientFeatures) ?? []
            self.outcome = try container.decodeIfPresent(DecisionOutcomeV3.self, forKey: .outcome) ?? DecisionOutcomeV3(rawValue: "")
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.playbackPlan = try container.decodeIfPresent(PlanV3.self, forKey: .playbackPlan)
            self.terminal = try container.decodeIfPresent(TerminalV3.self, forKey: .terminal)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.protocolVersion, forKey: .protocolVersion)
            try container.encode(self.serverFeatures, forKey: .serverFeatures)
            try container.encode(self.negotiatedClientFeatures, forKey: .negotiatedClientFeatures)
            try container.encode(self.outcome, forKey: .outcome)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encodeIfPresent(self.playbackPlan, forKey: .playbackPlan)
            try container.encodeIfPresent(self.terminal, forKey: .terminal)
        }
    }

    /// Wire type `internal/playback.DegradationWarningV3`. Direction: response. Dialect: upstream-compat.
    public struct DegradationWarningV3: Codable, Hashable, Sendable {
        public let code: String
        public let message: String

        public enum CodingKeys: String, CodingKey {
            case code = "code"
            case message = "message"
        }

        public init(
            code: String = "",
            message: String = ""
        ) {
            self.code = code
            self.message = message
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.code = try container.decodeIfPresent(String.self, forKey: .code) ?? ""
            self.message = try container.decodeIfPresent(String.self, forKey: .message) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.code, forKey: .code)
            try container.encode(self.message, forKey: .message)
        }
    }

    /// Wire type `internal/playback.DeliveryCapabilityV3`. Direction: request. Dialect: upstream-compat.
    public struct DeliveryCapabilityV3: Codable, Hashable, Sendable {
        public let enabled: Bool
        public let supportedOnDevice: Bool
        public let failureReason: String
        public let containers: [String]
        public let videoCodecs: [String]
        public let audioDecodeCodecs: [String]
        public let audioPassthroughCodecs: [String]
        public let maxChannels: Int?
        public let hdrDetails: HDRCapabilitiesV3?
        public let subtitles: DeliverySubtitleCapabilitiesV3
        public let features: [String]
        public let authHeaderRefresh: Bool
        public let validatedClaims: [String]
        public let transformations: [TransformationV3]

        public enum CodingKeys: String, CodingKey {
            case enabled = "enabled"
            case supportedOnDevice = "supported_on_device"
            case failureReason = "failure_reason"
            case containers = "containers"
            case videoCodecs = "video_codecs"
            case audioDecodeCodecs = "audio_decode_codecs"
            case audioPassthroughCodecs = "audio_passthrough_codecs"
            case maxChannels = "max_channels"
            case hdrDetails = "hdr_details"
            case subtitles = "subtitles"
            case features = "features"
            case authHeaderRefresh = "auth_header_refresh"
            case validatedClaims = "validated_claims"
            case transformations = "transformations"
        }

        public init(
            enabled: Bool,
            supportedOnDevice: Bool,
            failureReason: String = "",
            containers: [String],
            videoCodecs: [String],
            audioDecodeCodecs: [String],
            audioPassthroughCodecs: [String],
            maxChannels: Int? = nil,
            hdrDetails: HDRCapabilitiesV3? = nil,
            subtitles: DeliverySubtitleCapabilitiesV3,
            features: [String],
            authHeaderRefresh: Bool,
            validatedClaims: [String],
            transformations: [TransformationV3]
        ) {
            self.enabled = enabled
            self.supportedOnDevice = supportedOnDevice
            self.failureReason = failureReason
            self.containers = containers
            self.videoCodecs = videoCodecs
            self.audioDecodeCodecs = audioDecodeCodecs
            self.audioPassthroughCodecs = audioPassthroughCodecs
            self.maxChannels = maxChannels
            self.hdrDetails = hdrDetails
            self.subtitles = subtitles
            self.features = features
            self.authHeaderRefresh = authHeaderRefresh
            self.validatedClaims = validatedClaims
            self.transformations = transformations
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.enabled = try container.decode(Bool.self, forKey: .enabled)
            self.supportedOnDevice = try container.decode(Bool.self, forKey: .supportedOnDevice)
            self.failureReason = try container.decodeIfPresent(String.self, forKey: .failureReason) ?? ""
            self.containers = try container.decode([String].self, forKey: .containers)
            self.videoCodecs = try container.decode([String].self, forKey: .videoCodecs)
            self.audioDecodeCodecs = try container.decode([String].self, forKey: .audioDecodeCodecs)
            self.audioPassthroughCodecs = try container.decode([String].self, forKey: .audioPassthroughCodecs)
            self.maxChannels = try container.decodeIfPresent(Int.self, forKey: .maxChannels)
            self.hdrDetails = try container.decodeIfPresent(HDRCapabilitiesV3.self, forKey: .hdrDetails)
            self.subtitles = try container.decode(DeliverySubtitleCapabilitiesV3.self, forKey: .subtitles)
            self.features = try container.decode([String].self, forKey: .features)
            self.authHeaderRefresh = try container.decode(Bool.self, forKey: .authHeaderRefresh)
            self.validatedClaims = try container.decode([String].self, forKey: .validatedClaims)
            self.transformations = try container.decode([TransformationV3].self, forKey: .transformations)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.enabled, forKey: .enabled)
            try container.encode(self.supportedOnDevice, forKey: .supportedOnDevice)
            try container.encode(self.failureReason, forKey: .failureReason)
            try container.encode(self.containers, forKey: .containers)
            try container.encode(self.videoCodecs, forKey: .videoCodecs)
            try container.encode(self.audioDecodeCodecs, forKey: .audioDecodeCodecs)
            try container.encode(self.audioPassthroughCodecs, forKey: .audioPassthroughCodecs)
            try container.encodeIfPresent(self.maxChannels, forKey: .maxChannels)
            try container.encodeIfPresent(self.hdrDetails, forKey: .hdrDetails)
            try container.encode(self.subtitles, forKey: .subtitles)
            try container.encode(self.features, forKey: .features)
            try container.encode(self.authHeaderRefresh, forKey: .authHeaderRefresh)
            try container.encode(self.validatedClaims, forKey: .validatedClaims)
            try container.encode(self.transformations, forKey: .transformations)
        }
    }

    /// Wire type `internal/playback.DeliverySubtitleCapabilitiesV3`. Direction: request. Dialect: upstream-compat.
    public struct DeliverySubtitleCapabilitiesV3: Codable, Hashable, Sendable {
        public let embeddedText: Bool
        public let sidecarText: Bool
        public let assStyling: Bool
        public let embeddedBitmap: Bool
        public let sidecarBitmap: Bool
        public let fontAttachments: Bool

        public enum CodingKeys: String, CodingKey {
            case embeddedText = "embedded_text"
            case sidecarText = "sidecar_text"
            case assStyling = "ass_styling"
            case embeddedBitmap = "embedded_bitmap"
            case sidecarBitmap = "sidecar_bitmap"
            case fontAttachments = "font_attachments"
        }

        public init(
            embeddedText: Bool,
            sidecarText: Bool,
            assStyling: Bool,
            embeddedBitmap: Bool,
            sidecarBitmap: Bool,
            fontAttachments: Bool
        ) {
            self.embeddedText = embeddedText
            self.sidecarText = sidecarText
            self.assStyling = assStyling
            self.embeddedBitmap = embeddedBitmap
            self.sidecarBitmap = sidecarBitmap
            self.fontAttachments = fontAttachments
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.embeddedText = try container.decode(Bool.self, forKey: .embeddedText)
            self.sidecarText = try container.decode(Bool.self, forKey: .sidecarText)
            self.assStyling = try container.decode(Bool.self, forKey: .assStyling)
            self.embeddedBitmap = try container.decode(Bool.self, forKey: .embeddedBitmap)
            self.sidecarBitmap = try container.decode(Bool.self, forKey: .sidecarBitmap)
            self.fontAttachments = try container.decode(Bool.self, forKey: .fontAttachments)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.embeddedText, forKey: .embeddedText)
            try container.encode(self.sidecarText, forKey: .sidecarText)
            try container.encode(self.assStyling, forKey: .assStyling)
            try container.encode(self.embeddedBitmap, forKey: .embeddedBitmap)
            try container.encode(self.sidecarBitmap, forKey: .sidecarBitmap)
            try container.encode(self.fontAttachments, forKey: .fontAttachments)
        }
    }

    /// Wire type `internal/playback.DeliveryV3`. Direction: response. Dialect: upstream-compat.
    public struct DeliveryV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let ORIGINAL_HTTP: DeliveryV3 = DeliveryV3(rawValue: "original_http")
        public static let REMUX_HLS: DeliveryV3 = DeliveryV3(rawValue: "server_remux_hls")
        public static let REMUX_PROGRESSIVE: DeliveryV3 = DeliveryV3(rawValue: "server_remux_progressive")
        public static let TRANSCODE_HLS: DeliveryV3 = DeliveryV3(rawValue: "server_transcode_hls")
        public static let KNOWN: [DeliveryV3] = [ORIGINAL_HTTP, REMUX_HLS, REMUX_PROGRESSIVE, TRANSCODE_HLS]
    }

    /// Wire type `internal/playback.DeviceContextV3`. Direction: request. Dialect: upstream-compat.
    public struct DeviceContextV3: Codable, Hashable, Sendable {
        public let platform: String
        public let osVersion: String
        public let manufacturer: String
        public let model: String
        public let platformDetails: [String: String]

        public enum CodingKeys: String, CodingKey {
            case platform = "platform"
            case osVersion = "os_version"
            case manufacturer = "manufacturer"
            case model = "model"
            case platformDetails = "platform_details"
        }

        public init(
            platform: String = "",
            osVersion: String = "",
            manufacturer: String = "",
            model: String = "",
            platformDetails: [String: String] = [:]
        ) {
            self.platform = platform
            self.osVersion = osVersion
            self.manufacturer = manufacturer
            self.model = model
            self.platformDetails = platformDetails
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.platform = try container.decodeIfPresent(String.self, forKey: .platform) ?? ""
            self.osVersion = try container.decodeIfPresent(String.self, forKey: .osVersion) ?? ""
            self.manufacturer = try container.decodeIfPresent(String.self, forKey: .manufacturer) ?? ""
            self.model = try container.decodeIfPresent(String.self, forKey: .model) ?? ""
            self.platformDetails = try container.decodeIfPresent([String: String].self, forKey: .platformDetails) ?? [:]
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.platform, forKey: .platform)
            try container.encode(self.osVersion, forKey: .osVersion)
            try container.encode(self.manufacturer, forKey: .manufacturer)
            try container.encode(self.model, forKey: .model)
            try container.encode(self.platformDetails, forKey: .platformDetails)
        }
    }

    /// Wire type `internal/playback.DolbyVisionProfileCapabilityV3`. Direction: request. Dialect: upstream-compat.
    public struct DolbyVisionProfileCapabilityV3: Codable, Hashable, Sendable {
        public let profile: Int
        public let maxLevel: Int
        public let blCompatibilityIds: [Int]

        public enum CodingKeys: String, CodingKey {
            case profile = "profile"
            case maxLevel = "max_level"
            case blCompatibilityIds = "bl_compatibility_ids"
        }

        public init(
            profile: Int,
            maxLevel: Int,
            blCompatibilityIds: [Int] = []
        ) {
            self.profile = profile
            self.maxLevel = maxLevel
            self.blCompatibilityIds = blCompatibilityIds
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.profile = try container.decode(Int.self, forKey: .profile)
            self.maxLevel = try container.decode(Int.self, forKey: .maxLevel)
            self.blCompatibilityIds = try container.decodeIfPresent([Int].self, forKey: .blCompatibilityIds) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.profile, forKey: .profile)
            try container.encode(self.maxLevel, forKey: .maxLevel)
            try container.encode(self.blCompatibilityIds, forKey: .blCompatibilityIds)
        }
    }

    /// Wire type `internal/playback.EffectiveRecipeV3`. Direction: response. Dialect: upstream-compat.
    public struct EffectiveRecipeV3: Codable, Hashable, Sendable {
        public let videoCodec: String
        public let videoSampleEntry: String
        public let audioCodec: String
        public let width: Int?
        public let height: Int?
        public let frameRate: Double?
        public let bitrateKbps: Int?
        public let dynamicRange: String
        public let audioChannels: Int?
        public let audioLayout: String

        public enum CodingKeys: String, CodingKey {
            case videoCodec = "video_codec"
            case videoSampleEntry = "video_sample_entry"
            case audioCodec = "audio_codec"
            case width = "width"
            case height = "height"
            case frameRate = "frame_rate"
            case bitrateKbps = "bitrate_kbps"
            case dynamicRange = "dynamic_range"
            case audioChannels = "audio_channels"
            case audioLayout = "audio_layout"
        }

        public init(
            videoCodec: String = "",
            videoSampleEntry: String = "",
            audioCodec: String = "",
            width: Int? = nil,
            height: Int? = nil,
            frameRate: Double? = nil,
            bitrateKbps: Int? = nil,
            dynamicRange: String = "",
            audioChannels: Int? = nil,
            audioLayout: String = ""
        ) {
            self.videoCodec = videoCodec
            self.videoSampleEntry = videoSampleEntry
            self.audioCodec = audioCodec
            self.width = width
            self.height = height
            self.frameRate = frameRate
            self.bitrateKbps = bitrateKbps
            self.dynamicRange = dynamicRange
            self.audioChannels = audioChannels
            self.audioLayout = audioLayout
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.videoCodec = try container.decodeIfPresent(String.self, forKey: .videoCodec) ?? ""
            self.videoSampleEntry = try container.decodeIfPresent(String.self, forKey: .videoSampleEntry) ?? ""
            self.audioCodec = try container.decodeIfPresent(String.self, forKey: .audioCodec) ?? ""
            self.width = try container.decodeIfPresent(Int.self, forKey: .width)
            self.height = try container.decodeIfPresent(Int.self, forKey: .height)
            self.frameRate = try container.decodeIfPresent(Double.self, forKey: .frameRate)
            self.bitrateKbps = try container.decodeIfPresent(Int.self, forKey: .bitrateKbps)
            self.dynamicRange = try container.decodeIfPresent(String.self, forKey: .dynamicRange) ?? ""
            self.audioChannels = try container.decodeIfPresent(Int.self, forKey: .audioChannels)
            self.audioLayout = try container.decodeIfPresent(String.self, forKey: .audioLayout) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.videoCodec, forKey: .videoCodec)
            try container.encode(self.videoSampleEntry, forKey: .videoSampleEntry)
            try container.encode(self.audioCodec, forKey: .audioCodec)
            try container.encodeIfPresent(self.width, forKey: .width)
            try container.encodeIfPresent(self.height, forKey: .height)
            try container.encodeIfPresent(self.frameRate, forKey: .frameRate)
            try container.encodeIfPresent(self.bitrateKbps, forKey: .bitrateKbps)
            try container.encode(self.dynamicRange, forKey: .dynamicRange)
            try container.encodeIfPresent(self.audioChannels, forKey: .audioChannels)
            try container.encode(self.audioLayout, forKey: .audioLayout)
        }
    }

    /// Wire type `internal/playback.EnhancementLayerV3`. Direction: response. Dialect: upstream-compat.
    public struct EnhancementLayerV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let NONE: EnhancementLayerV3 = EnhancementLayerV3(rawValue: "none")
        public static let MEL: EnhancementLayerV3 = EnhancementLayerV3(rawValue: "mel")
        public static let FEL: EnhancementLayerV3 = EnhancementLayerV3(rawValue: "fel")
        public static let UNKNOWN: EnhancementLayerV3 = EnhancementLayerV3(rawValue: "unknown")
        public static let KNOWN: [EnhancementLayerV3] = [NONE, MEL, FEL, UNKNOWN]
    }

    /// Wire type `internal/playback.ErrorResponseV3`. Direction: response. Dialect: upstream-compat. Registered root.
    public struct ErrorResponseV3: Codable, Hashable, Sendable {
        public let error: String
        public let message: String

        public enum CodingKeys: String, CodingKey {
            case error = "error"
            case message = "message"
        }

        public init(
            error: String = "",
            message: String = ""
        ) {
            self.error = error
            self.message = message
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.error = try container.decodeIfPresent(String.self, forKey: .error) ?? ""
            self.message = try container.decodeIfPresent(String.self, forKey: .message) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.error, forKey: .error)
            try container.encode(self.message, forKey: .message)
        }
    }

    /// Wire type `internal/playback.EventEnvelope`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct EventEnvelope: Codable, Hashable, Sendable {
        public let type: RealtimeMessageType
        public let sessionId: String
        public let name: RealtimeEventName
        public let payload: BloemJSONValue?

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case sessionId = "session_id"
            case name = "name"
            case payload = "payload"
        }

        public init(
            type: RealtimeMessageType = RealtimeMessageType(rawValue: ""),
            sessionId: String = "",
            name: RealtimeEventName = RealtimeEventName(rawValue: ""),
            payload: BloemJSONValue? = nil
        ) {
            self.type = type
            self.sessionId = sessionId
            self.name = name
            self.payload = payload
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decodeIfPresent(RealtimeMessageType.self, forKey: .type) ?? RealtimeMessageType(rawValue: "")
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.name = try container.decodeIfPresent(RealtimeEventName.self, forKey: .name) ?? RealtimeEventName(rawValue: "")
            self.payload = try container.decodeIfPresent(BloemJSONValue.self, forKey: .payload)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.name, forKey: .name)
            try container.encodeIfPresent(self.payload, forKey: .payload)
        }
    }

    /// Wire type `internal/playback.FailureV3`. Direction: request. Dialect: upstream-compat.
    public struct FailureV3: Codable, Hashable, Sendable {
        public let classification: String
        public let message: String
        public let decoderName: String

        public enum CodingKeys: String, CodingKey {
            case classification = "classification"
            case message = "message"
            case decoderName = "decoder_name"
        }

        public init(
            classification: String,
            message: String = "",
            decoderName: String = ""
        ) {
            self.classification = classification
            self.message = message
            self.decoderName = decoderName
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.classification = try container.decode(String.self, forKey: .classification)
            self.message = try container.decodeIfPresent(String.self, forKey: .message) ?? ""
            self.decoderName = try container.decodeIfPresent(String.self, forKey: .decoderName) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.classification, forKey: .classification)
            try container.encode(self.message, forKey: .message)
            try container.encode(self.decoderName, forKey: .decoderName)
        }
    }

    /// Wire type `internal/playback.HDRCapabilitiesV3`. Direction: request. Dialect: upstream-compat.
    public struct HDRCapabilitiesV3: Codable, Hashable, Sendable {
        public let hdr10: Bool
        public let hdr10Plus: Bool
        public let hlg: Bool
        public let hdr10MaxWidth: Int
        public let hdr10MaxHeight: Int
        public let hdr10MaxFrameRate: Double
        public let hdr10MaxBitrateKbps: Int
        public let dolbyVisionProfiles: [Int]
        public let dolbyVisionProfileLevels: [DolbyVisionProfileCapabilityV3]

        public enum CodingKeys: String, CodingKey {
            case hdr10 = "hdr10"
            case hdr10Plus = "hdr10_plus"
            case hlg = "hlg"
            case hdr10MaxWidth = "hdr10_max_width"
            case hdr10MaxHeight = "hdr10_max_height"
            case hdr10MaxFrameRate = "hdr10_max_frame_rate"
            case hdr10MaxBitrateKbps = "hdr10_max_bitrate_kbps"
            case dolbyVisionProfiles = "dolby_vision_profiles"
            case dolbyVisionProfileLevels = "dolby_vision_profile_levels"
        }

        public init(
            hdr10: Bool,
            hdr10Plus: Bool,
            hlg: Bool,
            hdr10MaxWidth: Int = 0,
            hdr10MaxHeight: Int = 0,
            hdr10MaxFrameRate: Double = 0.0,
            hdr10MaxBitrateKbps: Int = 0,
            dolbyVisionProfiles: [Int],
            dolbyVisionProfileLevels: [DolbyVisionProfileCapabilityV3] = []
        ) {
            self.hdr10 = hdr10
            self.hdr10Plus = hdr10Plus
            self.hlg = hlg
            self.hdr10MaxWidth = hdr10MaxWidth
            self.hdr10MaxHeight = hdr10MaxHeight
            self.hdr10MaxFrameRate = hdr10MaxFrameRate
            self.hdr10MaxBitrateKbps = hdr10MaxBitrateKbps
            self.dolbyVisionProfiles = dolbyVisionProfiles
            self.dolbyVisionProfileLevels = dolbyVisionProfileLevels
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.hdr10 = try container.decode(Bool.self, forKey: .hdr10)
            self.hdr10Plus = try container.decode(Bool.self, forKey: .hdr10Plus)
            self.hlg = try container.decode(Bool.self, forKey: .hlg)
            self.hdr10MaxWidth = try container.decodeIfPresent(Int.self, forKey: .hdr10MaxWidth) ?? 0
            self.hdr10MaxHeight = try container.decodeIfPresent(Int.self, forKey: .hdr10MaxHeight) ?? 0
            self.hdr10MaxFrameRate = try container.decodeIfPresent(Double.self, forKey: .hdr10MaxFrameRate) ?? 0.0
            self.hdr10MaxBitrateKbps = try container.decodeIfPresent(Int.self, forKey: .hdr10MaxBitrateKbps) ?? 0
            self.dolbyVisionProfiles = try container.decode([Int].self, forKey: .dolbyVisionProfiles)
            self.dolbyVisionProfileLevels = try container.decodeIfPresent([DolbyVisionProfileCapabilityV3].self, forKey: .dolbyVisionProfileLevels) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.hdr10, forKey: .hdr10)
            try container.encode(self.hdr10Plus, forKey: .hdr10Plus)
            try container.encode(self.hlg, forKey: .hlg)
            try container.encode(self.hdr10MaxWidth, forKey: .hdr10MaxWidth)
            try container.encode(self.hdr10MaxHeight, forKey: .hdr10MaxHeight)
            try container.encode(self.hdr10MaxFrameRate, forKey: .hdr10MaxFrameRate)
            try container.encode(self.hdr10MaxBitrateKbps, forKey: .hdr10MaxBitrateKbps)
            try container.encode(self.dolbyVisionProfiles, forKey: .dolbyVisionProfiles)
            try container.encode(self.dolbyVisionProfileLevels, forKey: .dolbyVisionProfileLevels)
        }
    }

    /// Wire type `internal/playback.HeaderRefreshModeV3`. Direction: response. Dialect: upstream-compat.
    public struct HeaderRefreshModeV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let NONE: HeaderRefreshModeV3 = HeaderRefreshModeV3(rawValue: "none")
        public static let SESSION: HeaderRefreshModeV3 = HeaderRefreshModeV3(rawValue: "session")
        public static let KNOWN: [HeaderRefreshModeV3] = [NONE, SESSION]
    }

    /// Wire type `internal/playback.HelloCapabilities`. Direction: request. Dialect: upstream-compat. Gate: realtime.
    public struct HelloCapabilities: Codable, Hashable, Sendable {
        public let commands: [CommandName]

        public enum CodingKeys: String, CodingKey {
            case commands = "commands"
        }

        public init(
            commands: [CommandName]
        ) {
            self.commands = commands
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.commands = try container.decode([CommandName].self, forKey: .commands)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.commands, forKey: .commands)
        }
    }

    /// Wire type `internal/playback.HelloClientInfo`. Direction: request. Dialect: upstream-compat. Gate: realtime.
    public struct HelloClientInfo: Codable, Hashable, Sendable {
        public let name: String
        public let version: String

        public enum CodingKeys: String, CodingKey {
            case name = "name"
            case version = "version"
        }

        public init(
            name: String,
            version: String
        ) {
            self.name = name
            self.version = version
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decode(String.self, forKey: .name)
            self.version = try container.decode(String.self, forKey: .version)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.version, forKey: .version)
        }
    }

    /// Wire type `internal/playback.HelloEnvelope`. Direction: request. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct HelloEnvelope: Codable, Hashable, Sendable {
        public let type: RealtimeMessageType
        public let sessionId: String
        public let client: HelloClientInfo
        public let capabilities: HelloCapabilities

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case sessionId = "session_id"
            case client = "client"
            case capabilities = "capabilities"
        }

        public init(
            type: RealtimeMessageType,
            sessionId: String,
            client: HelloClientInfo,
            capabilities: HelloCapabilities
        ) {
            self.type = type
            self.sessionId = sessionId
            self.client = client
            self.capabilities = capabilities
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decode(RealtimeMessageType.self, forKey: .type)
            self.sessionId = try container.decode(String.self, forKey: .sessionId)
            self.client = try container.decode(HelloClientInfo.self, forKey: .client)
            self.capabilities = try container.decode(HelloCapabilities.self, forKey: .capabilities)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.client, forKey: .client)
            try container.encode(self.capabilities, forKey: .capabilities)
        }
    }

    /// Wire type `internal/playback.MarkersUpdatedPayload`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct MarkersUpdatedPayload: Codable, Hashable, Sendable {
        public let sessionId: String
        public let fileId: Int
        public let intro: TimeRangePayload?
        public let credits: TimeRangePayload?
        public let recap: TimeRangePayload?
        public let preview: TimeRangePayload?

        public enum CodingKeys: String, CodingKey {
            case sessionId = "session_id"
            case fileId = "file_id"
            case intro = "intro"
            case credits = "credits"
            case recap = "recap"
            case preview = "preview"
        }

        public init(
            sessionId: String = "",
            fileId: Int = 0,
            intro: TimeRangePayload? = nil,
            credits: TimeRangePayload? = nil,
            recap: TimeRangePayload? = nil,
            preview: TimeRangePayload? = nil
        ) {
            self.sessionId = sessionId
            self.fileId = fileId
            self.intro = intro
            self.credits = credits
            self.recap = recap
            self.preview = preview
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.fileId = try container.decodeIfPresent(Int.self, forKey: .fileId) ?? 0
            self.intro = try container.decodeIfPresent(TimeRangePayload.self, forKey: .intro)
            self.credits = try container.decodeIfPresent(TimeRangePayload.self, forKey: .credits)
            self.recap = try container.decodeIfPresent(TimeRangePayload.self, forKey: .recap)
            self.preview = try container.decodeIfPresent(TimeRangePayload.self, forKey: .preview)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encodeIfPresent(self.intro, forKey: .intro)
            try container.encodeIfPresent(self.credits, forKey: .credits)
            try container.encodeIfPresent(self.recap, forKey: .recap)
            try container.encodeIfPresent(self.preview, forKey: .preview)
        }
    }

    /// Wire type `internal/playback.OutputContextV3`. Direction: request. Dialect: upstream-compat.
    public struct OutputContextV3: Codable, Hashable, Sendable {
        public let hdrDetails: HDRCapabilitiesV3?
        public let audioPassthrough: AudioPassthroughV3?
        public let currentSink: String
        public let sinkType: String
        public let display: OutputDisplayV3?
        public let outputContextId: String

        public enum CodingKeys: String, CodingKey {
            case hdrDetails = "hdr_details"
            case audioPassthrough = "audio_passthrough"
            case currentSink = "current_sink"
            case sinkType = "sink_type"
            case display = "display"
            case outputContextId = "output_context_id"
        }

        public init(
            hdrDetails: HDRCapabilitiesV3? = nil,
            audioPassthrough: AudioPassthroughV3? = nil,
            currentSink: String = "",
            sinkType: String = "",
            display: OutputDisplayV3? = nil,
            outputContextId: String = ""
        ) {
            self.hdrDetails = hdrDetails
            self.audioPassthrough = audioPassthrough
            self.currentSink = currentSink
            self.sinkType = sinkType
            self.display = display
            self.outputContextId = outputContextId
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.hdrDetails = try container.decodeIfPresent(HDRCapabilitiesV3.self, forKey: .hdrDetails)
            self.audioPassthrough = try container.decodeIfPresent(AudioPassthroughV3.self, forKey: .audioPassthrough)
            self.currentSink = try container.decodeIfPresent(String.self, forKey: .currentSink) ?? ""
            self.sinkType = try container.decodeIfPresent(String.self, forKey: .sinkType) ?? ""
            self.display = try container.decodeIfPresent(OutputDisplayV3.self, forKey: .display)
            self.outputContextId = try container.decodeIfPresent(String.self, forKey: .outputContextId) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encodeIfPresent(self.hdrDetails, forKey: .hdrDetails)
            try container.encodeIfPresent(self.audioPassthrough, forKey: .audioPassthrough)
            try container.encode(self.currentSink, forKey: .currentSink)
            try container.encode(self.sinkType, forKey: .sinkType)
            try container.encodeIfPresent(self.display, forKey: .display)
            try container.encode(self.outputContextId, forKey: .outputContextId)
        }
    }

    /// Wire type `internal/playback.OutputDisplayV3`. Direction: request. Dialect: upstream-compat.
    public struct OutputDisplayV3: Codable, Hashable, Sendable {
        public let hdrEvidence: String
        public let hdrTypes: HDRCapabilitiesV3?
        public let displayId: String

        public enum CodingKeys: String, CodingKey {
            case hdrEvidence = "hdr_evidence"
            case hdrTypes = "hdr_types"
            case displayId = "display_id"
        }

        public init(
            hdrEvidence: String,
            hdrTypes: HDRCapabilitiesV3? = nil,
            displayId: String = ""
        ) {
            self.hdrEvidence = hdrEvidence
            self.hdrTypes = hdrTypes
            self.displayId = displayId
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.hdrEvidence = try container.decode(String.self, forKey: .hdrEvidence)
            self.hdrTypes = try container.decodeIfPresent(HDRCapabilitiesV3.self, forKey: .hdrTypes)
            self.displayId = try container.decodeIfPresent(String.self, forKey: .displayId) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.hdrEvidence, forKey: .hdrEvidence)
            try container.encodeIfPresent(self.hdrTypes, forKey: .hdrTypes)
            try container.encode(self.displayId, forKey: .displayId)
        }
    }

    /// Wire type `internal/playback.PlanInvalidatedPayload`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct PlanInvalidatedPayload: Codable, Hashable, Sendable {
        public let reason: String
        public let planId: String

        public enum CodingKeys: String, CodingKey {
            case reason = "reason"
            case planId = "plan_id"
        }

        public init(
            reason: String = "",
            planId: String = ""
        ) {
            self.reason = reason
            self.planId = planId
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.reason = try container.decodeIfPresent(String.self, forKey: .reason) ?? ""
            self.planId = try container.decodeIfPresent(String.self, forKey: .planId) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.reason, forKey: .reason)
            try container.encode(self.planId, forKey: .planId)
        }
    }

    /// Wire type `internal/playback.PlanV3`. Direction: response. Dialect: upstream-compat.
    public struct PlanV3: Codable, Hashable, Sendable {
        public let protocolVersion: Int
        public let planId: String
        public let planAttemptKey: String
        public let sessionId: String
        public let expiresAt: String
        public let delivery: DeliveryV3
        public let stream: StreamV3
        public let timeline: TimelineV3
        public let selectedTracks: SelectedTracksV3
        public let effectiveRecipe: EffectiveRecipeV3
        public let claims: ValidationClaimsV3
        public let subtitle: SubtitleDecisionV3
        public let transformations: [TransformationV3]
        public let appliedQuirks: [AppliedQuirkV3]
        public let runtimeCorrections: [String]
        public let availableQualities: [AvailableQualityV3]
        public let degradationWarnings: [DegradationWarningV3]
        public let decisionReason: String
        public let requestedMediaFileId: Int
        public let effectiveMediaFileId: Int
        public let source: SourceDescriptorV3
        public let subtitleFidelityPolicy: String

        public enum CodingKeys: String, CodingKey {
            case protocolVersion = "protocol_version"
            case planId = "plan_id"
            case planAttemptKey = "plan_attempt_key"
            case sessionId = "session_id"
            case expiresAt = "expires_at"
            case delivery = "delivery"
            case stream = "stream"
            case timeline = "timeline"
            case selectedTracks = "selected_tracks"
            case effectiveRecipe = "effective_recipe"
            case claims = "claims"
            case subtitle = "subtitle"
            case transformations = "transformations"
            case appliedQuirks = "applied_quirks"
            case runtimeCorrections = "runtime_corrections"
            case availableQualities = "available_qualities"
            case degradationWarnings = "degradation_warnings"
            case decisionReason = "decision_reason"
            case requestedMediaFileId = "requested_media_file_id"
            case effectiveMediaFileId = "effective_media_file_id"
            case source = "source"
            case subtitleFidelityPolicy = "subtitle_fidelity_policy"
        }

        public init(
            protocolVersion: Int = 0,
            planId: String = "",
            planAttemptKey: String = "",
            sessionId: String = "",
            expiresAt: String = "",
            delivery: DeliveryV3 = DeliveryV3(rawValue: ""),
            stream: StreamV3 = StreamV3(),
            timeline: TimelineV3 = TimelineV3(),
            selectedTracks: SelectedTracksV3 = SelectedTracksV3(),
            effectiveRecipe: EffectiveRecipeV3 = EffectiveRecipeV3(),
            claims: ValidationClaimsV3 = ValidationClaimsV3(),
            subtitle: SubtitleDecisionV3 = SubtitleDecisionV3(),
            transformations: [TransformationV3] = [],
            appliedQuirks: [AppliedQuirkV3] = [],
            runtimeCorrections: [String] = [],
            availableQualities: [AvailableQualityV3] = [],
            degradationWarnings: [DegradationWarningV3] = [],
            decisionReason: String = "",
            requestedMediaFileId: Int = 0,
            effectiveMediaFileId: Int = 0,
            source: SourceDescriptorV3 = SourceDescriptorV3(),
            subtitleFidelityPolicy: String = ""
        ) {
            self.protocolVersion = protocolVersion
            self.planId = planId
            self.planAttemptKey = planAttemptKey
            self.sessionId = sessionId
            self.expiresAt = expiresAt
            self.delivery = delivery
            self.stream = stream
            self.timeline = timeline
            self.selectedTracks = selectedTracks
            self.effectiveRecipe = effectiveRecipe
            self.claims = claims
            self.subtitle = subtitle
            self.transformations = transformations
            self.appliedQuirks = appliedQuirks
            self.runtimeCorrections = runtimeCorrections
            self.availableQualities = availableQualities
            self.degradationWarnings = degradationWarnings
            self.decisionReason = decisionReason
            self.requestedMediaFileId = requestedMediaFileId
            self.effectiveMediaFileId = effectiveMediaFileId
            self.source = source
            self.subtitleFidelityPolicy = subtitleFidelityPolicy
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.protocolVersion = try container.decodeIfPresent(Int.self, forKey: .protocolVersion) ?? 0
            self.planId = try container.decodeIfPresent(String.self, forKey: .planId) ?? ""
            self.planAttemptKey = try container.decodeIfPresent(String.self, forKey: .planAttemptKey) ?? ""
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.expiresAt = try container.decodeIfPresent(String.self, forKey: .expiresAt) ?? ""
            self.delivery = try container.decodeIfPresent(DeliveryV3.self, forKey: .delivery) ?? DeliveryV3(rawValue: "")
            self.stream = try container.decodeIfPresent(StreamV3.self, forKey: .stream) ?? StreamV3()
            self.timeline = try container.decodeIfPresent(TimelineV3.self, forKey: .timeline) ?? TimelineV3()
            self.selectedTracks = try container.decodeIfPresent(SelectedTracksV3.self, forKey: .selectedTracks) ?? SelectedTracksV3()
            self.effectiveRecipe = try container.decodeIfPresent(EffectiveRecipeV3.self, forKey: .effectiveRecipe) ?? EffectiveRecipeV3()
            self.claims = try container.decodeIfPresent(ValidationClaimsV3.self, forKey: .claims) ?? ValidationClaimsV3()
            self.subtitle = try container.decodeIfPresent(SubtitleDecisionV3.self, forKey: .subtitle) ?? SubtitleDecisionV3()
            self.transformations = try container.decodeIfPresent([TransformationV3].self, forKey: .transformations) ?? []
            self.appliedQuirks = try container.decodeIfPresent([AppliedQuirkV3].self, forKey: .appliedQuirks) ?? []
            self.runtimeCorrections = try container.decodeIfPresent([String].self, forKey: .runtimeCorrections) ?? []
            self.availableQualities = try container.decodeIfPresent([AvailableQualityV3].self, forKey: .availableQualities) ?? []
            self.degradationWarnings = try container.decodeIfPresent([DegradationWarningV3].self, forKey: .degradationWarnings) ?? []
            self.decisionReason = try container.decodeIfPresent(String.self, forKey: .decisionReason) ?? ""
            self.requestedMediaFileId = try container.decodeIfPresent(Int.self, forKey: .requestedMediaFileId) ?? 0
            self.effectiveMediaFileId = try container.decodeIfPresent(Int.self, forKey: .effectiveMediaFileId) ?? 0
            self.source = try container.decodeIfPresent(SourceDescriptorV3.self, forKey: .source) ?? SourceDescriptorV3()
            self.subtitleFidelityPolicy = try container.decodeIfPresent(String.self, forKey: .subtitleFidelityPolicy) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.protocolVersion, forKey: .protocolVersion)
            try container.encode(self.planId, forKey: .planId)
            try container.encode(self.planAttemptKey, forKey: .planAttemptKey)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.expiresAt, forKey: .expiresAt)
            try container.encode(self.delivery, forKey: .delivery)
            try container.encode(self.stream, forKey: .stream)
            try container.encode(self.timeline, forKey: .timeline)
            try container.encode(self.selectedTracks, forKey: .selectedTracks)
            try container.encode(self.effectiveRecipe, forKey: .effectiveRecipe)
            try container.encode(self.claims, forKey: .claims)
            try container.encode(self.subtitle, forKey: .subtitle)
            try container.encode(self.transformations, forKey: .transformations)
            try container.encode(self.appliedQuirks, forKey: .appliedQuirks)
            try container.encode(self.runtimeCorrections, forKey: .runtimeCorrections)
            try container.encode(self.availableQualities, forKey: .availableQualities)
            try container.encode(self.degradationWarnings, forKey: .degradationWarnings)
            try container.encode(self.decisionReason, forKey: .decisionReason)
            try container.encode(self.requestedMediaFileId, forKey: .requestedMediaFileId)
            try container.encode(self.effectiveMediaFileId, forKey: .effectiveMediaFileId)
            try container.encode(self.source, forKey: .source)
            try container.encode(self.subtitleFidelityPolicy, forKey: .subtitleFidelityPolicy)
        }
    }

    /// Wire type `internal/playback.ProgressPersistenceV3`. Direction: request. Dialect: upstream-compat.
    public struct ProgressPersistenceV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let SERVER: ProgressPersistenceV3 = ProgressPersistenceV3(rawValue: "server")
        public static let CLIENT: ProgressPersistenceV3 = ProgressPersistenceV3(rawValue: "client")
        public static let KNOWN: [ProgressPersistenceV3] = [SERVER, CLIENT]
    }

    /// Wire type `internal/playback.RealtimeAckStatus`. Direction: request. Dialect: upstream-compat. Gate: realtime.
    public struct RealtimeAckStatus: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let ACCEPTED: RealtimeAckStatus = RealtimeAckStatus(rawValue: "accepted")
        public static let KNOWN: [RealtimeAckStatus] = [ACCEPTED]
    }

    /// Wire type `internal/playback.RealtimeEventName`. Direction: response. Dialect: upstream-compat. Gate: realtime.
    public struct RealtimeEventName: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let CHAPTER_THUMBNAIL_READY: RealtimeEventName = RealtimeEventName(rawValue: "chapter_thumbnail_ready")
        public static let MARKERS_UPDATED: RealtimeEventName = RealtimeEventName(rawValue: "markers_updated")
        public static let SUBTITLE_READY: RealtimeEventName = RealtimeEventName(rawValue: "subtitle_ready")
        public static let SUBTITLE_TRANSLATION_START: RealtimeEventName = RealtimeEventName(rawValue: "subtitle_translation_started")
        public static let SUBTITLE_TRANSLATION_CUES: RealtimeEventName = RealtimeEventName(rawValue: "subtitle_translation_cues")
        public static let SUBTITLE_TRANSLATION_DONE: RealtimeEventName = RealtimeEventName(rawValue: "subtitle_translation_completed")
        public static let SUBTITLE_TRANSLATION_FAIL: RealtimeEventName = RealtimeEventName(rawValue: "subtitle_translation_failed")
        public static let KNOWN: [RealtimeEventName] = [CHAPTER_THUMBNAIL_READY, MARKERS_UPDATED, SUBTITLE_READY, SUBTITLE_TRANSLATION_START, SUBTITLE_TRANSLATION_CUES, SUBTITLE_TRANSLATION_DONE, SUBTITLE_TRANSLATION_FAIL]
    }

    /// Wire type `internal/playback.RealtimeMessageType`. Direction: both. Dialect: upstream-compat. Gate: realtime.
    public struct RealtimeMessageType: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let COMMAND: RealtimeMessageType = RealtimeMessageType(rawValue: "command")
        public static let EVENT: RealtimeMessageType = RealtimeMessageType(rawValue: "event")
        public static let HELLO: RealtimeMessageType = RealtimeMessageType(rawValue: "hello")
        public static let ACK: RealtimeMessageType = RealtimeMessageType(rawValue: "ack")
        public static let RESULT: RealtimeMessageType = RealtimeMessageType(rawValue: "result")
        public static let KNOWN: [RealtimeMessageType] = [COMMAND, EVENT, HELLO, ACK, RESULT]
    }

    /// Wire type `internal/playback.RealtimeResultStatus`. Direction: request. Dialect: upstream-compat. Gate: realtime.
    public struct RealtimeResultStatus: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let COMPLETED: RealtimeResultStatus = RealtimeResultStatus(rawValue: "completed")
        public static let REJECTED: RealtimeResultStatus = RealtimeResultStatus(rawValue: "rejected")
        public static let KNOWN: [RealtimeResultStatus] = [COMPLETED, REJECTED]
    }

    /// Wire type `internal/playback.ReplanOperationV3`. Direction: request. Dialect: upstream-compat.
    public struct ReplanOperationV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let FAILURE_RECOVERY: ReplanOperationV3 = ReplanOperationV3(rawValue: "failure_recovery")
        public static let SEEK_REANCHOR: ReplanOperationV3 = ReplanOperationV3(rawValue: "seek_reanchor")
        public static let SEEK_FAILURE_RECOVERY: ReplanOperationV3 = ReplanOperationV3(rawValue: "seek_failure_recovery")
        public static let TRACK_CHANGE: ReplanOperationV3 = ReplanOperationV3(rawValue: "track_change")
        public static let QUALITY_CHANGE: ReplanOperationV3 = ReplanOperationV3(rawValue: "quality_change")
        public static let OUTPUT_CHANGE: ReplanOperationV3 = ReplanOperationV3(rawValue: "output_change")
        public static let KNOWN: [ReplanOperationV3] = [FAILURE_RECOVERY, SEEK_REANCHOR, SEEK_FAILURE_RECOVERY, TRACK_CHANGE, QUALITY_CHANGE, OUTPUT_CHANGE]
    }

    /// Wire type `internal/playback.ReplanRequestV3`. Direction: request. Dialect: upstream-compat. Registered root.
    public struct ReplanRequestV3: Codable, Hashable, Sendable {
        public let protocolVersion: Int
        public let clientFeatures: [String]
        public let operation: ReplanOperationV3
        public let playbackAttemptId: String
        public let replanRequestId: String
        public let failedPlanId: String
        public let planAttemptId: String
        public let planAttemptKey: String
        public let attemptedPlanKeys: [String]
        public let localMutations: [String]
        public let attemptCount: Int
        public let qualityPreference: String
        public let positionSeconds: Double
        public let metered: Bool
        public let bandwidthEstimateKbps: Int?
        public let bandwidthCapKbps: Int?
        public let selectedTracks: SelectedTracksV3
        public let failure: FailureV3?
        public let capabilities: ClientCodecCapabilitiesV3
        public let clientPlaybackContext: ClientPlaybackContextV3

        public enum CodingKeys: String, CodingKey {
            case protocolVersion = "protocol_version"
            case clientFeatures = "client_features"
            case operation = "operation"
            case playbackAttemptId = "playback_attempt_id"
            case replanRequestId = "replan_request_id"
            case failedPlanId = "failed_plan_id"
            case planAttemptId = "plan_attempt_id"
            case planAttemptKey = "plan_attempt_key"
            case attemptedPlanKeys = "attempted_plan_keys"
            case localMutations = "local_mutations"
            case attemptCount = "attempt_count"
            case qualityPreference = "quality_preference"
            case positionSeconds = "position_seconds"
            case metered = "metered"
            case bandwidthEstimateKbps = "bandwidth_estimate_kbps"
            case bandwidthCapKbps = "bandwidth_cap_kbps"
            case selectedTracks = "selected_tracks"
            case failure = "failure"
            case capabilities = "client_capabilities"
            case clientPlaybackContext = "client_playback_context"
        }

        public init(
            protocolVersion: Int,
            clientFeatures: [String] = [],
            operation: ReplanOperationV3 = ReplanOperationV3(rawValue: ""),
            playbackAttemptId: String,
            replanRequestId: String,
            failedPlanId: String,
            planAttemptId: String,
            planAttemptKey: String,
            attemptedPlanKeys: [String],
            localMutations: [String] = [],
            attemptCount: Int,
            qualityPreference: String,
            positionSeconds: Double,
            metered: Bool,
            bandwidthEstimateKbps: Int? = nil,
            bandwidthCapKbps: Int? = nil,
            selectedTracks: SelectedTracksV3,
            failure: FailureV3? = nil,
            capabilities: ClientCodecCapabilitiesV3,
            clientPlaybackContext: ClientPlaybackContextV3
        ) {
            self.protocolVersion = protocolVersion
            self.clientFeatures = clientFeatures
            self.operation = operation
            self.playbackAttemptId = playbackAttemptId
            self.replanRequestId = replanRequestId
            self.failedPlanId = failedPlanId
            self.planAttemptId = planAttemptId
            self.planAttemptKey = planAttemptKey
            self.attemptedPlanKeys = attemptedPlanKeys
            self.localMutations = localMutations
            self.attemptCount = attemptCount
            self.qualityPreference = qualityPreference
            self.positionSeconds = positionSeconds
            self.metered = metered
            self.bandwidthEstimateKbps = bandwidthEstimateKbps
            self.bandwidthCapKbps = bandwidthCapKbps
            self.selectedTracks = selectedTracks
            self.failure = failure
            self.capabilities = capabilities
            self.clientPlaybackContext = clientPlaybackContext
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.protocolVersion = try container.decode(Int.self, forKey: .protocolVersion)
            self.clientFeatures = try container.decodeIfPresent([String].self, forKey: .clientFeatures) ?? []
            self.operation = try container.decodeIfPresent(ReplanOperationV3.self, forKey: .operation) ?? ReplanOperationV3(rawValue: "")
            self.playbackAttemptId = try container.decode(String.self, forKey: .playbackAttemptId)
            self.replanRequestId = try container.decode(String.self, forKey: .replanRequestId)
            self.failedPlanId = try container.decode(String.self, forKey: .failedPlanId)
            self.planAttemptId = try container.decode(String.self, forKey: .planAttemptId)
            self.planAttemptKey = try container.decode(String.self, forKey: .planAttemptKey)
            self.attemptedPlanKeys = try container.decode([String].self, forKey: .attemptedPlanKeys)
            self.localMutations = try container.decodeIfPresent([String].self, forKey: .localMutations) ?? []
            self.attemptCount = try container.decode(Int.self, forKey: .attemptCount)
            self.qualityPreference = try container.decode(String.self, forKey: .qualityPreference)
            self.positionSeconds = try container.decode(Double.self, forKey: .positionSeconds)
            self.metered = try container.decode(Bool.self, forKey: .metered)
            self.bandwidthEstimateKbps = try container.decodeIfPresent(Int.self, forKey: .bandwidthEstimateKbps)
            self.bandwidthCapKbps = try container.decodeIfPresent(Int.self, forKey: .bandwidthCapKbps)
            self.selectedTracks = try container.decode(SelectedTracksV3.self, forKey: .selectedTracks)
            self.failure = try container.decodeIfPresent(FailureV3.self, forKey: .failure)
            self.capabilities = try container.decode(ClientCodecCapabilitiesV3.self, forKey: .capabilities)
            self.clientPlaybackContext = try container.decode(ClientPlaybackContextV3.self, forKey: .clientPlaybackContext)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.protocolVersion, forKey: .protocolVersion)
            try container.encode(self.clientFeatures, forKey: .clientFeatures)
            try container.encode(self.operation, forKey: .operation)
            try container.encode(self.playbackAttemptId, forKey: .playbackAttemptId)
            try container.encode(self.replanRequestId, forKey: .replanRequestId)
            try container.encode(self.failedPlanId, forKey: .failedPlanId)
            try container.encode(self.planAttemptId, forKey: .planAttemptId)
            try container.encode(self.planAttemptKey, forKey: .planAttemptKey)
            try container.encode(self.attemptedPlanKeys, forKey: .attemptedPlanKeys)
            try container.encode(self.localMutations, forKey: .localMutations)
            try container.encode(self.attemptCount, forKey: .attemptCount)
            try container.encode(self.qualityPreference, forKey: .qualityPreference)
            try container.encode(self.positionSeconds, forKey: .positionSeconds)
            try container.encode(self.metered, forKey: .metered)
            try container.encodeIfPresent(self.bandwidthEstimateKbps, forKey: .bandwidthEstimateKbps)
            try container.encodeIfPresent(self.bandwidthCapKbps, forKey: .bandwidthCapKbps)
            try container.encode(self.selectedTracks, forKey: .selectedTracks)
            try container.encodeIfPresent(self.failure, forKey: .failure)
            try container.encode(self.capabilities, forKey: .capabilities)
            try container.encode(self.clientPlaybackContext, forKey: .clientPlaybackContext)
        }
    }

    /// Wire type `internal/playback.ResultEnvelope`. Direction: request. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct ResultEnvelope: Codable, Hashable, Sendable {
        public let type: RealtimeMessageType
        public let commandId: String
        public let sessionId: String
        public let status: RealtimeResultStatus
        public let error: String

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case commandId = "command_id"
            case sessionId = "session_id"
            case status = "status"
            case error = "error"
        }

        public init(
            type: RealtimeMessageType,
            commandId: String,
            sessionId: String,
            status: RealtimeResultStatus,
            error: String = ""
        ) {
            self.type = type
            self.commandId = commandId
            self.sessionId = sessionId
            self.status = status
            self.error = error
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decode(RealtimeMessageType.self, forKey: .type)
            self.commandId = try container.decode(String.self, forKey: .commandId)
            self.sessionId = try container.decode(String.self, forKey: .sessionId)
            self.status = try container.decode(RealtimeResultStatus.self, forKey: .status)
            self.error = try container.decodeIfPresent(String.self, forKey: .error) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.commandId, forKey: .commandId)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.status, forKey: .status)
            try container.encode(self.error, forKey: .error)
        }
    }

    /// Wire type `internal/playback.RouteEventV3`. Direction: request. Dialect: upstream-compat. Registered root.
    public struct RouteEventV3: Codable, Hashable, Sendable {
        public let protocolVersion: Int
        public let playbackAttemptId: String
        public let sessionId: String
        public let planId: String
        public let planAttemptId: String
        public let planAttemptKey: String
        public let event: String
        public let failureClassification: String
        public let fallbackReason: String
        public let appliedQuirkIds: [String]
        public let quirkRegistryRevision: String
        public let outputContextId: String
        public let diagnostics: [String: String]

        public enum CodingKeys: String, CodingKey {
            case protocolVersion = "protocol_version"
            case playbackAttemptId = "playback_attempt_id"
            case sessionId = "session_id"
            case planId = "plan_id"
            case planAttemptId = "plan_attempt_id"
            case planAttemptKey = "plan_attempt_key"
            case event = "event"
            case failureClassification = "failure_classification"
            case fallbackReason = "fallback_reason"
            case appliedQuirkIds = "applied_quirk_ids"
            case quirkRegistryRevision = "quirk_registry_revision"
            case outputContextId = "output_context_id"
            case diagnostics = "diagnostics"
        }

        public init(
            protocolVersion: Int,
            playbackAttemptId: String,
            sessionId: String = "",
            planId: String = "",
            planAttemptId: String = "",
            planAttemptKey: String = "",
            event: String,
            failureClassification: String = "",
            fallbackReason: String = "",
            appliedQuirkIds: [String] = [],
            quirkRegistryRevision: String = "",
            outputContextId: String = "",
            diagnostics: [String: String]
        ) {
            self.protocolVersion = protocolVersion
            self.playbackAttemptId = playbackAttemptId
            self.sessionId = sessionId
            self.planId = planId
            self.planAttemptId = planAttemptId
            self.planAttemptKey = planAttemptKey
            self.event = event
            self.failureClassification = failureClassification
            self.fallbackReason = fallbackReason
            self.appliedQuirkIds = appliedQuirkIds
            self.quirkRegistryRevision = quirkRegistryRevision
            self.outputContextId = outputContextId
            self.diagnostics = diagnostics
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.protocolVersion = try container.decode(Int.self, forKey: .protocolVersion)
            self.playbackAttemptId = try container.decode(String.self, forKey: .playbackAttemptId)
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.planId = try container.decodeIfPresent(String.self, forKey: .planId) ?? ""
            self.planAttemptId = try container.decodeIfPresent(String.self, forKey: .planAttemptId) ?? ""
            self.planAttemptKey = try container.decodeIfPresent(String.self, forKey: .planAttemptKey) ?? ""
            self.event = try container.decode(String.self, forKey: .event)
            self.failureClassification = try container.decodeIfPresent(String.self, forKey: .failureClassification) ?? ""
            self.fallbackReason = try container.decodeIfPresent(String.self, forKey: .fallbackReason) ?? ""
            self.appliedQuirkIds = try container.decodeIfPresent([String].self, forKey: .appliedQuirkIds) ?? []
            self.quirkRegistryRevision = try container.decodeIfPresent(String.self, forKey: .quirkRegistryRevision) ?? ""
            self.outputContextId = try container.decodeIfPresent(String.self, forKey: .outputContextId) ?? ""
            self.diagnostics = try container.decode([String: String].self, forKey: .diagnostics)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.protocolVersion, forKey: .protocolVersion)
            try container.encode(self.playbackAttemptId, forKey: .playbackAttemptId)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.planId, forKey: .planId)
            try container.encode(self.planAttemptId, forKey: .planAttemptId)
            try container.encode(self.planAttemptKey, forKey: .planAttemptKey)
            try container.encode(self.event, forKey: .event)
            try container.encode(self.failureClassification, forKey: .failureClassification)
            try container.encode(self.fallbackReason, forKey: .fallbackReason)
            try container.encode(self.appliedQuirkIds, forKey: .appliedQuirkIds)
            try container.encode(self.quirkRegistryRevision, forKey: .quirkRegistryRevision)
            try container.encode(self.outputContextId, forKey: .outputContextId)
            try container.encode(self.diagnostics, forKey: .diagnostics)
        }
    }

    /// Wire type `internal/playback.SelectedTracksV3`. Direction: both. Dialect: upstream-compat.
    public struct SelectedTracksV3: Codable, Hashable, Sendable {
        public let audio: TrackIdentityV3?
        public let subtitle: TrackIdentityV3?

        public enum CodingKeys: String, CodingKey {
            case audio = "audio"
            case subtitle = "subtitle"
        }

        public init(
            audio: TrackIdentityV3? = nil,
            subtitle: TrackIdentityV3? = nil
        ) {
            self.audio = audio
            self.subtitle = subtitle
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.audio = try container.decodeIfPresent(TrackIdentityV3.self, forKey: .audio)
            self.subtitle = try container.decodeIfPresent(TrackIdentityV3.self, forKey: .subtitle)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encodeIfPresent(self.audio, forKey: .audio)
            try container.encodeIfPresent(self.subtitle, forKey: .subtitle)
        }
    }

    /// Wire type `internal/playback.SourceDescriptorV3`. Direction: response. Dialect: upstream-compat.
    public struct SourceDescriptorV3: Codable, Hashable, Sendable {
        public let mediaFileId: Int
        public let durationSeconds: Double?
        public let container: String
        public let videoCodec: String
        public let videoProfile: String
        public let videoLevel: Int
        public let bitDepth: Int
        public let colorRange: String
        public let width: Int
        public let height: Int
        public let frameRate: Double
        public let bitrateKbps: Int
        public let dynamicRange: String
        public let hdr10Plus: Bool
        public let dvProfile: Int
        public let dvLevel: Int
        public let dvblCompatId: Int
        public let dvBaseLayerProven: Bool
        public let dvEnhancementLayer: EnhancementLayerV3
        public let audioCodec: String
        public let audioChannels: Int
        public let audioLayout: String
        public let videoCopyUnsafe: Bool

        public enum CodingKeys: String, CodingKey {
            case mediaFileId = "media_file_id"
            case durationSeconds = "duration_seconds"
            case container = "container"
            case videoCodec = "video_codec"
            case videoProfile = "video_profile"
            case videoLevel = "video_level"
            case bitDepth = "bit_depth"
            case colorRange = "color_range"
            case width = "width"
            case height = "height"
            case frameRate = "frame_rate"
            case bitrateKbps = "bitrate_kbps"
            case dynamicRange = "dynamic_range"
            case hdr10Plus = "hdr10_plus"
            case dvProfile = "dolby_vision_profile"
            case dvLevel = "dolby_vision_level"
            case dvblCompatId = "dv_bl_compat_id"
            case dvBaseLayerProven = "dv_base_layer_proven"
            case dvEnhancementLayer = "dv_enhancement_layer"
            case audioCodec = "audio_codec"
            case audioChannels = "audio_channels"
            case audioLayout = "audio_layout"
            case videoCopyUnsafe = "video_copy_unsafe"
        }

        public init(
            mediaFileId: Int = 0,
            durationSeconds: Double? = nil,
            container: String = "",
            videoCodec: String = "",
            videoProfile: String = "",
            videoLevel: Int = 0,
            bitDepth: Int = 0,
            colorRange: String = "",
            width: Int = 0,
            height: Int = 0,
            frameRate: Double = 0.0,
            bitrateKbps: Int = 0,
            dynamicRange: String = "",
            hdr10Plus: Bool = false,
            dvProfile: Int = 0,
            dvLevel: Int = 0,
            dvblCompatId: Int = 0,
            dvBaseLayerProven: Bool = false,
            dvEnhancementLayer: EnhancementLayerV3 = EnhancementLayerV3(rawValue: ""),
            audioCodec: String = "",
            audioChannels: Int = 0,
            audioLayout: String = "",
            videoCopyUnsafe: Bool = false
        ) {
            self.mediaFileId = mediaFileId
            self.durationSeconds = durationSeconds
            self.container = container
            self.videoCodec = videoCodec
            self.videoProfile = videoProfile
            self.videoLevel = videoLevel
            self.bitDepth = bitDepth
            self.colorRange = colorRange
            self.width = width
            self.height = height
            self.frameRate = frameRate
            self.bitrateKbps = bitrateKbps
            self.dynamicRange = dynamicRange
            self.hdr10Plus = hdr10Plus
            self.dvProfile = dvProfile
            self.dvLevel = dvLevel
            self.dvblCompatId = dvblCompatId
            self.dvBaseLayerProven = dvBaseLayerProven
            self.dvEnhancementLayer = dvEnhancementLayer
            self.audioCodec = audioCodec
            self.audioChannels = audioChannels
            self.audioLayout = audioLayout
            self.videoCopyUnsafe = videoCopyUnsafe
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.mediaFileId = try container.decodeIfPresent(Int.self, forKey: .mediaFileId) ?? 0
            self.durationSeconds = try container.decodeIfPresent(Double.self, forKey: .durationSeconds)
            self.container = try container.decodeIfPresent(String.self, forKey: .container) ?? ""
            self.videoCodec = try container.decodeIfPresent(String.self, forKey: .videoCodec) ?? ""
            self.videoProfile = try container.decodeIfPresent(String.self, forKey: .videoProfile) ?? ""
            self.videoLevel = try container.decodeIfPresent(Int.self, forKey: .videoLevel) ?? 0
            self.bitDepth = try container.decodeIfPresent(Int.self, forKey: .bitDepth) ?? 0
            self.colorRange = try container.decodeIfPresent(String.self, forKey: .colorRange) ?? ""
            self.width = try container.decodeIfPresent(Int.self, forKey: .width) ?? 0
            self.height = try container.decodeIfPresent(Int.self, forKey: .height) ?? 0
            self.frameRate = try container.decodeIfPresent(Double.self, forKey: .frameRate) ?? 0.0
            self.bitrateKbps = try container.decodeIfPresent(Int.self, forKey: .bitrateKbps) ?? 0
            self.dynamicRange = try container.decodeIfPresent(String.self, forKey: .dynamicRange) ?? ""
            self.hdr10Plus = try container.decodeIfPresent(Bool.self, forKey: .hdr10Plus) ?? false
            self.dvProfile = try container.decodeIfPresent(Int.self, forKey: .dvProfile) ?? 0
            self.dvLevel = try container.decodeIfPresent(Int.self, forKey: .dvLevel) ?? 0
            self.dvblCompatId = try container.decodeIfPresent(Int.self, forKey: .dvblCompatId) ?? 0
            self.dvBaseLayerProven = try container.decodeIfPresent(Bool.self, forKey: .dvBaseLayerProven) ?? false
            self.dvEnhancementLayer = try container.decodeIfPresent(EnhancementLayerV3.self, forKey: .dvEnhancementLayer) ?? EnhancementLayerV3(rawValue: "")
            self.audioCodec = try container.decodeIfPresent(String.self, forKey: .audioCodec) ?? ""
            self.audioChannels = try container.decodeIfPresent(Int.self, forKey: .audioChannels) ?? 0
            self.audioLayout = try container.decodeIfPresent(String.self, forKey: .audioLayout) ?? ""
            self.videoCopyUnsafe = try container.decodeIfPresent(Bool.self, forKey: .videoCopyUnsafe) ?? false
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.mediaFileId, forKey: .mediaFileId)
            try container.encodeIfPresent(self.durationSeconds, forKey: .durationSeconds)
            try container.encode(self.container, forKey: .container)
            try container.encode(self.videoCodec, forKey: .videoCodec)
            try container.encode(self.videoProfile, forKey: .videoProfile)
            try container.encode(self.videoLevel, forKey: .videoLevel)
            try container.encode(self.bitDepth, forKey: .bitDepth)
            try container.encode(self.colorRange, forKey: .colorRange)
            try container.encode(self.width, forKey: .width)
            try container.encode(self.height, forKey: .height)
            try container.encode(self.frameRate, forKey: .frameRate)
            try container.encode(self.bitrateKbps, forKey: .bitrateKbps)
            try container.encode(self.dynamicRange, forKey: .dynamicRange)
            try container.encode(self.hdr10Plus, forKey: .hdr10Plus)
            try container.encode(self.dvProfile, forKey: .dvProfile)
            try container.encode(self.dvLevel, forKey: .dvLevel)
            try container.encode(self.dvblCompatId, forKey: .dvblCompatId)
            try container.encode(self.dvBaseLayerProven, forKey: .dvBaseLayerProven)
            try container.encode(self.dvEnhancementLayer, forKey: .dvEnhancementLayer)
            try container.encode(self.audioCodec, forKey: .audioCodec)
            try container.encode(self.audioChannels, forKey: .audioChannels)
            try container.encode(self.audioLayout, forKey: .audioLayout)
            try container.encode(self.videoCopyUnsafe, forKey: .videoCopyUnsafe)
        }
    }

    /// Wire type `internal/playback.StartRequestV3`. Direction: request. Dialect: upstream-compat. Registered root.
    public struct StartRequestV3: Codable, Hashable, Sendable {
        public let protocolVersion: Int
        public let clientFeatures: [String]
        public let fileId: Int
        public let profileId: String
        public let playbackAttemptId: String
        public let qualityPreference: String
        public let subtitleFidelityPreference: SubtitleFidelityV3
        public let startPosition: Double?
        public let progressPersistence: ProgressPersistenceV3
        public let audioTrackId: String
        public let audioTrackIndex: Int?
        public let subtitleTrackId: String
        public let subtitleTrackIndex: Int?
        public let metered: Bool
        public let bandwidthEstimateKbps: Int?
        public let bandwidthCapKbps: Int?
        public let capabilities: ClientCodecCapabilitiesV3
        public let clientPlaybackContext: ClientPlaybackContextV3

        public enum CodingKeys: String, CodingKey {
            case protocolVersion = "protocol_version"
            case clientFeatures = "client_features"
            case fileId = "file_id"
            case profileId = "profile_id"
            case playbackAttemptId = "playback_attempt_id"
            case qualityPreference = "quality_preference"
            case subtitleFidelityPreference = "subtitle_fidelity_preference"
            case startPosition = "start_position"
            case progressPersistence = "progress_persistence"
            case audioTrackId = "audio_track_id"
            case audioTrackIndex = "audio_track_index"
            case subtitleTrackId = "subtitle_track_id"
            case subtitleTrackIndex = "subtitle_track_index"
            case metered = "metered"
            case bandwidthEstimateKbps = "bandwidth_estimate_kbps"
            case bandwidthCapKbps = "bandwidth_cap_kbps"
            case capabilities = "client_capabilities"
            case clientPlaybackContext = "client_playback_context"
        }

        public init(
            protocolVersion: Int,
            clientFeatures: [String],
            fileId: Int,
            profileId: String,
            playbackAttemptId: String,
            qualityPreference: String,
            subtitleFidelityPreference: SubtitleFidelityV3,
            startPosition: Double? = nil,
            progressPersistence: ProgressPersistenceV3 = ProgressPersistenceV3(rawValue: ""),
            audioTrackId: String = "",
            audioTrackIndex: Int? = nil,
            subtitleTrackId: String = "",
            subtitleTrackIndex: Int? = nil,
            metered: Bool,
            bandwidthEstimateKbps: Int? = nil,
            bandwidthCapKbps: Int? = nil,
            capabilities: ClientCodecCapabilitiesV3,
            clientPlaybackContext: ClientPlaybackContextV3
        ) {
            self.protocolVersion = protocolVersion
            self.clientFeatures = clientFeatures
            self.fileId = fileId
            self.profileId = profileId
            self.playbackAttemptId = playbackAttemptId
            self.qualityPreference = qualityPreference
            self.subtitleFidelityPreference = subtitleFidelityPreference
            self.startPosition = startPosition
            self.progressPersistence = progressPersistence
            self.audioTrackId = audioTrackId
            self.audioTrackIndex = audioTrackIndex
            self.subtitleTrackId = subtitleTrackId
            self.subtitleTrackIndex = subtitleTrackIndex
            self.metered = metered
            self.bandwidthEstimateKbps = bandwidthEstimateKbps
            self.bandwidthCapKbps = bandwidthCapKbps
            self.capabilities = capabilities
            self.clientPlaybackContext = clientPlaybackContext
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.protocolVersion = try container.decode(Int.self, forKey: .protocolVersion)
            self.clientFeatures = try container.decode([String].self, forKey: .clientFeatures)
            self.fileId = try container.decode(Int.self, forKey: .fileId)
            self.profileId = try container.decode(String.self, forKey: .profileId)
            self.playbackAttemptId = try container.decode(String.self, forKey: .playbackAttemptId)
            self.qualityPreference = try container.decode(String.self, forKey: .qualityPreference)
            self.subtitleFidelityPreference = try container.decode(SubtitleFidelityV3.self, forKey: .subtitleFidelityPreference)
            self.startPosition = try container.decodeIfPresent(Double.self, forKey: .startPosition)
            self.progressPersistence = try container.decodeIfPresent(ProgressPersistenceV3.self, forKey: .progressPersistence) ?? ProgressPersistenceV3(rawValue: "")
            self.audioTrackId = try container.decodeIfPresent(String.self, forKey: .audioTrackId) ?? ""
            self.audioTrackIndex = try container.decodeIfPresent(Int.self, forKey: .audioTrackIndex)
            self.subtitleTrackId = try container.decodeIfPresent(String.self, forKey: .subtitleTrackId) ?? ""
            self.subtitleTrackIndex = try container.decodeIfPresent(Int.self, forKey: .subtitleTrackIndex)
            self.metered = try container.decode(Bool.self, forKey: .metered)
            self.bandwidthEstimateKbps = try container.decodeIfPresent(Int.self, forKey: .bandwidthEstimateKbps)
            self.bandwidthCapKbps = try container.decodeIfPresent(Int.self, forKey: .bandwidthCapKbps)
            self.capabilities = try container.decode(ClientCodecCapabilitiesV3.self, forKey: .capabilities)
            self.clientPlaybackContext = try container.decode(ClientPlaybackContextV3.self, forKey: .clientPlaybackContext)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.protocolVersion, forKey: .protocolVersion)
            try container.encode(self.clientFeatures, forKey: .clientFeatures)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encode(self.profileId, forKey: .profileId)
            try container.encode(self.playbackAttemptId, forKey: .playbackAttemptId)
            try container.encode(self.qualityPreference, forKey: .qualityPreference)
            try container.encode(self.subtitleFidelityPreference, forKey: .subtitleFidelityPreference)
            try container.encodeIfPresent(self.startPosition, forKey: .startPosition)
            try container.encode(self.progressPersistence, forKey: .progressPersistence)
            try container.encode(self.audioTrackId, forKey: .audioTrackId)
            try container.encodeIfPresent(self.audioTrackIndex, forKey: .audioTrackIndex)
            try container.encode(self.subtitleTrackId, forKey: .subtitleTrackId)
            try container.encodeIfPresent(self.subtitleTrackIndex, forKey: .subtitleTrackIndex)
            try container.encode(self.metered, forKey: .metered)
            try container.encodeIfPresent(self.bandwidthEstimateKbps, forKey: .bandwidthEstimateKbps)
            try container.encodeIfPresent(self.bandwidthCapKbps, forKey: .bandwidthCapKbps)
            try container.encode(self.capabilities, forKey: .capabilities)
            try container.encode(self.clientPlaybackContext, forKey: .clientPlaybackContext)
        }
    }

    /// Wire type `internal/playback.StreamCue`. Direction: response. Dialect: upstream-compat. Gate: realtime.
    public struct StreamCue: Codable, Hashable, Sendable {
        public let start: Double
        public let end: Double
        public let text: String

        public enum CodingKeys: String, CodingKey {
            case start = "start"
            case end = "end"
            case text = "text"
        }

        public init(
            start: Double = 0.0,
            end: Double = 0.0,
            text: String = ""
        ) {
            self.start = start
            self.end = end
            self.text = text
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.start = try container.decodeIfPresent(Double.self, forKey: .start) ?? 0.0
            self.end = try container.decodeIfPresent(Double.self, forKey: .end) ?? 0.0
            self.text = try container.decodeIfPresent(String.self, forKey: .text) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.start, forKey: .start)
            try container.encode(self.end, forKey: .end)
            try container.encode(self.text, forKey: .text)
        }
    }

    /// Wire type `internal/playback.StreamProtocolV3`. Direction: response. Dialect: upstream-compat.
    public struct StreamProtocolV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let HTTP_PROGRESSIVE: StreamProtocolV3 = StreamProtocolV3(rawValue: "http_progressive")
        public static let HLS: StreamProtocolV3 = StreamProtocolV3(rawValue: "hls")
        public static let KNOWN: [StreamProtocolV3] = [HTTP_PROGRESSIVE, HLS]
    }

    /// Wire type `internal/playback.StreamV3`. Direction: response. Dialect: upstream-compat.
    public struct StreamV3: Codable, Hashable, Sendable {
        public let url: String
        public let protocolValue: StreamProtocolV3
        public let container: String
        public let mimeType: String
        public let headers: [String: String]
        public let headerRefresh: HeaderRefreshModeV3
        public let headerRefreshUrl: String

        public enum CodingKeys: String, CodingKey {
            case url = "url"
            case protocolValue = "protocol"
            case container = "container"
            case mimeType = "mime_type"
            case headers = "headers"
            case headerRefresh = "header_refresh"
            case headerRefreshUrl = "header_refresh_url"
        }

        public init(
            url: String = "",
            protocolValue: StreamProtocolV3 = StreamProtocolV3(rawValue: ""),
            container: String = "",
            mimeType: String = "",
            headers: [String: String] = [:],
            headerRefresh: HeaderRefreshModeV3 = HeaderRefreshModeV3(rawValue: ""),
            headerRefreshUrl: String = ""
        ) {
            self.url = url
            self.protocolValue = protocolValue
            self.container = container
            self.mimeType = mimeType
            self.headers = headers
            self.headerRefresh = headerRefresh
            self.headerRefreshUrl = headerRefreshUrl
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.url = try container.decodeIfPresent(String.self, forKey: .url) ?? ""
            self.protocolValue = try container.decodeIfPresent(StreamProtocolV3.self, forKey: .protocolValue) ?? StreamProtocolV3(rawValue: "")
            self.container = try container.decodeIfPresent(String.self, forKey: .container) ?? ""
            self.mimeType = try container.decodeIfPresent(String.self, forKey: .mimeType) ?? ""
            self.headers = try container.decodeIfPresent([String: String].self, forKey: .headers) ?? [:]
            self.headerRefresh = try container.decodeIfPresent(HeaderRefreshModeV3.self, forKey: .headerRefresh) ?? HeaderRefreshModeV3(rawValue: "")
            self.headerRefreshUrl = try container.decodeIfPresent(String.self, forKey: .headerRefreshUrl) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.url, forKey: .url)
            try container.encode(self.protocolValue, forKey: .protocolValue)
            try container.encode(self.container, forKey: .container)
            try container.encode(self.mimeType, forKey: .mimeType)
            try container.encode(self.headers, forKey: .headers)
            try container.encode(self.headerRefresh, forKey: .headerRefresh)
            try container.encode(self.headerRefreshUrl, forKey: .headerRefreshUrl)
        }
    }

    /// Wire type `internal/playback.SubtitleArtifactV3`. Direction: response. Dialect: upstream-compat.
    public struct SubtitleArtifactV3: Codable, Hashable, Sendable {
        public let url: String
        public let mimeType: String
        public let format: String
        public let timingOriginSeconds: Double

        public enum CodingKeys: String, CodingKey {
            case url = "url"
            case mimeType = "mime_type"
            case format = "format"
            case timingOriginSeconds = "timing_origin_seconds"
        }

        public init(
            url: String = "",
            mimeType: String = "",
            format: String = "",
            timingOriginSeconds: Double = 0.0
        ) {
            self.url = url
            self.mimeType = mimeType
            self.format = format
            self.timingOriginSeconds = timingOriginSeconds
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.url = try container.decodeIfPresent(String.self, forKey: .url) ?? ""
            self.mimeType = try container.decodeIfPresent(String.self, forKey: .mimeType) ?? ""
            self.format = try container.decodeIfPresent(String.self, forKey: .format) ?? ""
            self.timingOriginSeconds = try container.decodeIfPresent(Double.self, forKey: .timingOriginSeconds) ?? 0.0
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.url, forKey: .url)
            try container.encode(self.mimeType, forKey: .mimeType)
            try container.encode(self.format, forKey: .format)
            try container.encode(self.timingOriginSeconds, forKey: .timingOriginSeconds)
        }
    }

    /// Wire type `internal/playback.SubtitleClaimsV3`. Direction: response. Dialect: upstream-compat.
    public struct SubtitleClaimsV3: Codable, Hashable, Sendable {
        public let assStylingPreserved: Bool
        public let bitmapOverlay: Bool
        public let bitmapSidecar: Bool
        public let reason: String

        public enum CodingKeys: String, CodingKey {
            case assStylingPreserved = "ass_styling_preserved"
            case bitmapOverlay = "bitmap_overlay"
            case bitmapSidecar = "bitmap_sidecar"
            case reason = "reason"
        }

        public init(
            assStylingPreserved: Bool = false,
            bitmapOverlay: Bool = false,
            bitmapSidecar: Bool = false,
            reason: String = ""
        ) {
            self.assStylingPreserved = assStylingPreserved
            self.bitmapOverlay = bitmapOverlay
            self.bitmapSidecar = bitmapSidecar
            self.reason = reason
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.assStylingPreserved = try container.decodeIfPresent(Bool.self, forKey: .assStylingPreserved) ?? false
            self.bitmapOverlay = try container.decodeIfPresent(Bool.self, forKey: .bitmapOverlay) ?? false
            self.bitmapSidecar = try container.decodeIfPresent(Bool.self, forKey: .bitmapSidecar) ?? false
            self.reason = try container.decodeIfPresent(String.self, forKey: .reason) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.assStylingPreserved, forKey: .assStylingPreserved)
            try container.encode(self.bitmapOverlay, forKey: .bitmapOverlay)
            try container.encode(self.bitmapSidecar, forKey: .bitmapSidecar)
            try container.encode(self.reason, forKey: .reason)
        }
    }

    /// Wire type `internal/playback.SubtitleDecisionV3`. Direction: response. Dialect: upstream-compat.
    public struct SubtitleDecisionV3: Codable, Hashable, Sendable {
        public let mode: SubtitleModeV3
        public let trackId: String
        public let artifact: SubtitleArtifactV3?
        public let inventory: [SubtitleInventoryItemV3]

        public enum CodingKeys: String, CodingKey {
            case mode = "mode"
            case trackId = "track_id"
            case artifact = "artifact"
            case inventory = "inventory"
        }

        public init(
            mode: SubtitleModeV3 = SubtitleModeV3(rawValue: ""),
            trackId: String = "",
            artifact: SubtitleArtifactV3? = nil,
            inventory: [SubtitleInventoryItemV3] = []
        ) {
            self.mode = mode
            self.trackId = trackId
            self.artifact = artifact
            self.inventory = inventory
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.mode = try container.decodeIfPresent(SubtitleModeV3.self, forKey: .mode) ?? SubtitleModeV3(rawValue: "")
            self.trackId = try container.decodeIfPresent(String.self, forKey: .trackId) ?? ""
            self.artifact = try container.decodeIfPresent(SubtitleArtifactV3.self, forKey: .artifact)
            self.inventory = try container.decodeIfPresent([SubtitleInventoryItemV3].self, forKey: .inventory) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.mode, forKey: .mode)
            try container.encode(self.trackId, forKey: .trackId)
            try container.encodeIfPresent(self.artifact, forKey: .artifact)
            try container.encode(self.inventory, forKey: .inventory)
        }
    }

    /// Wire type `internal/playback.SubtitleFidelityV3`. Direction: request. Dialect: upstream-compat.
    public struct SubtitleFidelityV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let PRESERVE: SubtitleFidelityV3 = SubtitleFidelityV3(rawValue: "preserve")
        public static let COMPATIBLE: SubtitleFidelityV3 = SubtitleFidelityV3(rawValue: "compatible")
        public static let KNOWN: [SubtitleFidelityV3] = [PRESERVE, COMPATIBLE]
    }

    /// Wire type `internal/playback.SubtitleInventoryItemV3`. Direction: response. Dialect: upstream-compat.
    public struct SubtitleInventoryItemV3: Codable, Hashable, Sendable {
        public let trackId: String
        public let combinedIndex: Int
        public let source: String
        public let codec: String
        public let language: String
        public let label: String
        public let forced: Bool
        public let defaultValue: Bool
        public let hearingImpaired: Bool
        public let delivery: String
        public let url: String
        public let fontBundleUrl: String

        public enum CodingKeys: String, CodingKey {
            case trackId = "track_id"
            case combinedIndex = "combined_index"
            case source = "source"
            case codec = "codec"
            case language = "language"
            case label = "label"
            case forced = "forced"
            case defaultValue = "default"
            case hearingImpaired = "hearing_impaired"
            case delivery = "delivery"
            case url = "url"
            case fontBundleUrl = "font_bundle_url"
        }

        public init(
            trackId: String = "",
            combinedIndex: Int = 0,
            source: String = "",
            codec: String = "",
            language: String = "",
            label: String = "",
            forced: Bool = false,
            defaultValue: Bool = false,
            hearingImpaired: Bool = false,
            delivery: String = "",
            url: String = "",
            fontBundleUrl: String = ""
        ) {
            self.trackId = trackId
            self.combinedIndex = combinedIndex
            self.source = source
            self.codec = codec
            self.language = language
            self.label = label
            self.forced = forced
            self.defaultValue = defaultValue
            self.hearingImpaired = hearingImpaired
            self.delivery = delivery
            self.url = url
            self.fontBundleUrl = fontBundleUrl
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.trackId = try container.decodeIfPresent(String.self, forKey: .trackId) ?? ""
            self.combinedIndex = try container.decodeIfPresent(Int.self, forKey: .combinedIndex) ?? 0
            self.source = try container.decodeIfPresent(String.self, forKey: .source) ?? ""
            self.codec = try container.decodeIfPresent(String.self, forKey: .codec) ?? ""
            self.language = try container.decodeIfPresent(String.self, forKey: .language) ?? ""
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
            self.forced = try container.decodeIfPresent(Bool.self, forKey: .forced) ?? false
            self.defaultValue = try container.decodeIfPresent(Bool.self, forKey: .defaultValue) ?? false
            self.hearingImpaired = try container.decodeIfPresent(Bool.self, forKey: .hearingImpaired) ?? false
            self.delivery = try container.decodeIfPresent(String.self, forKey: .delivery) ?? ""
            self.url = try container.decodeIfPresent(String.self, forKey: .url) ?? ""
            self.fontBundleUrl = try container.decodeIfPresent(String.self, forKey: .fontBundleUrl) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.trackId, forKey: .trackId)
            try container.encode(self.combinedIndex, forKey: .combinedIndex)
            try container.encode(self.source, forKey: .source)
            try container.encode(self.codec, forKey: .codec)
            try container.encode(self.language, forKey: .language)
            try container.encode(self.label, forKey: .label)
            try container.encode(self.forced, forKey: .forced)
            try container.encode(self.defaultValue, forKey: .defaultValue)
            try container.encode(self.hearingImpaired, forKey: .hearingImpaired)
            try container.encode(self.delivery, forKey: .delivery)
            try container.encode(self.url, forKey: .url)
            try container.encode(self.fontBundleUrl, forKey: .fontBundleUrl)
        }
    }

    /// Wire type `internal/playback.SubtitleModeV3`. Direction: response. Dialect: upstream-compat.
    public struct SubtitleModeV3: RawRepresentable, Codable, Hashable, Sendable {
        public let wire: String
        public var rawValue: String { wire }
        public init(rawValue: String) { self.wire = rawValue }

        public static let OFF: SubtitleModeV3 = SubtitleModeV3(rawValue: "off")
        public static let RENDER: SubtitleModeV3 = SubtitleModeV3(rawValue: "render")
        public static let CONVERT: SubtitleModeV3 = SubtitleModeV3(rawValue: "convert")
        public static let BURN_IN: SubtitleModeV3 = SubtitleModeV3(rawValue: "burn_in")
        public static let KNOWN: [SubtitleModeV3] = [OFF, RENDER, CONVERT, BURN_IN]
    }

    /// Wire type `internal/playback.SubtitleReadyPayload`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct SubtitleReadyPayload: Codable, Hashable, Sendable {
        public let sessionId: String
        public let fileId: Int
        public let subtitleId: Int
        public let language: String
        public let label: String
        public let track: SubtitleInventoryItemV3?

        public enum CodingKeys: String, CodingKey {
            case sessionId = "session_id"
            case fileId = "file_id"
            case subtitleId = "subtitle_id"
            case language = "language"
            case label = "label"
            case track = "track"
        }

        public init(
            sessionId: String = "",
            fileId: Int = 0,
            subtitleId: Int = 0,
            language: String = "",
            label: String = "",
            track: SubtitleInventoryItemV3? = nil
        ) {
            self.sessionId = sessionId
            self.fileId = fileId
            self.subtitleId = subtitleId
            self.language = language
            self.label = label
            self.track = track
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.fileId = try container.decodeIfPresent(Int.self, forKey: .fileId) ?? 0
            self.subtitleId = try container.decodeIfPresent(Int.self, forKey: .subtitleId) ?? 0
            self.language = try container.decodeIfPresent(String.self, forKey: .language) ?? ""
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
            self.track = try container.decodeIfPresent(SubtitleInventoryItemV3.self, forKey: .track)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encode(self.subtitleId, forKey: .subtitleId)
            try container.encode(self.language, forKey: .language)
            try container.encode(self.label, forKey: .label)
            try container.encodeIfPresent(self.track, forKey: .track)
        }
    }

    /// Wire type `internal/playback.SubtitleTranslationCompletedPayload`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct SubtitleTranslationCompletedPayload: Codable, Hashable, Sendable {
        public let sessionId: String
        public let fileId: Int
        public let jobId: Int64
        public let trackKey: String
        public let subtitleId: Int
        public let language: String
        public let label: String
        public let track: SubtitleInventoryItemV3?

        public enum CodingKeys: String, CodingKey {
            case sessionId = "session_id"
            case fileId = "file_id"
            case jobId = "job_id"
            case trackKey = "track_key"
            case subtitleId = "subtitle_id"
            case language = "language"
            case label = "label"
            case track = "track"
        }

        public init(
            sessionId: String = "",
            fileId: Int = 0,
            jobId: Int64 = 0,
            trackKey: String = "",
            subtitleId: Int = 0,
            language: String = "",
            label: String = "",
            track: SubtitleInventoryItemV3? = nil
        ) {
            self.sessionId = sessionId
            self.fileId = fileId
            self.jobId = jobId
            self.trackKey = trackKey
            self.subtitleId = subtitleId
            self.language = language
            self.label = label
            self.track = track
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.fileId = try container.decodeIfPresent(Int.self, forKey: .fileId) ?? 0
            self.jobId = try container.decodeIfPresent(Int64.self, forKey: .jobId) ?? 0
            self.trackKey = try container.decodeIfPresent(String.self, forKey: .trackKey) ?? ""
            self.subtitleId = try container.decodeIfPresent(Int.self, forKey: .subtitleId) ?? 0
            self.language = try container.decodeIfPresent(String.self, forKey: .language) ?? ""
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
            self.track = try container.decodeIfPresent(SubtitleInventoryItemV3.self, forKey: .track)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encode(self.jobId, forKey: .jobId)
            try container.encode(self.trackKey, forKey: .trackKey)
            try container.encode(self.subtitleId, forKey: .subtitleId)
            try container.encode(self.language, forKey: .language)
            try container.encode(self.label, forKey: .label)
            try container.encodeIfPresent(self.track, forKey: .track)
        }
    }

    /// Wire type `internal/playback.SubtitleTranslationCuesPayload`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct SubtitleTranslationCuesPayload: Codable, Hashable, Sendable {
        public let sessionId: String
        public let fileId: Int
        public let jobId: Int64
        public let trackKey: String
        public let cues: [StreamCue]
        public let done: Int
        public let total: Int

        public enum CodingKeys: String, CodingKey {
            case sessionId = "session_id"
            case fileId = "file_id"
            case jobId = "job_id"
            case trackKey = "track_key"
            case cues = "cues"
            case done = "done"
            case total = "total"
        }

        public init(
            sessionId: String = "",
            fileId: Int = 0,
            jobId: Int64 = 0,
            trackKey: String = "",
            cues: [StreamCue] = [],
            done: Int = 0,
            total: Int = 0
        ) {
            self.sessionId = sessionId
            self.fileId = fileId
            self.jobId = jobId
            self.trackKey = trackKey
            self.cues = cues
            self.done = done
            self.total = total
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.fileId = try container.decodeIfPresent(Int.self, forKey: .fileId) ?? 0
            self.jobId = try container.decodeIfPresent(Int64.self, forKey: .jobId) ?? 0
            self.trackKey = try container.decodeIfPresent(String.self, forKey: .trackKey) ?? ""
            self.cues = try container.decodeIfPresent([StreamCue].self, forKey: .cues) ?? []
            self.done = try container.decodeIfPresent(Int.self, forKey: .done) ?? 0
            self.total = try container.decodeIfPresent(Int.self, forKey: .total) ?? 0
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encode(self.jobId, forKey: .jobId)
            try container.encode(self.trackKey, forKey: .trackKey)
            try container.encode(self.cues, forKey: .cues)
            try container.encode(self.done, forKey: .done)
            try container.encode(self.total, forKey: .total)
        }
    }

    /// Wire type `internal/playback.SubtitleTranslationFailedPayload`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct SubtitleTranslationFailedPayload: Codable, Hashable, Sendable {
        public let sessionId: String
        public let fileId: Int
        public let jobId: Int64
        public let trackKey: String
        public let message: String

        public enum CodingKeys: String, CodingKey {
            case sessionId = "session_id"
            case fileId = "file_id"
            case jobId = "job_id"
            case trackKey = "track_key"
            case message = "message"
        }

        public init(
            sessionId: String = "",
            fileId: Int = 0,
            jobId: Int64 = 0,
            trackKey: String = "",
            message: String = ""
        ) {
            self.sessionId = sessionId
            self.fileId = fileId
            self.jobId = jobId
            self.trackKey = trackKey
            self.message = message
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.fileId = try container.decodeIfPresent(Int.self, forKey: .fileId) ?? 0
            self.jobId = try container.decodeIfPresent(Int64.self, forKey: .jobId) ?? 0
            self.trackKey = try container.decodeIfPresent(String.self, forKey: .trackKey) ?? ""
            self.message = try container.decodeIfPresent(String.self, forKey: .message) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encode(self.jobId, forKey: .jobId)
            try container.encode(self.trackKey, forKey: .trackKey)
            try container.encode(self.message, forKey: .message)
        }
    }

    /// Wire type `internal/playback.SubtitleTranslationStartedPayload`. Direction: response. Dialect: upstream-compat. Gate: realtime. Registered root.
    public struct SubtitleTranslationStartedPayload: Codable, Hashable, Sendable {
        public let sessionId: String
        public let fileId: Int
        public let jobId: Int64
        public let trackKey: String
        public let language: String
        public let label: String
        public let totalCues: Int

        public enum CodingKeys: String, CodingKey {
            case sessionId = "session_id"
            case fileId = "file_id"
            case jobId = "job_id"
            case trackKey = "track_key"
            case language = "language"
            case label = "label"
            case totalCues = "total_cues"
        }

        public init(
            sessionId: String = "",
            fileId: Int = 0,
            jobId: Int64 = 0,
            trackKey: String = "",
            language: String = "",
            label: String = "",
            totalCues: Int = 0
        ) {
            self.sessionId = sessionId
            self.fileId = fileId
            self.jobId = jobId
            self.trackKey = trackKey
            self.language = language
            self.label = label
            self.totalCues = totalCues
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.sessionId = try container.decodeIfPresent(String.self, forKey: .sessionId) ?? ""
            self.fileId = try container.decodeIfPresent(Int.self, forKey: .fileId) ?? 0
            self.jobId = try container.decodeIfPresent(Int64.self, forKey: .jobId) ?? 0
            self.trackKey = try container.decodeIfPresent(String.self, forKey: .trackKey) ?? ""
            self.language = try container.decodeIfPresent(String.self, forKey: .language) ?? ""
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
            self.totalCues = try container.decodeIfPresent(Int.self, forKey: .totalCues) ?? 0
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.sessionId, forKey: .sessionId)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encode(self.jobId, forKey: .jobId)
            try container.encode(self.trackKey, forKey: .trackKey)
            try container.encode(self.language, forKey: .language)
            try container.encode(self.label, forKey: .label)
            try container.encode(self.totalCues, forKey: .totalCues)
        }
    }

    /// Wire type `internal/playback.TerminalV3`. Direction: response. Dialect: upstream-compat.
    public struct TerminalV3: Codable, Hashable, Sendable {
        public let reason: String
        public let message: String
        public let retryable: Bool

        public enum CodingKeys: String, CodingKey {
            case reason = "reason"
            case message = "message"
            case retryable = "retryable"
        }

        public init(
            reason: String = "",
            message: String = "",
            retryable: Bool = false
        ) {
            self.reason = reason
            self.message = message
            self.retryable = retryable
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.reason = try container.decodeIfPresent(String.self, forKey: .reason) ?? ""
            self.message = try container.decodeIfPresent(String.self, forKey: .message) ?? ""
            self.retryable = try container.decodeIfPresent(Bool.self, forKey: .retryable) ?? false
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.reason, forKey: .reason)
            try container.encode(self.message, forKey: .message)
            try container.encode(self.retryable, forKey: .retryable)
        }
    }

    /// Wire type `internal/playback.TimeRangePayload`. Direction: response. Dialect: upstream-compat. Gate: realtime.
    public struct TimeRangePayload: Codable, Hashable, Sendable {
        public let start: Double
        public let end: Double

        public enum CodingKeys: String, CodingKey {
            case start = "start"
            case end = "end"
        }

        public init(
            start: Double = 0.0,
            end: Double = 0.0
        ) {
            self.start = start
            self.end = end
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.start = try container.decodeIfPresent(Double.self, forKey: .start) ?? 0.0
            self.end = try container.decodeIfPresent(Double.self, forKey: .end) ?? 0.0
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.start, forKey: .start)
            try container.encode(self.end, forKey: .end)
        }
    }

    /// Wire type `internal/playback.TimelineV3`. Direction: response. Dialect: upstream-compat.
    public struct TimelineV3: Codable, Hashable, Sendable {
        public let sourceStartSeconds: Double
        public let streamOriginSeconds: Double
        public let playerStartSeconds: Double
        public let timelineOffsetSeconds: Double
        public let seekWindowStartSeconds: Double?
        public let seekWindowEndSeconds: Double?
        public let canSeekAnywhere: Bool
        public let seekRestoration: String

        public enum CodingKeys: String, CodingKey {
            case sourceStartSeconds = "source_start_seconds"
            case streamOriginSeconds = "stream_origin_seconds"
            case playerStartSeconds = "player_start_seconds"
            case timelineOffsetSeconds = "timeline_offset_seconds"
            case seekWindowStartSeconds = "seek_window_start_seconds"
            case seekWindowEndSeconds = "seek_window_end_seconds"
            case canSeekAnywhere = "can_seek_anywhere"
            case seekRestoration = "seek_restoration"
        }

        public init(
            sourceStartSeconds: Double = 0.0,
            streamOriginSeconds: Double = 0.0,
            playerStartSeconds: Double = 0.0,
            timelineOffsetSeconds: Double = 0.0,
            seekWindowStartSeconds: Double? = nil,
            seekWindowEndSeconds: Double? = nil,
            canSeekAnywhere: Bool = false,
            seekRestoration: String = ""
        ) {
            self.sourceStartSeconds = sourceStartSeconds
            self.streamOriginSeconds = streamOriginSeconds
            self.playerStartSeconds = playerStartSeconds
            self.timelineOffsetSeconds = timelineOffsetSeconds
            self.seekWindowStartSeconds = seekWindowStartSeconds
            self.seekWindowEndSeconds = seekWindowEndSeconds
            self.canSeekAnywhere = canSeekAnywhere
            self.seekRestoration = seekRestoration
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.sourceStartSeconds = try container.decodeIfPresent(Double.self, forKey: .sourceStartSeconds) ?? 0.0
            self.streamOriginSeconds = try container.decodeIfPresent(Double.self, forKey: .streamOriginSeconds) ?? 0.0
            self.playerStartSeconds = try container.decodeIfPresent(Double.self, forKey: .playerStartSeconds) ?? 0.0
            self.timelineOffsetSeconds = try container.decodeIfPresent(Double.self, forKey: .timelineOffsetSeconds) ?? 0.0
            self.seekWindowStartSeconds = try container.decodeIfPresent(Double.self, forKey: .seekWindowStartSeconds)
            self.seekWindowEndSeconds = try container.decodeIfPresent(Double.self, forKey: .seekWindowEndSeconds)
            self.canSeekAnywhere = try container.decodeIfPresent(Bool.self, forKey: .canSeekAnywhere) ?? false
            self.seekRestoration = try container.decodeIfPresent(String.self, forKey: .seekRestoration) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.sourceStartSeconds, forKey: .sourceStartSeconds)
            try container.encode(self.streamOriginSeconds, forKey: .streamOriginSeconds)
            try container.encode(self.playerStartSeconds, forKey: .playerStartSeconds)
            try container.encode(self.timelineOffsetSeconds, forKey: .timelineOffsetSeconds)
            try container.encodeIfPresent(self.seekWindowStartSeconds, forKey: .seekWindowStartSeconds)
            try container.encodeIfPresent(self.seekWindowEndSeconds, forKey: .seekWindowEndSeconds)
            try container.encode(self.canSeekAnywhere, forKey: .canSeekAnywhere)
            try container.encode(self.seekRestoration, forKey: .seekRestoration)
        }
    }

    /// Wire type `internal/playback.TrackIdentityV3`. Direction: both. Dialect: upstream-compat.
    public struct TrackIdentityV3: Codable, Hashable, Sendable {
        public let id: String
        public let index: Int?

        public enum CodingKeys: String, CodingKey {
            case id = "id"
            case index = "index"
        }

        public init(
            id: String = "",
            index: Int? = nil
        ) {
            self.id = id
            self.index = index
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.id = try container.decodeIfPresent(String.self, forKey: .id) ?? ""
            self.index = try container.decodeIfPresent(Int.self, forKey: .index)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.id, forKey: .id)
            try container.encodeIfPresent(self.index, forKey: .index)
        }
    }

    /// Wire type `internal/playback.TransformationV3`. Direction: both. Dialect: upstream-compat.
    public struct TransformationV3: Codable, Hashable, Sendable {
        public let name: String
        public let executor: String
        public let recipeVersion: String
        public let validatedClaims: [String]

        public enum CodingKeys: String, CodingKey {
            case name = "name"
            case executor = "executor"
            case recipeVersion = "recipe_version"
            case validatedClaims = "validated_claims"
        }

        public init(
            name: String = "",
            executor: String = "",
            recipeVersion: String = "",
            validatedClaims: [String] = []
        ) {
            self.name = name
            self.executor = executor
            self.recipeVersion = recipeVersion
            self.validatedClaims = validatedClaims
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self.executor = try container.decodeIfPresent(String.self, forKey: .executor) ?? ""
            self.recipeVersion = try container.decodeIfPresent(String.self, forKey: .recipeVersion) ?? ""
            self.validatedClaims = try container.decodeIfPresent([String].self, forKey: .validatedClaims) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.executor, forKey: .executor)
            try container.encode(self.recipeVersion, forKey: .recipeVersion)
            try container.encode(self.validatedClaims, forKey: .validatedClaims)
        }
    }

    /// Wire type `internal/playback.ValidationClaimsV3`. Direction: response. Dialect: upstream-compat.
    public struct ValidationClaimsV3: Codable, Hashable, Sendable {
        public let video: VideoClaimsV3
        public let audio: AudioClaimsV3
        public let subtitles: SubtitleClaimsV3

        public enum CodingKeys: String, CodingKey {
            case video = "video"
            case audio = "audio"
            case subtitles = "subtitles"
        }

        public init(
            video: VideoClaimsV3 = VideoClaimsV3(),
            audio: AudioClaimsV3 = AudioClaimsV3(),
            subtitles: SubtitleClaimsV3 = SubtitleClaimsV3()
        ) {
            self.video = video
            self.audio = audio
            self.subtitles = subtitles
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.video = try container.decodeIfPresent(VideoClaimsV3.self, forKey: .video) ?? VideoClaimsV3()
            self.audio = try container.decodeIfPresent(AudioClaimsV3.self, forKey: .audio) ?? AudioClaimsV3()
            self.subtitles = try container.decodeIfPresent(SubtitleClaimsV3.self, forKey: .subtitles) ?? SubtitleClaimsV3()
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.video, forKey: .video)
            try container.encode(self.audio, forKey: .audio)
            try container.encode(self.subtitles, forKey: .subtitles)
        }
    }

    /// Wire type `internal/playback.VideoClaimsV3`. Direction: response. Dialect: upstream-compat.
    public struct VideoClaimsV3: Codable, Hashable, Sendable {
        public let hdr10: Bool
        public let hdr10Plus: Bool
        public let hlg: Bool
        public let dolbyVision: Bool
        public let dolbyVisionReason: String

        public enum CodingKeys: String, CodingKey {
            case hdr10 = "hdr10"
            case hdr10Plus = "hdr10_plus"
            case hlg = "hlg"
            case dolbyVision = "dolby_vision"
            case dolbyVisionReason = "dolby_vision_reason"
        }

        public init(
            hdr10: Bool = false,
            hdr10Plus: Bool = false,
            hlg: Bool = false,
            dolbyVision: Bool = false,
            dolbyVisionReason: String = ""
        ) {
            self.hdr10 = hdr10
            self.hdr10Plus = hdr10Plus
            self.hlg = hlg
            self.dolbyVision = dolbyVision
            self.dolbyVisionReason = dolbyVisionReason
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.hdr10 = try container.decodeIfPresent(Bool.self, forKey: .hdr10) ?? false
            self.hdr10Plus = try container.decodeIfPresent(Bool.self, forKey: .hdr10Plus) ?? false
            self.hlg = try container.decodeIfPresent(Bool.self, forKey: .hlg) ?? false
            self.dolbyVision = try container.decodeIfPresent(Bool.self, forKey: .dolbyVision) ?? false
            self.dolbyVisionReason = try container.decodeIfPresent(String.self, forKey: .dolbyVisionReason) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.hdr10, forKey: .hdr10)
            try container.encode(self.hdr10Plus, forKey: .hdr10Plus)
            try container.encode(self.hlg, forKey: .hlg)
            try container.encode(self.dolbyVision, forKey: .dolbyVision)
            try container.encode(self.dolbyVisionReason, forKey: .dolbyVisionReason)
        }
    }

    /// Wire type `internal/playback.VideoDecodeCapabilityV3`. Direction: request. Dialect: upstream-compat.
    public struct VideoDecodeCapabilityV3: Codable, Hashable, Sendable {
        public let codec: String
        public let decoderName: String
        public let profiles: [String]
        public let levels: [Int]
        public let bitDepths: [Int]
        public let maxWidth: Int
        public let maxHeight: Int
        public let maxFrameRate: Double
        public let maxBitrateKbps: Int
        public let hardware: Bool

        public enum CodingKeys: String, CodingKey {
            case codec = "codec"
            case decoderName = "decoder_name"
            case profiles = "profiles"
            case levels = "levels"
            case bitDepths = "bit_depths"
            case maxWidth = "max_width"
            case maxHeight = "max_height"
            case maxFrameRate = "max_frame_rate"
            case maxBitrateKbps = "max_bitrate_kbps"
            case hardware = "hardware"
        }

        public init(
            codec: String,
            decoderName: String = "",
            profiles: [String] = [],
            levels: [Int] = [],
            bitDepths: [Int] = [],
            maxWidth: Int = 0,
            maxHeight: Int = 0,
            maxFrameRate: Double = 0.0,
            maxBitrateKbps: Int = 0,
            hardware: Bool
        ) {
            self.codec = codec
            self.decoderName = decoderName
            self.profiles = profiles
            self.levels = levels
            self.bitDepths = bitDepths
            self.maxWidth = maxWidth
            self.maxHeight = maxHeight
            self.maxFrameRate = maxFrameRate
            self.maxBitrateKbps = maxBitrateKbps
            self.hardware = hardware
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.codec = try container.decode(String.self, forKey: .codec)
            self.decoderName = try container.decodeIfPresent(String.self, forKey: .decoderName) ?? ""
            self.profiles = try container.decodeIfPresent([String].self, forKey: .profiles) ?? []
            self.levels = try container.decodeIfPresent([Int].self, forKey: .levels) ?? []
            self.bitDepths = try container.decodeIfPresent([Int].self, forKey: .bitDepths) ?? []
            self.maxWidth = try container.decodeIfPresent(Int.self, forKey: .maxWidth) ?? 0
            self.maxHeight = try container.decodeIfPresent(Int.self, forKey: .maxHeight) ?? 0
            self.maxFrameRate = try container.decodeIfPresent(Double.self, forKey: .maxFrameRate) ?? 0.0
            self.maxBitrateKbps = try container.decodeIfPresent(Int.self, forKey: .maxBitrateKbps) ?? 0
            self.hardware = try container.decode(Bool.self, forKey: .hardware)
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.codec, forKey: .codec)
            try container.encode(self.decoderName, forKey: .decoderName)
            try container.encode(self.profiles, forKey: .profiles)
            try container.encode(self.levels, forKey: .levels)
            try container.encode(self.bitDepths, forKey: .bitDepths)
            try container.encode(self.maxWidth, forKey: .maxWidth)
            try container.encode(self.maxHeight, forKey: .maxHeight)
            try container.encode(self.maxFrameRate, forKey: .maxFrameRate)
            try container.encode(self.maxBitrateKbps, forKey: .maxBitrateKbps)
            try container.encode(self.hardware, forKey: .hardware)
        }
    }
}
