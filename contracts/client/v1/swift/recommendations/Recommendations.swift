// Code generated by cmd/clientdtogen from bloem-server internal/recommendations. DO NOT EDIT.
// Server revision: 8c612bd9 (generator v1, registry contracts/client/v1/registry.json)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `internal/recommendations`.
public enum Recommendations {
    /// Wire type `internal/recommendations.ForYouRow`. Direction: response. Dialect: upstream-compat.
    public struct ForYouRow: Codable, Hashable, Sendable {
        public let type: String
        public let label: String
        public let clusterIndex: Int
        public let items: [ScoredItem]

        public enum CodingKeys: String, CodingKey {
            case type = "type"
            case label = "label"
            case clusterIndex = "cluster_index"
            case items = "items"
        }

        public init(
            type: String = "",
            label: String = "",
            clusterIndex: Int = 0,
            items: [ScoredItem] = []
        ) {
            self.type = type
            self.label = label
            self.clusterIndex = clusterIndex
            self.items = items
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.type = try container.decodeIfPresent(String.self, forKey: .type) ?? ""
            self.label = try container.decodeIfPresent(String.self, forKey: .label) ?? ""
            self.clusterIndex = try container.decodeIfPresent(Int.self, forKey: .clusterIndex) ?? 0
            self.items = try container.decodeIfPresent([ScoredItem].self, forKey: .items) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.type, forKey: .type)
            try container.encode(self.label, forKey: .label)
            try container.encode(self.clusterIndex, forKey: .clusterIndex)
            try container.encode(self.items, forKey: .items)
        }
    }

    /// Wire type `internal/recommendations.ScoredItem`. Direction: response. Dialect: upstream-compat.
    public struct ScoredItem: Codable, Hashable, Sendable {
        public let mediaItemId: String
        public let score: Double
        public let reason: String
        public let reasonDetail: String

        public enum CodingKeys: String, CodingKey {
            case mediaItemId = "media_item_id"
            case score = "score"
            case reason = "reason"
            case reasonDetail = "reason_detail"
        }

        public init(
            mediaItemId: String = "",
            score: Double = 0.0,
            reason: String = "",
            reasonDetail: String = ""
        ) {
            self.mediaItemId = mediaItemId
            self.score = score
            self.reason = reason
            self.reasonDetail = reasonDetail
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.mediaItemId = try container.decodeIfPresent(String.self, forKey: .mediaItemId) ?? ""
            self.score = try container.decodeIfPresent(Double.self, forKey: .score) ?? 0.0
            self.reason = try container.decodeIfPresent(String.self, forKey: .reason) ?? ""
            self.reasonDetail = try container.decodeIfPresent(String.self, forKey: .reasonDetail) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.mediaItemId, forKey: .mediaItemId)
            try container.encode(self.score, forKey: .score)
            try container.encode(self.reason, forKey: .reason)
            try container.encode(self.reasonDetail, forKey: .reasonDetail)
        }
    }
}
