// Code generated by cmd/clientdtogen from bloem-server internal/watchdoc. DO NOT EDIT.
// Server revision: 8c612bd9 (generator v1, registry contracts/client/v1/registry.json)
// Regenerate in the server repo with: make client-dtos
//
// Decoding is exact: every initializer reads an absent or null key as the documented
// default, so a server that omits a key still decodes, and encoding omits a nil value
// rather than writing null. Requires the client-owned BloemJSONValue (a Codable, Hashable,
// Sendable JSON value) and Indirect (a box for self-referential fields).

/// Wire types of the Go package `internal/watchdoc`.
public enum Watchdoc {
    /// Wire type `internal/watchdoc.Document`. Direction: response. Dialect: bloem. Registered root.
    public struct Document: Codable, Hashable, Sendable {
        public let schema: String
        public let snapshot: String
        public let featuredContentId: String
        public let items: [DocumentItem]
        public let progress: [DocumentProgress]

        public enum CodingKeys: String, CodingKey {
            case schema = "schema"
            case snapshot = "snapshot"
            case featuredContentId = "featured_content_id"
            case items = "items"
            case progress = "progress"
        }

        public init(
            schema: String = "",
            snapshot: String = "",
            featuredContentId: String = "",
            items: [DocumentItem] = [],
            progress: [DocumentProgress] = []
        ) {
            self.schema = schema
            self.snapshot = snapshot
            self.featuredContentId = featuredContentId
            self.items = items
            self.progress = progress
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.schema = try container.decodeIfPresent(String.self, forKey: .schema) ?? ""
            self.snapshot = try container.decodeIfPresent(String.self, forKey: .snapshot) ?? ""
            self.featuredContentId = try container.decodeIfPresent(String.self, forKey: .featuredContentId) ?? ""
            self.items = try container.decodeIfPresent([DocumentItem].self, forKey: .items) ?? []
            self.progress = try container.decodeIfPresent([DocumentProgress].self, forKey: .progress) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.schema, forKey: .schema)
            try container.encode(self.snapshot, forKey: .snapshot)
            try container.encode(self.featuredContentId, forKey: .featuredContentId)
            try container.encode(self.items, forKey: .items)
            try container.encode(self.progress, forKey: .progress)
        }
    }

    /// Wire type `internal/watchdoc.DocumentCastMember`. Direction: response. Dialect: bloem.
    public struct DocumentCastMember: Codable, Hashable, Sendable {
        public let personId: String
        public let name: String
        public let character: String
        public let photoUrl: String

        public enum CodingKeys: String, CodingKey {
            case personId = "person_id"
            case name = "name"
            case character = "character"
            case photoUrl = "photo_url"
        }

        public init(
            personId: String = "",
            name: String = "",
            character: String = "",
            photoUrl: String = ""
        ) {
            self.personId = personId
            self.name = name
            self.character = character
            self.photoUrl = photoUrl
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.personId = try container.decodeIfPresent(String.self, forKey: .personId) ?? ""
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self.character = try container.decodeIfPresent(String.self, forKey: .character) ?? ""
            self.photoUrl = try container.decodeIfPresent(String.self, forKey: .photoUrl) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.personId, forKey: .personId)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.character, forKey: .character)
            try container.encode(self.photoUrl, forKey: .photoUrl)
        }
    }

    /// Wire type `internal/watchdoc.DocumentChapter`. Direction: response. Dialect: bloem.
    public struct DocumentChapter: Codable, Hashable, Sendable {
        public let index: Int
        public let title: String
        public let startSeconds: Double
        public let endSeconds: Double

        public enum CodingKeys: String, CodingKey {
            case index = "index"
            case title = "title"
            case startSeconds = "start_seconds"
            case endSeconds = "end_seconds"
        }

        public init(
            index: Int = 0,
            title: String = "",
            startSeconds: Double = 0.0,
            endSeconds: Double = 0.0
        ) {
            self.index = index
            self.title = title
            self.startSeconds = startSeconds
            self.endSeconds = endSeconds
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.index = try container.decodeIfPresent(Int.self, forKey: .index) ?? 0
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.startSeconds = try container.decodeIfPresent(Double.self, forKey: .startSeconds) ?? 0.0
            self.endSeconds = try container.decodeIfPresent(Double.self, forKey: .endSeconds) ?? 0.0
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.index, forKey: .index)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.startSeconds, forKey: .startSeconds)
            try container.encode(self.endSeconds, forKey: .endSeconds)
        }
    }

    /// Wire type `internal/watchdoc.DocumentCrewMember`. Direction: response. Dialect: bloem.
    public struct DocumentCrewMember: Codable, Hashable, Sendable {
        public let personId: String
        public let name: String
        public let job: String
        public let photoUrl: String

        public enum CodingKeys: String, CodingKey {
            case personId = "person_id"
            case name = "name"
            case job = "job"
            case photoUrl = "photo_url"
        }

        public init(
            personId: String = "",
            name: String = "",
            job: String = "",
            photoUrl: String = ""
        ) {
            self.personId = personId
            self.name = name
            self.job = job
            self.photoUrl = photoUrl
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.personId = try container.decodeIfPresent(String.self, forKey: .personId) ?? ""
            self.name = try container.decodeIfPresent(String.self, forKey: .name) ?? ""
            self.job = try container.decodeIfPresent(String.self, forKey: .job) ?? ""
            self.photoUrl = try container.decodeIfPresent(String.self, forKey: .photoUrl) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.personId, forKey: .personId)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.job, forKey: .job)
            try container.encode(self.photoUrl, forKey: .photoUrl)
        }
    }

    /// Wire type `internal/watchdoc.DocumentEdition`. Direction: response. Dialect: bloem.
    public struct DocumentEdition: Codable, Hashable, Sendable {
        public let fileId: Int64
        public let resolution: String
        public let codecVideo: String
        public let codecAudio: String
        public let hdr: Bool
        public let container: String
        public let durationSeconds: Double
        public let editionKey: String
        public let editionLabel: String

        public enum CodingKeys: String, CodingKey {
            case fileId = "file_id"
            case resolution = "resolution"
            case codecVideo = "codec_video"
            case codecAudio = "codec_audio"
            case hdr = "hdr"
            case container = "container"
            case durationSeconds = "duration_seconds"
            case editionKey = "edition_key"
            case editionLabel = "edition_label"
        }

        public init(
            fileId: Int64 = 0,
            resolution: String = "",
            codecVideo: String = "",
            codecAudio: String = "",
            hdr: Bool = false,
            container: String = "",
            durationSeconds: Double = 0.0,
            editionKey: String = "",
            editionLabel: String = ""
        ) {
            self.fileId = fileId
            self.resolution = resolution
            self.codecVideo = codecVideo
            self.codecAudio = codecAudio
            self.hdr = hdr
            self.container = container
            self.durationSeconds = durationSeconds
            self.editionKey = editionKey
            self.editionLabel = editionLabel
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.fileId = try container.decodeIfPresent(Int64.self, forKey: .fileId) ?? 0
            self.resolution = try container.decodeIfPresent(String.self, forKey: .resolution) ?? ""
            self.codecVideo = try container.decodeIfPresent(String.self, forKey: .codecVideo) ?? ""
            self.codecAudio = try container.decodeIfPresent(String.self, forKey: .codecAudio) ?? ""
            self.hdr = try container.decodeIfPresent(Bool.self, forKey: .hdr) ?? false
            self.container = try container.decodeIfPresent(String.self, forKey: .container) ?? ""
            self.durationSeconds = try container.decodeIfPresent(Double.self, forKey: .durationSeconds) ?? 0.0
            self.editionKey = try container.decodeIfPresent(String.self, forKey: .editionKey) ?? ""
            self.editionLabel = try container.decodeIfPresent(String.self, forKey: .editionLabel) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encode(self.resolution, forKey: .resolution)
            try container.encode(self.codecVideo, forKey: .codecVideo)
            try container.encode(self.codecAudio, forKey: .codecAudio)
            try container.encode(self.hdr, forKey: .hdr)
            try container.encode(self.container, forKey: .container)
            try container.encode(self.durationSeconds, forKey: .durationSeconds)
            try container.encode(self.editionKey, forKey: .editionKey)
            try container.encode(self.editionLabel, forKey: .editionLabel)
        }
    }

    /// Wire type `internal/watchdoc.DocumentItem`. Direction: response. Dialect: bloem.
    public struct DocumentItem: Codable, Hashable, Sendable {
        public let kind: String
        public let contentId: String
        public let title: String
        public let year: Int
        public let runtimeSeconds: Int
        public let rating: String
        public let overview: String
        public let genres: [String]
        public let keywords: [String]
        public let posterUrl: String
        public let seasonCount: Int
        public let seasons: [DocumentSeason]
        public let seriesId: String
        public let seasonNumber: Int
        public let episodeNumber: Int
        public let fileId: Int64
        public let featured: Bool
        public let chapters: [DocumentChapter]
        public let introStartSeconds: Double?
        public let introEndSeconds: Double?
        public let creditsStartSeconds: Double?
        public let creditsEndSeconds: Double?
        public let recapStartSeconds: Double?
        public let recapEndSeconds: Double?
        public let previewStartSeconds: Double?
        public let previewEndSeconds: Double?
        public let cast: [DocumentCastMember]
        public let crew: [DocumentCrewMember]
        public let editions: [DocumentEdition]

        public enum CodingKeys: String, CodingKey {
            case kind = "kind"
            case contentId = "content_id"
            case title = "title"
            case year = "year"
            case runtimeSeconds = "runtime_seconds"
            case rating = "rating"
            case overview = "overview"
            case genres = "genres"
            case keywords = "keywords"
            case posterUrl = "poster_url"
            case seasonCount = "season_count"
            case seasons = "seasons"
            case seriesId = "series_id"
            case seasonNumber = "season_number"
            case episodeNumber = "episode_number"
            case fileId = "file_id"
            case featured = "featured"
            case chapters = "chapters"
            case introStartSeconds = "intro_start_seconds"
            case introEndSeconds = "intro_end_seconds"
            case creditsStartSeconds = "credits_start_seconds"
            case creditsEndSeconds = "credits_end_seconds"
            case recapStartSeconds = "recap_start_seconds"
            case recapEndSeconds = "recap_end_seconds"
            case previewStartSeconds = "preview_start_seconds"
            case previewEndSeconds = "preview_end_seconds"
            case cast = "cast"
            case crew = "crew"
            case editions = "editions"
        }

        public init(
            kind: String = "",
            contentId: String = "",
            title: String = "",
            year: Int = 0,
            runtimeSeconds: Int = 0,
            rating: String = "",
            overview: String = "",
            genres: [String] = [],
            keywords: [String] = [],
            posterUrl: String = "",
            seasonCount: Int = 0,
            seasons: [DocumentSeason] = [],
            seriesId: String = "",
            seasonNumber: Int = 0,
            episodeNumber: Int = 0,
            fileId: Int64 = 0,
            featured: Bool = false,
            chapters: [DocumentChapter] = [],
            introStartSeconds: Double? = nil,
            introEndSeconds: Double? = nil,
            creditsStartSeconds: Double? = nil,
            creditsEndSeconds: Double? = nil,
            recapStartSeconds: Double? = nil,
            recapEndSeconds: Double? = nil,
            previewStartSeconds: Double? = nil,
            previewEndSeconds: Double? = nil,
            cast: [DocumentCastMember] = [],
            crew: [DocumentCrewMember] = [],
            editions: [DocumentEdition] = []
        ) {
            self.kind = kind
            self.contentId = contentId
            self.title = title
            self.year = year
            self.runtimeSeconds = runtimeSeconds
            self.rating = rating
            self.overview = overview
            self.genres = genres
            self.keywords = keywords
            self.posterUrl = posterUrl
            self.seasonCount = seasonCount
            self.seasons = seasons
            self.seriesId = seriesId
            self.seasonNumber = seasonNumber
            self.episodeNumber = episodeNumber
            self.fileId = fileId
            self.featured = featured
            self.chapters = chapters
            self.introStartSeconds = introStartSeconds
            self.introEndSeconds = introEndSeconds
            self.creditsStartSeconds = creditsStartSeconds
            self.creditsEndSeconds = creditsEndSeconds
            self.recapStartSeconds = recapStartSeconds
            self.recapEndSeconds = recapEndSeconds
            self.previewStartSeconds = previewStartSeconds
            self.previewEndSeconds = previewEndSeconds
            self.cast = cast
            self.crew = crew
            self.editions = editions
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.kind = try container.decodeIfPresent(String.self, forKey: .kind) ?? ""
            self.contentId = try container.decodeIfPresent(String.self, forKey: .contentId) ?? ""
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
            self.year = try container.decodeIfPresent(Int.self, forKey: .year) ?? 0
            self.runtimeSeconds = try container.decodeIfPresent(Int.self, forKey: .runtimeSeconds) ?? 0
            self.rating = try container.decodeIfPresent(String.self, forKey: .rating) ?? ""
            self.overview = try container.decodeIfPresent(String.self, forKey: .overview) ?? ""
            self.genres = try container.decodeIfPresent([String].self, forKey: .genres) ?? []
            self.keywords = try container.decodeIfPresent([String].self, forKey: .keywords) ?? []
            self.posterUrl = try container.decodeIfPresent(String.self, forKey: .posterUrl) ?? ""
            self.seasonCount = try container.decodeIfPresent(Int.self, forKey: .seasonCount) ?? 0
            self.seasons = try container.decodeIfPresent([DocumentSeason].self, forKey: .seasons) ?? []
            self.seriesId = try container.decodeIfPresent(String.self, forKey: .seriesId) ?? ""
            self.seasonNumber = try container.decodeIfPresent(Int.self, forKey: .seasonNumber) ?? 0
            self.episodeNumber = try container.decodeIfPresent(Int.self, forKey: .episodeNumber) ?? 0
            self.fileId = try container.decodeIfPresent(Int64.self, forKey: .fileId) ?? 0
            self.featured = try container.decodeIfPresent(Bool.self, forKey: .featured) ?? false
            self.chapters = try container.decodeIfPresent([DocumentChapter].self, forKey: .chapters) ?? []
            self.introStartSeconds = try container.decodeIfPresent(Double.self, forKey: .introStartSeconds)
            self.introEndSeconds = try container.decodeIfPresent(Double.self, forKey: .introEndSeconds)
            self.creditsStartSeconds = try container.decodeIfPresent(Double.self, forKey: .creditsStartSeconds)
            self.creditsEndSeconds = try container.decodeIfPresent(Double.self, forKey: .creditsEndSeconds)
            self.recapStartSeconds = try container.decodeIfPresent(Double.self, forKey: .recapStartSeconds)
            self.recapEndSeconds = try container.decodeIfPresent(Double.self, forKey: .recapEndSeconds)
            self.previewStartSeconds = try container.decodeIfPresent(Double.self, forKey: .previewStartSeconds)
            self.previewEndSeconds = try container.decodeIfPresent(Double.self, forKey: .previewEndSeconds)
            self.cast = try container.decodeIfPresent([DocumentCastMember].self, forKey: .cast) ?? []
            self.crew = try container.decodeIfPresent([DocumentCrewMember].self, forKey: .crew) ?? []
            self.editions = try container.decodeIfPresent([DocumentEdition].self, forKey: .editions) ?? []
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.kind, forKey: .kind)
            try container.encode(self.contentId, forKey: .contentId)
            try container.encode(self.title, forKey: .title)
            try container.encode(self.year, forKey: .year)
            try container.encode(self.runtimeSeconds, forKey: .runtimeSeconds)
            try container.encode(self.rating, forKey: .rating)
            try container.encode(self.overview, forKey: .overview)
            try container.encode(self.genres, forKey: .genres)
            try container.encode(self.keywords, forKey: .keywords)
            try container.encode(self.posterUrl, forKey: .posterUrl)
            try container.encode(self.seasonCount, forKey: .seasonCount)
            try container.encode(self.seasons, forKey: .seasons)
            try container.encode(self.seriesId, forKey: .seriesId)
            try container.encode(self.seasonNumber, forKey: .seasonNumber)
            try container.encode(self.episodeNumber, forKey: .episodeNumber)
            try container.encode(self.fileId, forKey: .fileId)
            try container.encode(self.featured, forKey: .featured)
            try container.encode(self.chapters, forKey: .chapters)
            try container.encodeIfPresent(self.introStartSeconds, forKey: .introStartSeconds)
            try container.encodeIfPresent(self.introEndSeconds, forKey: .introEndSeconds)
            try container.encodeIfPresent(self.creditsStartSeconds, forKey: .creditsStartSeconds)
            try container.encodeIfPresent(self.creditsEndSeconds, forKey: .creditsEndSeconds)
            try container.encodeIfPresent(self.recapStartSeconds, forKey: .recapStartSeconds)
            try container.encodeIfPresent(self.recapEndSeconds, forKey: .recapEndSeconds)
            try container.encodeIfPresent(self.previewStartSeconds, forKey: .previewStartSeconds)
            try container.encodeIfPresent(self.previewEndSeconds, forKey: .previewEndSeconds)
            try container.encode(self.cast, forKey: .cast)
            try container.encode(self.crew, forKey: .crew)
            try container.encode(self.editions, forKey: .editions)
        }
    }

    /// Wire type `internal/watchdoc.DocumentProgress`. Direction: response. Dialect: bloem.
    public struct DocumentProgress: Codable, Hashable, Sendable {
        public let contentId: String
        public let episodeId: String
        public let positionSeconds: Double
        public let durationSeconds: Double
        public let completed: Bool
        public let updatedAt: String

        public enum CodingKeys: String, CodingKey {
            case contentId = "content_id"
            case episodeId = "episode_id"
            case positionSeconds = "position_seconds"
            case durationSeconds = "duration_seconds"
            case completed = "completed"
            case updatedAt = "updated_at"
        }

        public init(
            contentId: String = "",
            episodeId: String = "",
            positionSeconds: Double = 0.0,
            durationSeconds: Double = 0.0,
            completed: Bool = false,
            updatedAt: String = ""
        ) {
            self.contentId = contentId
            self.episodeId = episodeId
            self.positionSeconds = positionSeconds
            self.durationSeconds = durationSeconds
            self.completed = completed
            self.updatedAt = updatedAt
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.contentId = try container.decodeIfPresent(String.self, forKey: .contentId) ?? ""
            self.episodeId = try container.decodeIfPresent(String.self, forKey: .episodeId) ?? ""
            self.positionSeconds = try container.decodeIfPresent(Double.self, forKey: .positionSeconds) ?? 0.0
            self.durationSeconds = try container.decodeIfPresent(Double.self, forKey: .durationSeconds) ?? 0.0
            self.completed = try container.decodeIfPresent(Bool.self, forKey: .completed) ?? false
            self.updatedAt = try container.decodeIfPresent(String.self, forKey: .updatedAt) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.contentId, forKey: .contentId)
            try container.encode(self.episodeId, forKey: .episodeId)
            try container.encode(self.positionSeconds, forKey: .positionSeconds)
            try container.encode(self.durationSeconds, forKey: .durationSeconds)
            try container.encode(self.completed, forKey: .completed)
            try container.encode(self.updatedAt, forKey: .updatedAt)
        }
    }

    /// Wire type `internal/watchdoc.DocumentSeason`. Direction: response. Dialect: bloem.
    public struct DocumentSeason: Codable, Hashable, Sendable {
        public let seasonNumber: Int
        public let title: String

        public enum CodingKeys: String, CodingKey {
            case seasonNumber = "season_number"
            case title = "title"
        }

        public init(
            seasonNumber: Int = 0,
            title: String = ""
        ) {
            self.seasonNumber = seasonNumber
            self.title = title
        }

        public init(from decoder: any Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.seasonNumber = try container.decodeIfPresent(Int.self, forKey: .seasonNumber) ?? 0
            self.title = try container.decodeIfPresent(String.self, forKey: .title) ?? ""
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.seasonNumber, forKey: .seasonNumber)
            try container.encode(self.title, forKey: .title)
        }
    }
}
